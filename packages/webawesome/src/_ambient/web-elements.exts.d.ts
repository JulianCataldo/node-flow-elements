// ---
// THIS FILE IS AUTO-GENERATED BY WEA (Web Elements Analyzer).
// DO NOT EDIT MANUALLY — your changes will be overwritten.
// ---
interface CustomElementsExternals {
	/**
	 * ```jsx
	 * class WaIcon extends WebAwesomeElement (<wa-icon>)
	 * ```
	 * ---
	 * @summary Icons are scalable vector symbols that represent actions, content, or status throughout your application.
	 * They support Font Awesome and custom icon libraries with animation presets.
	 * @fires `wa-load` — {`Event`} — Emitted when the icon has loaded. When using `spriteSheet: true` this will not emit.
	 * @fires `wa-error` — {`Event`} — Emitted when the icon fails to load due to an error. When using `spriteSheet: true` this will not emit.
	 * @attribute `name` — {`string | undefined`} — The name of the icon to draw. Available names depend on the icon library being used.
	 * @attribute `family` — {`string`} — The family of icons to choose from. For Font Awesome Free, valid options include `classic` and `brands`. For
	 * Font Awesome Pro subscribers, valid options include, `classic`, `sharp`, `duotone`, `sharp-duotone`, and `brands`.
	 * A valid kit code must be present to show pro icons via CDN. You can set `<html data-fa-kit-code="...">` to provide
	 * one.
	 * @attribute `variant` — {`string`} — The name of the icon's variant. For Font Awesome, valid options include `thin`, `light`, `regular`, and `solid` for
	 * the `classic` and `sharp` families. Some variants require a Font Awesome Pro subscription. Custom icon libraries
	 * may or may not use this property.
	 * @attribute `auto-width` — {`boolean`} — Sets the width of the icon to match the cropped SVG viewBox. This operates like the Font `fa-width-auto` class.
	 * @attribute `swap-opacity` — {`boolean`} — Swaps the opacity of duotone icons.
	 * @attribute `src` — {`string | undefined`} — An external URL of an SVG file. Be sure you trust the content you are including, as it will be executed as code and
	 * can result in XSS attacks.
	 * @attribute `label` — {`string`} — An alternate description to use for assistive devices. If omitted, the icon will be considered presentational and
	 * ignored by assistive devices.
	 * @attribute `library` — {`string`} — The name of a registered custom icon library.
	 * @attribute `rotate` — {`number`} — Sets the rotation degree of the icon
	 * @attribute `flip` — {`'x' | 'y' | 'both' | undefined`} — Sets the flip direction of the icon along the 'x' (horizontal), 'y' (vertical), or 'both' axes.
	 * @attribute `animation` — {`IconAnimation | undefined`} — Sets the animation for the icon
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `name` — {`string | undefined`} — The name of the icon to draw. Available names depend on the icon library being used.
	 * @property `family` — {`string`} — The family of icons to choose from. For Font Awesome Free, valid options include `classic` and `brands`. For
	 * Font Awesome Pro subscribers, valid options include, `classic`, `sharp`, `duotone`, `sharp-duotone`, and `brands`.
	 * A valid kit code must be present to show pro icons via CDN. You can set `<html data-fa-kit-code="...">` to provide
	 * one.
	 * @property `variant` — {`string`} — The name of the icon's variant. For Font Awesome, valid options include `thin`, `light`, `regular`, and `solid` for
	 * the `classic` and `sharp` families. Some variants require a Font Awesome Pro subscription. Custom icon libraries
	 * may or may not use this property.
	 * @property `autoWidth` — {`boolean`} — Sets the width of the icon to match the cropped SVG viewBox. This operates like the Font `fa-width-auto` class.
	 * @property `swapOpacity` — {`boolean`} — Swaps the opacity of duotone icons.
	 * @property `src` — {`string | undefined`} — An external URL of an SVG file. Be sure you trust the content you are including, as it will be executed as code and
	 * can result in XSS attacks.
	 * @property `label` — {`string`} — An alternate description to use for assistive devices. If omitted, the icon will be considered presentational and
	 * ignored by assistive devices.
	 * @property `library` — {`string`} — The name of a registered custom icon library.
	 * @property `rotate` — {`number`} — Sets the rotation degree of the icon
	 * @property `flip` — {`'x' | 'y' | 'both' | undefined`} — Sets the flip direction of the icon along the 'x' (horizontal), 'y' (vertical), or 'both' axes.
	 * @property `animation` — {`IconAnimation | undefined`} — Sets the animation for the icon
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `svg` — The internal SVG element.
	 * @cssPart `use` — The `<use>` element generated when using `spriteSheet: true`
	 * @cssProperty `--animation-delay` — Sets when the animation will start.
	 * @cssProperty `--animation-direction` — Defines whether or not the animation should play in reverse on alternate cycles.
	 * @cssProperty `--animation-duration` — Defines the length of time that an animation takes to complete one cycle.
	 * @cssProperty `--animation-iteration-count` — Defines the number of times an animation cycle is played.
	 * @cssProperty `--animation-timing` — Describes how the animation will progress over one cycle of its duration.
	 * @cssProperty `--beat-fade-opacity` — Set lowest opacity value an icon with `beat-fade` animation will fade to and from.
	 * @cssProperty `--beat-fade-scale` — Set max value that an icon with `beat-fade` animation will scale.
	 * @cssProperty `--beat-scale` — Set max value that an icon with `beat` animation will scale.
	 * @cssProperty `--bounce-height` — Set the max height an icon with `bounce` animation will jump to when bouncing.
	 * @cssProperty `--bounce-jump-scale-x` — Set the icon’s horizontal distortion (“squish”) at the top of the jump.
	 * @cssProperty `--bounce-jump-scale-y` — Set the icon’s vertical distortion (“squish”) at the top of the jump.
	 * @cssProperty `--bounce-land-scale-x` — Set the icon’s horizontal distortion (“squish”) when landing after the jump.
	 * @cssProperty `--bounce-land-scale-y` — Set the icon’s vertical distortion (“squish”) when landing after the jump.
	 * @cssProperty `--bounce-rebound` — Set the amount of rebound an icon with `bounce` animation has when landing after the jump.
	 * @cssProperty `--bounce-start-scale-x` — Set the icon’s horizontal distortion (“squish”) when starting to bounce.
	 * @cssProperty `--bounce-start-scale-y` — Set the icon’s vertical distortion (“squish”) when starting to bounce.
	 * @cssProperty `--fade-opacity` — Set lowest opacity value an icon with `fade` animation will fade to and from.
	 * @cssProperty `--flip-angle` — Set rotation angle of flip for an icon with `flip` animation. A positive angle denotes a clockwise rotation, a negative angle a counter-clockwise one.
	 * @cssProperty `--flip-x` — Set x-coordinate of the vector denoting the axis of rotation (between 0 and 1) for an icon with `flip` animation.
	 * @cssProperty `--flip-y` — Set y-coordinate of the vector denoting the axis of rotation (between 0 and 1) for an icon with `flip` animation.
	 * @cssProperty `--flip-z` — Set z-coordinate of the vector denoting the axis of rotation (between 0 and 1) for an icon with `flip` animation.
	 * @cssProperty `--primary-color` — Sets a duotone icon's primary color.
	 * @cssProperty `--primary-opacity` — Sets a duotone icon's primary opacity.
	 * @cssProperty `--secondary-color` — Sets a duotone icon's secondary color.
	 * @cssProperty `--secondary-opacity` — Sets a duotone icon's secondary opacity.
	 */
	'wa-icon': {
		elementClass: HTMLElementTagNameMap['wa-icon'];
		properties: {
			/**
			 * @attribute `name`
			 * @reflects
			 */
			name?: HTMLElementTagNameMap['wa-icon']['name'];
			/**
			 * @attribute `family`
			 * @reflects
			 */
			family?: HTMLElementTagNameMap['wa-icon']['family'];
			/**
			 * @attribute `variant`
			 * @reflects
			 */
			variant?: HTMLElementTagNameMap['wa-icon']['variant'];
			/**
			 * @attribute `auto-width`
			 * @reflects
			 */
			autoWidth?: HTMLElementTagNameMap['wa-icon']['autoWidth'];
			/**
			 * @attribute `swap-opacity`
			 * @reflects
			 */
			swapOpacity?: HTMLElementTagNameMap['wa-icon']['swapOpacity'];
			/**
			 * @attribute `src`
			 */
			src?: HTMLElementTagNameMap['wa-icon']['src'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-icon']['label'];
			/**
			 * @attribute `library`
			 * @reflects
			 */
			library?: HTMLElementTagNameMap['wa-icon']['library'];
			/**
			 * @attribute `rotate`
			 * @reflects
			 */
			rotate?: HTMLElementTagNameMap['wa-icon']['rotate'];
			/**
			 * @attribute `flip`
			 * @reflects
			 */
			flip?: HTMLElementTagNameMap['wa-icon']['flip'];
			/**
			 * @attribute `animation`
			 * @reflects
			 */
			animation?: HTMLElementTagNameMap['wa-icon']['animation'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-icon']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-icon']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-icon']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-icon']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-icon']['didSSR'];
		};
		attributes: {
			/**
			 * @property `name`
			 */
			name?: string | undefined;
			/**
			 * @property `family`
			 */
			family?: string;
			/**
			 * @property `variant`
			 */
			variant?: string;
			/**
			 * @property `autoWidth`
			 */
			'auto-width'?: boolean;
			/**
			 * @property `swapOpacity`
			 */
			'swap-opacity'?: boolean;
			/**
			 * @property `src`
			 */
			src?: string | undefined;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `library`
			 */
			library?: string;
			/**
			 * @property `rotate`
			 */
			rotate?: number;
			/**
			 * @property `flip`
			 */
			flip?: 'x' | 'y' | 'both' | undefined;
			/**
			 * @property `animation`
			 */
			animation?: unknown | undefined;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-load'?: GlobalEventHandlersEventMap['wa-load'];
			'wa-error'?: GlobalEventHandlersEventMap['wa-error'];
		};
		cssProperties: {
			'--animation-delay'?: string;
			'--animation-direction'?: string;
			'--animation-duration'?: string;
			'--animation-iteration-count'?: string;
			'--animation-timing'?: string;
			'--beat-fade-opacity'?: string;
			'--beat-fade-scale'?: string;
			'--beat-scale'?: string;
			'--bounce-height'?: string;
			'--bounce-jump-scale-x'?: string;
			'--bounce-jump-scale-y'?: string;
			'--bounce-land-scale-x'?: string;
			'--bounce-land-scale-y'?: string;
			'--bounce-rebound'?: string;
			'--bounce-start-scale-x'?: string;
			'--bounce-start-scale-y'?: string;
			'--fade-opacity'?: string;
			'--flip-angle'?: string;
			'--flip-x'?: string;
			'--flip-y'?: string;
			'--flip-z'?: string;
			'--primary-color'?: string;
			'--primary-opacity'?: string;
			'--secondary-color'?: string;
			'--secondary-opacity'?: string;
		};
		cssParts: {
			svg?: string;
			use?: string;
		};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaCheckbox extends WebAwesomeFormAssociatedElement (<wa-checkbox>)
	 * ```
	 * ---
	 * @summary Checkboxes let users toggle an option on or off, or select multiple items from a list. They also support an
	 * indeterminate state for partial selections in groups.
	 * @slot [***default***] — The checkbox's label.
	 * @slot `hint` — Text that describes how to use the checkbox. Alternatively, you can use the `hint` attribute.
	 * @fires `change` — {`Event`} — Emitted when the checked state changes.
	 * @fires `blur` — {`Event`} — Emitted when the checkbox loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the checkbox gains focus.
	 * @fires `input` — {`Event`} — Emitted when the checkbox receives input.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `title` — {`string`}
	 * @attribute `name` — {`string | null`} — The name of the checkbox, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `value` — {`string | null`} — The value of the checkbox, submitted as a name/value pair with form data.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The checkbox's size.
	 * @attribute `disabled` — {`boolean`} — Disables the checkbox. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `indeterminate` — {`boolean`} — Draws the checkbox in an indeterminate state. This is usually applied to checkboxes that represents a "select
	 * all/none" behavior when associated checkboxes have a mix of checked and unchecked states.
	 * @attribute `checked` — {`boolean`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `required` — {`boolean`} — Makes the checkbox a required field.
	 * @attribute `hint` — {`string`} — The checkbox's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `title` — {`string`}
	 * @property `name` — {`string | null`} — The name of the checkbox, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `value` — {`string | null`} — The value of the checkbox, submitted as a name/value pair with form data.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The checkbox's size.
	 * @property `disabled` — {`boolean`} — Disables the checkbox. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `indeterminate` — {`boolean`} — Draws the checkbox in an indeterminate state. This is usually applied to checkboxes that represents a "select
	 * all/none" behavior when associated checkboxes have a mix of checked and unchecked states.
	 * @property `_checked` — {`boolean | null`}
	 * @property `checked` — Draws the checkbox in a checked state.
	 * @property `defaultChecked` — {`boolean`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `required` — {`boolean`} — Makes the checkbox a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hint` — {`string`} — The checkbox's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's label .
	 * @cssPart `control` — The square container that wraps the checkbox's checked state.
	 * @cssPart `checked-icon` — The checked icon, a `<wa-icon>` element.
	 * @cssPart `indeterminate-icon` — The indeterminate icon, a `<wa-icon>` element.
	 * @cssPart `label` — The container that wraps the checkbox's label.
	 * @cssPart `hint` — The hint's wrapper.
	 * @cssProperty `--checked-icon-color` — The color of the checked and indeterminate icons.
	 * @cssProperty `--checked-icon-scale` — The size of the checked and indeterminate icons relative to the checkbox.
	 * @cssState `checked` — Applied when the checkbox is checked.
	 * @cssState `disabled` — Applied when the checkbox is disabled.
	 * @cssState `indeterminate` — Applied when the checkbox is in an indeterminate state.
	 */
	'wa-checkbox': {
		elementClass: HTMLElementTagNameMap['wa-checkbox'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-checkbox']['input'];
			/**
			 * @attribute `title`
			 */
			title?: HTMLElementTagNameMap['wa-checkbox']['title'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-checkbox']['name'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-checkbox']['value'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-checkbox']['size'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-checkbox']['disabled'];
			/**
			 * @attribute `indeterminate`
			 * @reflects
			 */
			indeterminate?: HTMLElementTagNameMap['wa-checkbox']['indeterminate'];
			_checked?: HTMLElementTagNameMap['wa-checkbox']['_checked'];
			checked?: HTMLElementTagNameMap['wa-checkbox']['checked'];
			/**
			 * @attribute `checked`
			 * @reflects
			 */
			defaultChecked?: HTMLElementTagNameMap['wa-checkbox']['defaultChecked'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-checkbox']['required'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-checkbox']['hint'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-checkbox']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-checkbox']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-checkbox']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-checkbox']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-checkbox']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-checkbox']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-checkbox']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-checkbox']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-checkbox']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-checkbox']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-checkbox']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-checkbox']['didSSR'];
		};
		attributes: {
			/**
			 * @property `title`
			 */
			title?: string;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `value`
			 */
			value?: string | null;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `indeterminate`
			 */
			indeterminate?: boolean;
			/**
			 * @property `defaultChecked`
			 */
			checked?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			change?: GlobalEventHandlersEventMap['change'];
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			input?: GlobalEventHandlersEventMap['input'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {
			'--checked-icon-color'?: string;
			'--checked-icon-scale'?: string;
		};
		cssParts: {
			base?: string;
			control?: string;
			'checked-icon'?: string;
			'indeterminate-icon'?: string;
			label?: string;
			hint?: string;
		};
		cssStates: {
			checked?: string;
			disabled?: string;
			indeterminate?: string;
		};
		slots: {
			''?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaSpinner extends WebAwesomeElement (<wa-spinner>)
	 * ```
	 * ---
	 * @summary Spinners indicate that an operation is in progress when the duration is unknown. Use them for loading states
	 * where a determinate progress bar isn't practical.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssProperty `--track-width` — The width of the track.
	 * @cssProperty `--track-color` — The color of the track.
	 * @cssProperty `--indicator-color` — The color of the spinner's indicator.
	 * @cssProperty `--speed` — The time it takes for the spinner to complete one animation cycle.
	 */
	'wa-spinner': {
		elementClass: HTMLElementTagNameMap['wa-spinner'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-spinner']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-spinner']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-spinner']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-spinner']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-spinner']['didSSR'];
		};
		attributes: {
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--track-width'?: string;
			'--track-color'?: string;
			'--indicator-color'?: string;
			'--speed'?: string;
		};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaTreeItem extends WebAwesomeElement (<wa-tree-item>)
	 * ```
	 * ---
	 * @summary Tree items represent a single hierarchical node inside a tree, and can contain nested items that expand and
	 * collapse.
	 * @slot [***default***] — The default slot.
	 * @slot `expand-icon` — The icon to show when the tree item is expanded.
	 * @slot `collapse-icon` — The icon to show when the tree item is collapsed.
	 * @fires `wa-expand` — {`Event`} — Emitted when the tree item expands.
	 * @fires `wa-after-expand` — {`Event`} — Emitted after the tree item expands and all animations are complete.
	 * @fires `wa-collapse` — {`Event`} — Emitted when the tree item collapses.
	 * @fires `wa-after-collapse` — {`Event`} — Emitted after the tree item collapses and all animations are complete.
	 * @fires `wa-lazy-change` — {`Event`} — Emitted when the tree item's lazy state changes.
	 * @fires `wa-lazy-load` — {`Event`} — Emitted when a lazy item is selected. Use this event to asynchronously load data and append items to the tree before expanding. After appending new items, remove the `lazy` attribute to remove the loading state and update the tree.
	 * @attribute `expanded` — {`boolean`} — Expands the tree item.
	 * @attribute `selected` — {`boolean`} — Draws the tree item in a selected state.
	 * @attribute `disabled` — {`boolean`} — Disables the tree item.
	 * @attribute `lazy` — {`boolean`} — Enables lazy loading behavior.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `indeterminate` — {`boolean`}
	 * @property `isLeaf` — {`boolean`}
	 * @property `loading` — {`boolean`}
	 * @property `selectable` — {`boolean`}
	 * @property `expanded` — {`boolean`} — Expands the tree item.
	 * @property `selected` — {`boolean`} — Draws the tree item in a selected state.
	 * @property `disabled` — {`boolean`} — Disables the tree item.
	 * @property `lazy` — {`boolean`} — Enables lazy loading behavior.
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `childrenSlot` — {`HTMLSlotElement`}
	 * @property `itemElement` — {`HTMLDivElement`}
	 * @property `childrenContainer` — {`HTMLDivElement`}
	 * @property `expandButtonSlot` — {`HTMLSlotElement`}
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `item` — The tree item's container. This element wraps everything except slotted tree item children.
	 * @cssPart `indentation` — The tree item's indentation container.
	 * @cssPart `expand-button` — The container that wraps the tree item's expand button and spinner.
	 * @cssPart `spinner` — The spinner that shows when a lazy tree item is in the loading state.
	 * @cssPart `spinner__base` — The spinner's base part.
	 * @cssPart `label` — The tree item's label.
	 * @cssPart `children` — The container that wraps the tree item's nested children.
	 * @cssPart `checkbox` — The checkbox that shows when using multiselect.
	 * @cssPart `checkbox__base` — The checkbox's exported `base` part.
	 * @cssPart `checkbox__control` — The checkbox's exported `control` part.
	 * @cssPart `checkbox__checked-icon` — The checkbox's exported `checked-icon` part.
	 * @cssPart `checkbox__indeterminate-icon` — The checkbox's exported `indeterminate-icon` part.
	 * @cssPart `checkbox__label` — The checkbox's exported `label` part.
	 * @cssProperty `--show-duration` — The animation duration when expanding tree items.
	 * @cssProperty `--hide-duration` — The animation duration when collapsing tree items.
	 * @cssState `disabled` — Applied when the tree item is disabled.
	 * @cssState `expanded` — Applied when the tree item is expanded.
	 * @cssState `indeterminate` — Applied when the selection is indeterminate.
	 * @cssState `selected` — Applied when the tree item is selected.
	 */
	'wa-tree-item': {
		elementClass: HTMLElementTagNameMap['wa-tree-item'];
		properties: {
			indeterminate?: HTMLElementTagNameMap['wa-tree-item']['indeterminate'];
			isLeaf?: HTMLElementTagNameMap['wa-tree-item']['isLeaf'];
			loading?: HTMLElementTagNameMap['wa-tree-item']['loading'];
			selectable?: HTMLElementTagNameMap['wa-tree-item']['selectable'];
			/**
			 * @attribute `expanded`
			 * @reflects
			 */
			expanded?: HTMLElementTagNameMap['wa-tree-item']['expanded'];
			/**
			 * @attribute `selected`
			 * @reflects
			 */
			selected?: HTMLElementTagNameMap['wa-tree-item']['selected'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-tree-item']['disabled'];
			/**
			 * @attribute `lazy`
			 * @reflects
			 */
			lazy?: HTMLElementTagNameMap['wa-tree-item']['lazy'];
			defaultSlot?: HTMLElementTagNameMap['wa-tree-item']['defaultSlot'];
			childrenSlot?: HTMLElementTagNameMap['wa-tree-item']['childrenSlot'];
			itemElement?: HTMLElementTagNameMap['wa-tree-item']['itemElement'];
			childrenContainer?: HTMLElementTagNameMap['wa-tree-item']['childrenContainer'];
			expandButtonSlot?: HTMLElementTagNameMap['wa-tree-item']['expandButtonSlot'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tree-item']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tree-item']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tree-item']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tree-item']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tree-item']['didSSR'];
		};
		attributes: {
			/**
			 * @property `expanded`
			 */
			expanded?: boolean;
			/**
			 * @property `selected`
			 */
			selected?: boolean;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `lazy`
			 */
			lazy?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-expand'?: GlobalEventHandlersEventMap['wa-expand'];
			'wa-after-expand'?: GlobalEventHandlersEventMap['wa-after-expand'];
			'wa-collapse'?: GlobalEventHandlersEventMap['wa-collapse'];
			'wa-after-collapse'?: GlobalEventHandlersEventMap['wa-after-collapse'];
			'wa-lazy-change'?: GlobalEventHandlersEventMap['wa-lazy-change'];
			'wa-lazy-load'?: GlobalEventHandlersEventMap['wa-lazy-load'];
		};
		cssProperties: {
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			base?: string;
			item?: string;
			indentation?: string;
			'expand-button'?: string;
			spinner?: string;
			spinner__base?: string;
			label?: string;
			children?: string;
			checkbox?: string;
			checkbox__base?: string;
			checkbox__control?: string;
			'checkbox__checked-icon'?: string;
			'checkbox__indeterminate-icon'?: string;
			checkbox__label?: string;
		};
		cssStates: {
			disabled?: string;
			expanded?: string;
			indeterminate?: string;
			selected?: string;
		};
		slots: {
			''?: string;
			'expand-icon'?: string;
			'collapse-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaCarouselItem extends WebAwesomeElement (<wa-carousel-item>)
	 * ```
	 * ---
	 * @summary Carousel items represent individual slides within a carousel.
	 * @slot [***default***] — The carousel item's content..
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssProperty `--aspect-ratio` — The slide's aspect ratio. Inherited from the carousel by default.
	 */
	'wa-carousel-item': {
		elementClass: HTMLElementTagNameMap['wa-carousel-item'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-carousel-item']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-carousel-item']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-carousel-item']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-carousel-item']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-carousel-item']['didSSR'];
		};
		attributes: {
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--aspect-ratio'?: string;
		};
		cssParts: {};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaButton extends WebAwesomeFormAssociatedElement (<wa-button>)
	 * ```
	 * ---
	 * @summary Buttons represent actions the user can take, such as submitting a form, opening a dialog, or navigating to
	 * another page.
	 * @slot [***default***] — The button's label.
	 * @slot `start` — An element, such as `<wa-icon>`, placed before the label.
	 * @slot `end` — An element, such as `<wa-icon>`, placed after the label.
	 * @fires `blur` — {`Event`} — Emitted when the button loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the button gains focus.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `title` — {`string`}
	 * @attribute `variant` — {`'neutral' | 'brand' | 'success' | 'warning' | 'danger'`} — The button's theme variant. Defaults to `neutral` if not within another element with a variant.
	 * @attribute `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined' | 'plain'`} — The button's visual appearance.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The button's size.
	 * @attribute `with-caret` — {`boolean`} — Draws the button with a caret. Used to indicate that the button triggers a dropdown menu or similar behavior.
	 * @attribute `with-start` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `start` element so the server-rendered markup
	 * includes the start slot before the component hydrates on the client.
	 * @attribute `with-end` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in an `end` element so the server-rendered markup
	 * includes the end slot before the component hydrates on the client.
	 * @attribute `disabled` — {`boolean`} — Disables the button. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `loading` — {`boolean`} — Draws the button in a loading state.
	 * @attribute `pill` — {`boolean`} — Draws a pill-style button with rounded edges.
	 * @attribute `type` — {`'button' | 'submit' | 'reset'`} — The type of button. Note that the default value is `button` instead of `submit`, which is opposite of how native
	 * `<button>` elements behave. When the type is `submit`, the button will submit the surrounding form.
	 * @attribute `name` — {`string | null`} — The name of the button, submitted as a name/value pair with form data, but only when this button is the submitter.
	 * This attribute is ignored when `href` is present. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `value` — {`string`} — The value of the button, submitted as a pair with the button's name as part of the form data, but only when this
	 * button is the submitter. This attribute is ignored when `href` is present.
	 * @attribute `href` — {`string`} — When set, the underlying button will be rendered as an `<a>` with this `href` instead of a `<button>`.
	 * @attribute `target` — {`'_blank' | '_parent' | '_self' | '_top'`} — Tells the browser where to open the link. Only used when `href` is present.
	 * @attribute `rel` — {`string | undefined`} — When using `href`, this attribute will map to the underlying link's `rel` attribute.
	 * @attribute `download` — {`string | undefined`} — Tells the browser to download the linked file as this filename. Only used when `href` is present.
	 * @attribute `formaction` — {`string`} — Used to override the form owner's `action` attribute.
	 * @attribute `formenctype` — {`'application/x-www-form-urlencoded' | 'multipart/form-data' | 'text/plain'`} — Used to override the form owner's `enctype` attribute.
	 * @attribute `formmethod` — {`'post' | 'get'`} — Used to override the form owner's `method` attribute.
	 * @attribute `formnovalidate` — {`boolean`} — Used to override the form owner's `novalidate` attribute.
	 * @attribute `formtarget` — {`'_self' | '_blank' | '_parent' | '_top' | string`} — Used to override the form owner's `target` attribute.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `button` — {`HTMLButtonElement | HTMLLinkElement`}
	 * @property `labelSlot` — {`HTMLSlotElement`}
	 * @property `invalid` — {`boolean`}
	 * @property `isIconButton` — {`boolean`}
	 * @property `title` — {`string`}
	 * @property `variant` — {`'neutral' | 'brand' | 'success' | 'warning' | 'danger'`} — The button's theme variant. Defaults to `neutral` if not within another element with a variant.
	 * @property `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined' | 'plain'`} — The button's visual appearance.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The button's size.
	 * @property `withCaret` — {`boolean`} — Draws the button with a caret. Used to indicate that the button triggers a dropdown menu or similar behavior.
	 * @property `withStart` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `start` element so the server-rendered markup
	 * includes the start slot before the component hydrates on the client.
	 * @property `withEnd` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in an `end` element so the server-rendered markup
	 * includes the end slot before the component hydrates on the client.
	 * @property `disabled` — {`boolean`} — Disables the button. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `loading` — {`boolean`} — Draws the button in a loading state.
	 * @property `pill` — {`boolean`} — Draws a pill-style button with rounded edges.
	 * @property `type` — {`'button' | 'submit' | 'reset'`} — The type of button. Note that the default value is `button` instead of `submit`, which is opposite of how native
	 * `<button>` elements behave. When the type is `submit`, the button will submit the surrounding form.
	 * @property `name` — {`string | null`} — The name of the button, submitted as a name/value pair with form data, but only when this button is the submitter.
	 * This attribute is ignored when `href` is present. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `value` — {`string`} — The value of the button, submitted as a pair with the button's name as part of the form data, but only when this
	 * button is the submitter. This attribute is ignored when `href` is present.
	 * @property `href` — {`string`} — When set, the underlying button will be rendered as an `<a>` with this `href` instead of a `<button>`.
	 * @property `target` — {`'_blank' | '_parent' | '_self' | '_top'`} — Tells the browser where to open the link. Only used when `href` is present.
	 * @property `rel` — {`string | undefined`} — When using `href`, this attribute will map to the underlying link's `rel` attribute.
	 * @property `download` — {`string | undefined`} — Tells the browser to download the linked file as this filename. Only used when `href` is present.
	 * @property `formAction` — {`string`} — Used to override the form owner's `action` attribute.
	 * @property `formEnctype` — {`'application/x-www-form-urlencoded' | 'multipart/form-data' | 'text/plain'`} — Used to override the form owner's `enctype` attribute.
	 * @property `formMethod` — {`'post' | 'get'`} — Used to override the form owner's `method` attribute.
	 * @property `formNoValidate` — {`boolean`} — Used to override the form owner's `novalidate` attribute.
	 * @property `formTarget` — {`'_self' | '_blank' | '_parent' | '_top' | string`} — Used to override the form owner's `target` attribute.
	 * @property `required` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `label` — The button's label.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssPart `caret` — The button's caret icon, a `<wa-icon>` element.
	 * @cssPart `spinner` — The spinner that shows when the button is in the loading state.
	 * @cssState `disabled` — Applied when the button is disabled.
	 * @cssState `icon-button` — Applied when the button contains only a `<wa-icon>` with no other content.
	 * @cssState `link` — Applied when the button is rendered as a link (i.e. `href` is set).
	 * @cssState `loading` — Applied when the button is in the loading state.
	 */
	'wa-button': {
		elementClass: HTMLElementTagNameMap['wa-button'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-button']['assumeInteractionOn'];
			button?: HTMLElementTagNameMap['wa-button']['button'];
			labelSlot?: HTMLElementTagNameMap['wa-button']['labelSlot'];
			invalid?: HTMLElementTagNameMap['wa-button']['invalid'];
			isIconButton?: HTMLElementTagNameMap['wa-button']['isIconButton'];
			/**
			 * @attribute `title`
			 */
			title?: HTMLElementTagNameMap['wa-button']['title'];
			/**
			 * @attribute `variant`
			 * @reflects
			 */
			variant?: HTMLElementTagNameMap['wa-button']['variant'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-button']['appearance'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-button']['size'];
			/**
			 * @attribute `with-caret`
			 * @reflects
			 */
			withCaret?: HTMLElementTagNameMap['wa-button']['withCaret'];
			/**
			 * @attribute `with-start`
			 */
			withStart?: HTMLElementTagNameMap['wa-button']['withStart'];
			/**
			 * @attribute `with-end`
			 */
			withEnd?: HTMLElementTagNameMap['wa-button']['withEnd'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-button']['disabled'];
			/**
			 * @attribute `loading`
			 * @reflects
			 */
			loading?: HTMLElementTagNameMap['wa-button']['loading'];
			/**
			 * @attribute `pill`
			 * @reflects
			 */
			pill?: HTMLElementTagNameMap['wa-button']['pill'];
			/**
			 * @attribute `type`
			 */
			type?: HTMLElementTagNameMap['wa-button']['type'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-button']['name'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-button']['value'];
			/**
			 * @attribute `href`
			 * @reflects
			 */
			href?: HTMLElementTagNameMap['wa-button']['href'];
			/**
			 * @attribute `target`
			 */
			target?: HTMLElementTagNameMap['wa-button']['target'];
			/**
			 * @attribute `rel`
			 */
			rel?: HTMLElementTagNameMap['wa-button']['rel'];
			/**
			 * @attribute `download`
			 */
			download?: HTMLElementTagNameMap['wa-button']['download'];
			/**
			 * @attribute `formaction`
			 */
			formAction?: HTMLElementTagNameMap['wa-button']['formAction'];
			/**
			 * @attribute `formenctype`
			 */
			formEnctype?: HTMLElementTagNameMap['wa-button']['formEnctype'];
			/**
			 * @attribute `formmethod`
			 */
			formMethod?: HTMLElementTagNameMap['wa-button']['formMethod'];
			/**
			 * @attribute `formnovalidate`
			 */
			formNoValidate?: HTMLElementTagNameMap['wa-button']['formNoValidate'];
			/**
			 * @attribute `formtarget`
			 */
			formTarget?: HTMLElementTagNameMap['wa-button']['formTarget'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-button']['required'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-button']['input'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-button']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-button']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-button']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-button']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-button']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-button']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-button']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-button']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-button']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-button']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-button']['didSSR'];
		};
		attributes: {
			/**
			 * @property `title`
			 */
			title?: string;
			/**
			 * @property `variant`
			 */
			variant?: 'neutral' | 'brand' | 'success' | 'warning' | 'danger';
			/**
			 * @property `appearance`
			 */
			appearance?:
				| 'accent'
				| 'filled'
				| 'outlined'
				| 'filled-outlined'
				| 'plain';
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `withCaret`
			 */
			'with-caret'?: boolean;
			/**
			 * @property `withStart`
			 */
			'with-start'?: boolean;
			/**
			 * @property `withEnd`
			 */
			'with-end'?: boolean;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `loading`
			 */
			loading?: boolean;
			/**
			 * @property `pill`
			 */
			pill?: boolean;
			/**
			 * @property `type`
			 */
			type?: 'button' | 'submit' | 'reset';
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `value`
			 */
			value?: string;
			/**
			 * @property `href`
			 */
			href?: string;
			/**
			 * @property `target`
			 */
			target?: '_blank' | '_parent' | '_self' | '_top';
			/**
			 * @property `rel`
			 */
			rel?: string | undefined;
			/**
			 * @property `download`
			 */
			download?: string | undefined;
			/**
			 * @property `formAction`
			 */
			formaction?: string;
			/**
			 * @property `formEnctype`
			 */
			formenctype?:
				| 'application/x-www-form-urlencoded'
				| 'multipart/form-data'
				| 'text/plain';
			/**
			 * @property `formMethod`
			 */
			formmethod?: 'post' | 'get';
			/**
			 * @property `formNoValidate`
			 */
			formnovalidate?: boolean;
			/**
			 * @property `formTarget`
			 */
			formtarget?: '_self' | '_blank' | '_parent' | '_top' | string;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {};
		cssParts: {
			base?: string;
			start?: string;
			label?: string;
			end?: string;
			caret?: string;
			spinner?: string;
		};
		cssStates: {
			disabled?: string;
			'icon-button'?: string;
			link?: string;
			loading?: string;
		};
		slots: {
			''?: string;
			start?: string;
			end?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaAnimatedImage extends WebAwesomeElement (<wa-animated-image>)
	 * ```
	 * ---
	 * @summary Animated images display GIFs and WEBPs with controls to play and pause them on demand. Use them when you
	 * want motion but need to give users control over when it plays.
	 * @slot `play-icon` — Optional play icon to use instead of the default. Works best with `<wa-icon>`.
	 * @slot `pause-icon` — Optional pause icon to use instead of the default. Works best with `<wa-icon>`.
	 * @fires `wa-load` — {`Event`} — Emitted when the image loads successfully.
	 * @fires `wa-error` — {`Event`} — Emitted when the image fails to load.
	 * @attribute `src` — {`string`} — The path to the image to load.
	 * @attribute `alt` — {`string`} — A description of the image used by assistive devices.
	 * @attribute `play` — {`boolean`} — Plays the animation. When this attribute is remove, the animation will pause.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `animatedImage` — {`HTMLImageElement`}
	 * @property `frozenFrame` — {`string`}
	 * @property `isLoaded` — {`boolean`}
	 * @property `src` — {`string`} — The path to the image to load.
	 * @property `alt` — {`string`} — A description of the image used by assistive devices.
	 * @property `play` — {`boolean`} — Plays the animation. When this attribute is remove, the animation will pause.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `control-box` — The container that surrounds the pause/play icons and provides their background.
	 * @cssProperty `--control-box-size` — The size of the icon box.
	 * @cssProperty `--icon-size` — The size of the play/pause icons.
	 */
	'wa-animated-image': {
		elementClass: HTMLElementTagNameMap['wa-animated-image'];
		properties: {
			animatedImage?: HTMLElementTagNameMap['wa-animated-image']['animatedImage'];
			frozenFrame?: HTMLElementTagNameMap['wa-animated-image']['frozenFrame'];
			isLoaded?: HTMLElementTagNameMap['wa-animated-image']['isLoaded'];
			/**
			 * @attribute `src`
			 */
			src?: HTMLElementTagNameMap['wa-animated-image']['src'];
			/**
			 * @attribute `alt`
			 */
			alt?: HTMLElementTagNameMap['wa-animated-image']['alt'];
			/**
			 * @attribute `play`
			 * @reflects
			 */
			play?: HTMLElementTagNameMap['wa-animated-image']['play'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-animated-image']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-animated-image']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-animated-image']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-animated-image']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-animated-image']['didSSR'];
		};
		attributes: {
			/**
			 * @property `src`
			 */
			src?: string;
			/**
			 * @property `alt`
			 */
			alt?: string;
			/**
			 * @property `play`
			 */
			play?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-load'?: GlobalEventHandlersEventMap['wa-load'];
			'wa-error'?: GlobalEventHandlersEventMap['wa-error'];
		};
		cssProperties: {
			'--control-box-size'?: string;
			'--icon-size'?: string;
		};
		cssParts: {
			'control-box'?: string;
		};
		cssStates: {};
		slots: {
			'play-icon'?: string;
			'pause-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaAnimation extends WebAwesomeElement (<wa-animation>)
	 * ```
	 * ---
	 * @summary Animate elements declaratively with nearly 100 baked-in presets, or roll your own with custom keyframes.
	 * Powered by the Web Animations API.
	 * @slot [***default***] — The element to animate. Avoid slotting in more than one element, as subsequent ones will be ignored. To animate multiple elements, either wrap them in a single container or use multiple `<wa-animation>` elements.
	 * @fires `wa-cancel` — {`Event`} — Emitted when the animation is canceled.
	 * @fires `wa-finish` — {`Event`} — Emitted when the animation finishes.
	 * @fires `wa-start` — {`Event`} — Emitted when the animation starts or restarts.
	 * @attribute `name` — {`string`} — The name of the built-in animation to use. For custom animations, use the `keyframes` prop.
	 * @attribute `play` — {`boolean`} — Plays the animation. When omitted, the animation will be paused. This attribute will be automatically removed when
	 * the animation finishes or gets canceled.
	 * @attribute `delay` — {`number`} — The number of milliseconds to delay the start of the animation.
	 * @attribute `direction` — {`PlaybackDirection`} — Determines the direction of playback as well as the behavior when reaching the end of an iteration.
	 * [Learn more](https://developer.mozilla.org/en-US/docs/Web/CSS/animation-direction)
	 * @attribute `duration` — {`number`} — The number of milliseconds each iteration of the animation takes to complete.
	 * @attribute `easing` — {`string`} — The easing function to use for the animation. This can be a Web Awesome easing function or a custom easing function
	 * such as `cubic-bezier(0, 1, .76, 1.14)`.
	 * @attribute `end-delay` — {`number`} — The number of milliseconds to delay after the active period of an animation sequence.
	 * @attribute `fill` — {`FillMode`} — Sets how the animation applies styles to its target before and after its execution.
	 * @attribute `iterations` — {`number`} — The number of iterations to run before the animation completes. Defaults to `Infinity`, which loops.
	 * @attribute `iteration-start` — {`number`} — The offset at which to start the animation, usually between 0 (start) and 1 (end).
	 * @attribute `playback-rate` — {`number`} — Sets the animation's playback rate. The default is `1`, which plays the animation at a normal speed. Setting this
	 * to `2`, for example, will double the animation's speed. A negative value can be used to reverse the animation. This
	 * value can be changed without causing the animation to restart.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`Promise<HTMLSlotElement>`}
	 * @property `name` — {`string`} — The name of the built-in animation to use. For custom animations, use the `keyframes` prop.
	 * @property `play` — {`boolean`} — Plays the animation. When omitted, the animation will be paused. This attribute will be automatically removed when
	 * the animation finishes or gets canceled.
	 * @property `delay` — {`number`} — The number of milliseconds to delay the start of the animation.
	 * @property `direction` — {`PlaybackDirection`} — Determines the direction of playback as well as the behavior when reaching the end of an iteration.
	 * [Learn more](https://developer.mozilla.org/en-US/docs/Web/CSS/animation-direction)
	 * @property `duration` — {`number`} — The number of milliseconds each iteration of the animation takes to complete.
	 * @property `easing` — {`string`} — The easing function to use for the animation. This can be a Web Awesome easing function or a custom easing function
	 * such as `cubic-bezier(0, 1, .76, 1.14)`.
	 * @property `endDelay` — {`number`} — The number of milliseconds to delay after the active period of an animation sequence.
	 * @property `fill` — {`FillMode`} — Sets how the animation applies styles to its target before and after its execution.
	 * @property `iterations` — {`number`} — The number of iterations to run before the animation completes. Defaults to `Infinity`, which loops.
	 * @property `iterationStart` — {`number`} — The offset at which to start the animation, usually between 0 (start) and 1 (end).
	 * @property `keyframes` — {`Keyframe[] | undefined`} — The keyframes to use for the animation. If this is set, `name` will be ignored.
	 * @property `playbackRate` — {`number`} — Sets the animation's playback rate. The default is `1`, which plays the animation at a normal speed. Setting this
	 * to `2`, for example, will double the animation's speed. A negative value can be used to reverse the animation. This
	 * value can be changed without causing the animation to restart.
	 * @property `currentTime` — {`CSSNumberish`} — Gets and sets the current animation time.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-animation': {
		elementClass: HTMLElementTagNameMap['wa-animation'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-animation']['defaultSlot'];
			/**
			 * @attribute `name`
			 */
			name?: HTMLElementTagNameMap['wa-animation']['name'];
			/**
			 * @attribute `play`
			 * @reflects
			 */
			play?: HTMLElementTagNameMap['wa-animation']['play'];
			/**
			 * @attribute `delay`
			 */
			delay?: HTMLElementTagNameMap['wa-animation']['delay'];
			/**
			 * @attribute `direction`
			 */
			direction?: HTMLElementTagNameMap['wa-animation']['direction'];
			/**
			 * @attribute `duration`
			 */
			duration?: HTMLElementTagNameMap['wa-animation']['duration'];
			/**
			 * @attribute `easing`
			 */
			easing?: HTMLElementTagNameMap['wa-animation']['easing'];
			/**
			 * @attribute `end-delay`
			 */
			endDelay?: HTMLElementTagNameMap['wa-animation']['endDelay'];
			/**
			 * @attribute `fill`
			 */
			fill?: HTMLElementTagNameMap['wa-animation']['fill'];
			/**
			 * @attribute `iterations`
			 */
			iterations?: HTMLElementTagNameMap['wa-animation']['iterations'];
			/**
			 * @attribute `iteration-start`
			 */
			iterationStart?: HTMLElementTagNameMap['wa-animation']['iterationStart'];
			keyframes?: HTMLElementTagNameMap['wa-animation']['keyframes'];
			/**
			 * @attribute `playback-rate`
			 */
			playbackRate?: HTMLElementTagNameMap['wa-animation']['playbackRate'];
			currentTime?: HTMLElementTagNameMap['wa-animation']['currentTime'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-animation']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-animation']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-animation']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-animation']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-animation']['didSSR'];
		};
		attributes: {
			/**
			 * @property `name`
			 */
			name?: string;
			/**
			 * @property `play`
			 */
			play?: boolean;
			/**
			 * @property `delay`
			 */
			delay?: number;
			/**
			 * @property `direction`
			 */
			direction?: unknown;
			/**
			 * @property `duration`
			 */
			duration?: number;
			/**
			 * @property `easing`
			 */
			easing?: string;
			/**
			 * @property `endDelay`
			 */
			'end-delay'?: number;
			/**
			 * @property `fill`
			 */
			fill?: unknown;
			/**
			 * @property `iterations`
			 */
			iterations?: number;
			/**
			 * @property `iterationStart`
			 */
			'iteration-start'?: number;
			/**
			 * @property `playbackRate`
			 */
			'playback-rate'?: number;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-cancel'?: GlobalEventHandlersEventMap['wa-cancel'];
			'wa-finish'?: GlobalEventHandlersEventMap['wa-finish'];
			'wa-start'?: GlobalEventHandlersEventMap['wa-start'];
		};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaAvatar extends WebAwesomeElement (<wa-avatar>)
	 * ```
	 * ---
	 * @summary Avatars represent a person or object with an image, initials, or icon. Use them in lists, comments, and
	 * profiles to give users visual context at a glance.
	 * @slot `icon` — The default icon to use when no image or initials are present. Works best with `<wa-icon>`.
	 * @fires `wa-error` — {`Event`} — The image could not be loaded. This may because of an invalid URL, a temporary network condition, or some unknown cause.
	 * @attribute `image` — {`string`} — The image source to use for the avatar.
	 * @attribute `label` — {`string`} — A label to use to describe the avatar to assistive devices.
	 * @attribute `initials` — {`string`} — Initials to use as a fallback when no image is available (1-2 characters max recommended).
	 * @attribute `loading` — {`'eager' | 'lazy'`} — Indicates how the browser should load the image.
	 * @attribute `shape` — {`'circle' | 'square' | 'rounded'`} — The shape of the avatar.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `image` — {`string`} — The image source to use for the avatar.
	 * @property `label` — {`string`} — A label to use to describe the avatar to assistive devices.
	 * @property `initials` — {`string`} — Initials to use as a fallback when no image is available (1-2 characters max recommended).
	 * @property `loading` — {`'eager' | 'lazy'`} — Indicates how the browser should load the image.
	 * @property `shape` — {`'circle' | 'square' | 'rounded'`} — The shape of the avatar.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `icon` — The container that wraps the avatar's icon.
	 * @cssPart `initials` — The container that wraps the avatar's initials.
	 * @cssPart `image` — The avatar image. Only shown when the `image` attribute is set.
	 * @cssProperty `--size` — The size of the avatar.
	 */
	'wa-avatar': {
		elementClass: HTMLElementTagNameMap['wa-avatar'];
		properties: {
			/**
			 * @attribute `image`
			 */
			image?: HTMLElementTagNameMap['wa-avatar']['image'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-avatar']['label'];
			/**
			 * @attribute `initials`
			 */
			initials?: HTMLElementTagNameMap['wa-avatar']['initials'];
			/**
			 * @attribute `loading`
			 */
			loading?: HTMLElementTagNameMap['wa-avatar']['loading'];
			/**
			 * @attribute `shape`
			 * @reflects
			 */
			shape?: HTMLElementTagNameMap['wa-avatar']['shape'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-avatar']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-avatar']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-avatar']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-avatar']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-avatar']['didSSR'];
		};
		attributes: {
			/**
			 * @property `image`
			 */
			image?: string;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `initials`
			 */
			initials?: string;
			/**
			 * @property `loading`
			 */
			loading?: 'eager' | 'lazy';
			/**
			 * @property `shape`
			 */
			shape?: 'circle' | 'square' | 'rounded';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-error'?: GlobalEventHandlersEventMap['wa-error'];
		};
		cssProperties: {
			'--size'?: string;
		};
		cssParts: {
			icon?: string;
			initials?: string;
			image?: string;
		};
		cssStates: {};
		slots: {
			icon?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaBadge extends WebAwesomeElement (<wa-badge>)
	 * ```
	 * ---
	 * @summary Badges draw attention to adjacent content by displaying a status, count, or label. Use them to highlight
	 * notifications, categorize items, or flag new activity.
	 * @slot [***default***] — The badge's content.
	 * @slot `start` — An element, such as `<wa-icon>`, placed before the label.
	 * @slot `end` — An element, such as `<wa-icon>`, placed after the label.
	 * @attribute `variant` — {`'brand' | 'neutral' | 'success' | 'warning' | 'danger'`} — The badge's theme variant. Defaults to `brand` if not within another element with a variant.
	 * @attribute `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined'`} — The badge's visual appearance.
	 * @attribute `pill` — {`boolean`} — Draws a pill-style badge with rounded edges.
	 * @attribute `attention` — {`'none' | 'pulse' | 'bounce'`} — Adds an animation to draw attention to the badge.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `variant` — {`'brand' | 'neutral' | 'success' | 'warning' | 'danger'`} — The badge's theme variant. Defaults to `brand` if not within another element with a variant.
	 * @property `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined'`} — The badge's visual appearance.
	 * @property `pill` — {`boolean`} — Draws a pill-style badge with rounded edges.
	 * @property `attention` — {`'none' | 'pulse' | 'bounce'`} — Adds an animation to draw attention to the badge.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssProperty `--pulse-color` — The color of the badge's pulse effect when using `attention="pulse"`.
	 */
	'wa-badge': {
		elementClass: HTMLElementTagNameMap['wa-badge'];
		properties: {
			/**
			 * @attribute `variant`
			 * @reflects
			 */
			variant?: HTMLElementTagNameMap['wa-badge']['variant'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-badge']['appearance'];
			/**
			 * @attribute `pill`
			 * @reflects
			 */
			pill?: HTMLElementTagNameMap['wa-badge']['pill'];
			/**
			 * @attribute `attention`
			 * @reflects
			 */
			attention?: HTMLElementTagNameMap['wa-badge']['attention'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-badge']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-badge']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-badge']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-badge']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-badge']['didSSR'];
		};
		attributes: {
			/**
			 * @property `variant`
			 */
			variant?: 'brand' | 'neutral' | 'success' | 'warning' | 'danger';
			/**
			 * @property `appearance`
			 */
			appearance?: 'accent' | 'filled' | 'outlined' | 'filled-outlined';
			/**
			 * @property `pill`
			 */
			pill?: boolean;
			/**
			 * @property `attention`
			 */
			attention?: 'none' | 'pulse' | 'bounce';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--pulse-color'?: string;
		};
		cssParts: {
			base?: string;
			start?: string;
			end?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			start?: string;
			end?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaBreadcrumbItem extends WebAwesomeElement (<wa-breadcrumb-item>)
	 * ```
	 * ---
	 * @summary Breadcrumb items represent individual links inside a breadcrumb, typically one per level of the site
	 * hierarchy.
	 * @slot [***default***] — The breadcrumb item's label.
	 * @slot `start` — An element, such as `<wa-icon>`, placed before the label.
	 * @slot `end` — An element, such as `<wa-icon>`, placed after the label.
	 * @slot `separator` — The separator to use for the breadcrumb item. This will only change the separator for this item. If you want to change it for all items in the group, set the separator on `<wa-breadcrumb>` instead.
	 * @attribute `href` — {`string | undefined`} — Optional URL to direct the user to when the breadcrumb item is activated. When set, a link will be rendered
	 * internally. When unset, a button will be rendered instead.
	 * @attribute `target` — {`'_blank' | '_parent' | '_self' | '_top' | undefined`} — Tells the browser where to open the link. Only used when `href` is set.
	 * @attribute `rel` — {`string`} — The `rel` attribute to use on the link. Only used when `href` is set.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `href` — {`string | undefined`} — Optional URL to direct the user to when the breadcrumb item is activated. When set, a link will be rendered
	 * internally. When unset, a button will be rendered instead.
	 * @property `target` — {`'_blank' | '_parent' | '_self' | '_top' | undefined`} — Tells the browser where to open the link. Only used when `href` is set.
	 * @property `rel` — {`string`} — The `rel` attribute to use on the link. Only used when `href` is set.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `label` — The breadcrumb item's label.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssPart `separator` — The container that wraps the separator.
	 */
	'wa-breadcrumb-item': {
		elementClass: HTMLElementTagNameMap['wa-breadcrumb-item'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-breadcrumb-item']['defaultSlot'];
			/**
			 * @attribute `href`
			 */
			href?: HTMLElementTagNameMap['wa-breadcrumb-item']['href'];
			/**
			 * @attribute `target`
			 */
			target?: HTMLElementTagNameMap['wa-breadcrumb-item']['target'];
			/**
			 * @attribute `rel`
			 */
			rel?: HTMLElementTagNameMap['wa-breadcrumb-item']['rel'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-breadcrumb-item']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-breadcrumb-item']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-breadcrumb-item']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-breadcrumb-item']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-breadcrumb-item']['didSSR'];
		};
		attributes: {
			/**
			 * @property `href`
			 */
			href?: string | undefined;
			/**
			 * @property `target`
			 */
			target?: '_blank' | '_parent' | '_self' | '_top' | undefined;
			/**
			 * @property `rel`
			 */
			rel?: string;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			label?: string;
			start?: string;
			end?: string;
			separator?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			start?: string;
			end?: string;
			separator?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaBreadcrumb extends WebAwesomeElement (<wa-breadcrumb>)
	 * ```
	 * ---
	 * @summary Breadcrumbs display a trail of links that show users where they are in a site's hierarchy. They help users
	 * understand the current location and navigate back to parent pages.
	 * @slot [***default***] — One or more breadcrumb items to display.
	 * @slot `separator` — The separator to use between breadcrumb items. Works best with `<wa-icon>`.
	 * @attribute `label` — {`string`} — The label to use for the breadcrumb control. This will not be shown on the screen, but it will be announced by
	 * screen readers and other assistive devices to provide more context for users.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `separatorSlot` — {`HTMLSlotElement`}
	 * @property `label` — {`string`} — The label to use for the breadcrumb control. This will not be shown on the screen, but it will be announced by
	 * screen readers and other assistive devices to provide more context for users.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 */
	'wa-breadcrumb': {
		elementClass: HTMLElementTagNameMap['wa-breadcrumb'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-breadcrumb']['defaultSlot'];
			separatorSlot?: HTMLElementTagNameMap['wa-breadcrumb']['separatorSlot'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-breadcrumb']['label'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-breadcrumb']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-breadcrumb']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-breadcrumb']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-breadcrumb']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-breadcrumb']['didSSR'];
		};
		attributes: {
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			separator?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaButtonGroup extends WebAwesomeElement (<wa-button-group>)
	 * ```
	 * ---
	 * @summary Button groups combine related buttons into a single visual unit. Use them for toolbars, segmented controls,
	 * or any set of actions that belong together.
	 * @slot [***default***] — One or more `<wa-button>` elements to display in the button group.
	 * @attribute `label` — {`string`} — A label to use for the button group. This won't be displayed on the screen, but it will be announced by assistive
	 * devices when interacting with the control and is strongly recommended.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — The button group's orientation.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `disableRole` — {`boolean`}
	 * @property `hasOutlined` — {`boolean`}
	 * @property `label` — {`string`} — A label to use for the button group. This won't be displayed on the screen, but it will be announced by assistive
	 * devices when interacting with the control and is strongly recommended.
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — The button group's orientation.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 */
	'wa-button-group': {
		elementClass: HTMLElementTagNameMap['wa-button-group'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-button-group']['defaultSlot'];
			disableRole?: HTMLElementTagNameMap['wa-button-group']['disableRole'];
			hasOutlined?: HTMLElementTagNameMap['wa-button-group']['hasOutlined'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-button-group']['label'];
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-button-group']['orientation'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-button-group']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-button-group']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-button-group']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-button-group']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-button-group']['didSSR'];
		};
		attributes: {
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaCallout extends WebAwesomeElement (<wa-callout>)
	 * ```
	 * ---
	 * @summary Callouts display important messages inline with surrounding content. Use them to highlight tips, warnings,
	 * errors, or other information users should not miss.
	 * @slot [***default***] — The callout's main content.
	 * @slot `icon` — An icon to show in the callout. Works best with `<wa-icon>`.
	 * @attribute `variant` — {`'brand' | 'neutral' | 'success' | 'warning' | 'danger'`} — The callout's theme variant. Defaults to `brand` if not within another element with a variant.
	 * @attribute `appearance` — {`'accent' | 'filled' | 'outlined' | 'plain' | 'filled-outlined'`} — The callout's visual appearance.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The callout's size.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `variant` — {`'brand' | 'neutral' | 'success' | 'warning' | 'danger'`} — The callout's theme variant. Defaults to `brand` if not within another element with a variant.
	 * @property `appearance` — {`'accent' | 'filled' | 'outlined' | 'plain' | 'filled-outlined'`} — The callout's visual appearance.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The callout's size.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `icon` — The container that wraps the optional icon.
	 * @cssPart `message` — The container that wraps the callout's main content.
	 */
	'wa-callout': {
		elementClass: HTMLElementTagNameMap['wa-callout'];
		properties: {
			/**
			 * @attribute `variant`
			 * @reflects
			 */
			variant?: HTMLElementTagNameMap['wa-callout']['variant'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-callout']['appearance'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-callout']['size'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-callout']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-callout']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-callout']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-callout']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-callout']['didSSR'];
		};
		attributes: {
			/**
			 * @property `variant`
			 */
			variant?: 'brand' | 'neutral' | 'success' | 'warning' | 'danger';
			/**
			 * @property `appearance`
			 */
			appearance?:
				| 'accent'
				| 'filled'
				| 'outlined'
				| 'plain'
				| 'filled-outlined';
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			icon?: string;
			message?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			icon?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaCard extends WebAwesomeElement (<wa-card>)
	 * ```
	 * ---
	 * @summary Cards group related content and actions inside a bordered container. Use them to present products, articles,
	 * user profiles, or any self-contained unit of information.
	 * @slot [***default***] — The card's main content.
	 * @slot `header` — An optional header for the card.
	 * @slot `footer` — An optional footer for the card.
	 * @slot `media` — An optional media section to render at the start of the card.
	 * @slot `actions` — An optional actions section to render at the end for the horizontal card.
	 * @slot `header-actions` — An optional actions section to render in the header of the vertical card.
	 * @slot `footer-actions` — An optional actions section to render in the footer of the vertical card.
	 * @attribute `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined' | 'plain'`} — The card's visual appearance.
	 * @attribute `with-header` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `header` element so the server-rendered markup
	 * includes the header before the component hydrates on the client.
	 * @attribute `with-media` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `media` element so the server-rendered markup
	 * includes the media before the component hydrates on the client.
	 * @attribute `with-footer` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `footer` element so the server-rendered markup
	 * includes the footer before the component hydrates on the client.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — Renders the card's orientation *
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined' | 'plain'`} — The card's visual appearance.
	 * @property `withHeader` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `header` element so the server-rendered markup
	 * includes the header before the component hydrates on the client.
	 * @property `withMedia` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `media` element so the server-rendered markup
	 * includes the media before the component hydrates on the client.
	 * @property `withFooter` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `footer` element so the server-rendered markup
	 * includes the footer before the component hydrates on the client.
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — Renders the card's orientation *
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `media` — The container that wraps the card's media.
	 * @cssPart `header` — The container that wraps the card's header.
	 * @cssPart `body` — The container that wraps the card's main content.
	 * @cssPart `footer` — The container that wraps the card's footer.
	 * @cssProperty `--spacing` — The amount of space around and between sections of the card. Expects a single value.
	 */
	'wa-card': {
		elementClass: HTMLElementTagNameMap['wa-card'];
		properties: {
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-card']['appearance'];
			/**
			 * @attribute `with-header`
			 * @reflects
			 */
			withHeader?: HTMLElementTagNameMap['wa-card']['withHeader'];
			/**
			 * @attribute `with-media`
			 * @reflects
			 */
			withMedia?: HTMLElementTagNameMap['wa-card']['withMedia'];
			/**
			 * @attribute `with-footer`
			 * @reflects
			 */
			withFooter?: HTMLElementTagNameMap['wa-card']['withFooter'];
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-card']['orientation'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-card']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-card']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-card']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-card']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-card']['didSSR'];
		};
		attributes: {
			/**
			 * @property `appearance`
			 */
			appearance?:
				| 'accent'
				| 'filled'
				| 'outlined'
				| 'filled-outlined'
				| 'plain';
			/**
			 * @property `withHeader`
			 */
			'with-header'?: boolean;
			/**
			 * @property `withMedia`
			 */
			'with-media'?: boolean;
			/**
			 * @property `withFooter`
			 */
			'with-footer'?: boolean;
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--spacing'?: string;
		};
		cssParts: {
			media?: string;
			header?: string;
			body?: string;
			footer?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			header?: string;
			footer?: string;
			media?: string;
			actions?: string;
			'header-actions'?: string;
			'footer-actions'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaCarousel extends WebAwesomeElement (<wa-carousel>)
	 * ```
	 * ---
	 * @summary Carousels display a series of content slides along a horizontal or vertical axis, one or more at a time.
	 * Users can navigate between slides with controls, pagination, or autoplay.
	 * @slot [***default***] — The carousel's main content, one or more `<wa-carousel-item>` elements.
	 * @slot `next-icon` — Optional next icon to use instead of the default. Works best with `<wa-icon>`.
	 * @slot `previous-icon` — Optional previous icon to use instead of the default. Works best with `<wa-icon>`.
	 * @fires `wa-slide-change` — {`{ index: number, slide: WaCarouselItem }`} — Emitted when the active slide changes.
	 * @attribute `loop` — {`boolean`} — When set, allows the user to navigate the carousel in the same direction indefinitely.
	 * @attribute `slides` — {`number`}
	 * @attribute `currentSlide` — {`number`}
	 * @attribute `navigation` — {`boolean`} — When set, show the carousel's navigation.
	 * @attribute `pagination` — {`boolean`} — When set, show the carousel's pagination indicators.
	 * @attribute `autoplay` — {`boolean`} — When set, the slides will scroll automatically when the user is not interacting with them.
	 * @attribute `autoplay-interval` — {`number`} — Specifies the amount of time, in milliseconds, between each automatic scroll.
	 * @attribute `slides-per-page` — {`number`} — Specifies how many slides should be shown at a given time.
	 * @attribute `slides-per-move` — {`number`} — Specifies the number of slides the carousel will advance when scrolling, useful when specifying a `slides-per-page`
	 * greater than one. It can't be higher than `slides-per-page`.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — Specifies the orientation in which the carousel will lay out.
	 * @attribute `mouse-dragging` — {`boolean`} — When set, it is possible to scroll through the slides by dragging them with the mouse.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `loop` — {`boolean`} — When set, allows the user to navigate the carousel in the same direction indefinitely.
	 * @property `slides` — {`number`}
	 * @property `currentSlide` — {`number`}
	 * @property `navigation` — {`boolean`} — When set, show the carousel's navigation.
	 * @property `pagination` — {`boolean`} — When set, show the carousel's pagination indicators.
	 * @property `autoplay` — {`boolean`} — When set, the slides will scroll automatically when the user is not interacting with them.
	 * @property `autoplayInterval` — {`number`} — Specifies the amount of time, in milliseconds, between each automatic scroll.
	 * @property `slidesPerPage` — {`number`} — Specifies how many slides should be shown at a given time.
	 * @property `slidesPerMove` — {`number`} — Specifies the number of slides the carousel will advance when scrolling, useful when specifying a `slides-per-page`
	 * greater than one. It can't be higher than `slides-per-page`.
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — Specifies the orientation in which the carousel will lay out.
	 * @property `mouseDragging` — {`boolean`} — When set, it is possible to scroll through the slides by dragging them with the mouse.
	 * @property `scrollContainer` — {`HTMLElement`}
	 * @property `paginationContainer` — {`HTMLElement`}
	 * @property `activeSlide` — {`number`}
	 * @property `scrolling` — {`boolean`}
	 * @property `dragging` — {`boolean`}
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The carousel's internal wrapper.
	 * @cssPart `scroll-container` — The scroll container that wraps the slides.
	 * @cssPart `pagination` — The pagination indicators wrapper.
	 * @cssPart `pagination-item` — The pagination indicator.
	 * @cssPart `pagination-item-active` — Applied when the item is active.
	 * @cssPart `navigation` — The navigation wrapper.
	 * @cssPart `navigation-button` — The navigation button.
	 * @cssPart `navigation-button-previous` — Applied to the previous button.
	 * @cssPart `navigation-button-next` — Applied to the next button.
	 * @cssProperty `--aspect-ratio` — The aspect ratio of each slide.
	 * @cssProperty `--scroll-hint` — The amount of padding to apply to the scroll area, allowing adjacent slides to become partially visible as a scroll hint.
	 * @cssProperty `--slide-gap` — The space between each slide.
	 */
	'wa-carousel': {
		elementClass: HTMLElementTagNameMap['wa-carousel'];
		properties: {
			/**
			 * @attribute `loop`
			 * @reflects
			 */
			loop?: HTMLElementTagNameMap['wa-carousel']['loop'];
			/**
			 * @attribute `slides`
			 * @reflects
			 */
			slides?: HTMLElementTagNameMap['wa-carousel']['slides'];
			/**
			 * @attribute `currentSlide`
			 * @reflects
			 */
			currentSlide?: HTMLElementTagNameMap['wa-carousel']['currentSlide'];
			/**
			 * @attribute `navigation`
			 * @reflects
			 */
			navigation?: HTMLElementTagNameMap['wa-carousel']['navigation'];
			/**
			 * @attribute `pagination`
			 * @reflects
			 */
			pagination?: HTMLElementTagNameMap['wa-carousel']['pagination'];
			/**
			 * @attribute `autoplay`
			 * @reflects
			 */
			autoplay?: HTMLElementTagNameMap['wa-carousel']['autoplay'];
			/**
			 * @attribute `autoplay-interval`
			 */
			autoplayInterval?: HTMLElementTagNameMap['wa-carousel']['autoplayInterval'];
			/**
			 * @attribute `slides-per-page`
			 */
			slidesPerPage?: HTMLElementTagNameMap['wa-carousel']['slidesPerPage'];
			/**
			 * @attribute `slides-per-move`
			 */
			slidesPerMove?: HTMLElementTagNameMap['wa-carousel']['slidesPerMove'];
			/**
			 * @attribute `orientation`
			 */
			orientation?: HTMLElementTagNameMap['wa-carousel']['orientation'];
			/**
			 * @attribute `mouse-dragging`
			 * @reflects
			 */
			mouseDragging?: HTMLElementTagNameMap['wa-carousel']['mouseDragging'];
			scrollContainer?: HTMLElementTagNameMap['wa-carousel']['scrollContainer'];
			paginationContainer?: HTMLElementTagNameMap['wa-carousel']['paginationContainer'];
			activeSlide?: HTMLElementTagNameMap['wa-carousel']['activeSlide'];
			scrolling?: HTMLElementTagNameMap['wa-carousel']['scrolling'];
			dragging?: HTMLElementTagNameMap['wa-carousel']['dragging'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-carousel']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-carousel']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-carousel']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-carousel']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-carousel']['didSSR'];
		};
		attributes: {
			/**
			 * @property `loop`
			 */
			loop?: boolean;
			/**
			 * @property `slides`
			 */
			slides?: number;
			/**
			 * @property `currentSlide`
			 */
			currentSlide?: number;
			/**
			 * @property `navigation`
			 */
			navigation?: boolean;
			/**
			 * @property `pagination`
			 */
			pagination?: boolean;
			/**
			 * @property `autoplay`
			 */
			autoplay?: boolean;
			/**
			 * @property `autoplayInterval`
			 */
			'autoplay-interval'?: number;
			/**
			 * @property `slidesPerPage`
			 */
			'slides-per-page'?: number;
			/**
			 * @property `slidesPerMove`
			 */
			'slides-per-move'?: number;
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `mouseDragging`
			 */
			'mouse-dragging'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-slide-change'?: GlobalEventHandlersEventMap['wa-slide-change'];
		};
		cssProperties: {
			'--aspect-ratio'?: string;
			'--scroll-hint'?: string;
			'--slide-gap'?: string;
		};
		cssParts: {
			base?: string;
			'scroll-container'?: string;
			pagination?: string;
			'pagination-item'?: string;
			'pagination-item-active'?: string;
			navigation?: string;
			'navigation-button'?: string;
			'navigation-button-previous'?: string;
			'navigation-button-next'?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			'next-icon'?: string;
			'previous-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaInput extends WebAwesomeFormAssociatedElement (<wa-input>)
	 * ```
	 * ---
	 * @summary Inputs collect single-line data from the user, such as text, numbers, email addresses, and passwords. They
	 * support labels, hints, validation, and prefix or suffix slots.
	 * @slot `label` — The input's label. Alternatively, you can use the `label` attribute.
	 * @slot `start` — An element, such as `<wa-icon>`, placed at the start of the input control.
	 * @slot `end` — An element, such as `<wa-icon>`, placed at the end of the input control.
	 * @slot `clear-icon` — An icon to use in lieu of the default clear icon.
	 * @slot `show-password-icon` — An icon to use in lieu of the default show password icon.
	 * @slot `hide-password-icon` — An icon to use in lieu of the default hide password icon.
	 * @slot `hint` — Text that describes how to use the input. Alternatively, you can use the `hint` attribute.
	 * @fires `input` — {`InputEvent`} — Emitted when the control receives input.
	 * @fires `change` — {`Event`} — Emitted when an alteration to the control's value is committed by the user.
	 * @fires `blur` — {`Event`} — Emitted when the control loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the control gains focus.
	 * @fires `wa-clear` — {`Event`} — Emitted when the clear button is activated.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `title` — {`string`}
	 * @attribute `type` — {`| 'date'
	 *	 | 'datetime-local'
	 *	 | 'email'
	 *	 | 'number'
	 *	 | 'password'
	 *	 | 'search'
	 *	 | 'tel'
	 *	 | 'text'
	 *	 | 'time'
	 *	 | 'url'`} — The type of input. Works the same as a native `<input>` element, but only a subset of types are supported. Defaults
	 * to `text`.
	 * @attribute `value` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The input's size.
	 * @attribute `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The input's visual appearance.
	 * @attribute `pill` — {`boolean`} — Draws a pill-style input with rounded edges.
	 * @attribute `label` — {`string`} — The input's label. If you need to display HTML, use the `label` slot instead.
	 * @attribute `hint` — {`string`} — The input's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `with-clear` — {`boolean`} — Adds a clear button when the input is not empty.
	 * @attribute `placeholder` — {`string`} — Placeholder text to show as a hint when the input is empty.
	 * @attribute `readonly` — {`boolean`} — Makes the input readonly.
	 * @attribute `password-toggle` — {`boolean`} — Adds a button to toggle the password's visibility. Only applies to password types.
	 * @attribute `password-visible` — {`boolean`} — Determines whether or not the password is currently visible. Only applies to password input types.
	 * @attribute `without-spin-buttons` — {`boolean`} — Hides the browser's built-in increment/decrement spin buttons for number inputs.
	 * @attribute `required` — {`boolean`} — Makes the input a required field.
	 * @attribute `pattern` — {`string`} — A regular expression pattern to validate input against.
	 * @attribute `minlength` — {`number`} — The minimum length of input that will be considered valid.
	 * @attribute `maxlength` — {`number`} — The maximum length of input that will be considered valid.
	 * @attribute `min` — {`number | string`} — The input's minimum value. Only applies to date and number input types.
	 * @attribute `max` — {`number | string`} — The input's maximum value. Only applies to date and number input types.
	 * @attribute `step` — {`number | 'any'`} — Specifies the granularity that the value must adhere to, or the special value `any` which means no stepping is
	 * implied, allowing any numeric value. Only applies to date and number input types.
	 * @attribute `autocapitalize` — {`'off' | 'none' | 'on' | 'sentences' | 'words' | 'characters'`} — Controls whether and how text input is automatically capitalized as it is entered by the user.
	 * @attribute `autocorrect` — {`boolean`} — Indicates whether the browser's autocorrect feature is on or off. When set as an attribute, use `"off"` or `"on"`.
	 * When set as a property, use `true` or `false`.
	 * @attribute `autocomplete` — {`string`} — Specifies what permission the browser has to provide assistance in filling out form field values. Refer to
	 * [this page on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) for available values.
	 * @attribute `autofocus` — {`boolean`} — Indicates that the input should receive focus on page load.
	 * @attribute `enterkeyhint` — {`'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send'`} — Used to customize the label or icon of the Enter key on virtual keyboards.
	 * @attribute `spellcheck` — {`boolean`} — Enables spell checking on the input.
	 * @attribute `inputmode` — {`'none' | 'text' | 'decimal' | 'numeric' | 'tel' | 'search' | 'email' | 'url'`} — Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
	 * keyboard on supportive devices.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `name` — {`string | null`} — The name of the input, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `disabled` — {`boolean`} — Disables the form control. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `title` — {`string`}
	 * @property `type` — {`| 'date'
	 *	 | 'datetime-local'
	 *	 | 'email'
	 *	 | 'number'
	 *	 | 'password'
	 *	 | 'search'
	 *	 | 'tel'
	 *	 | 'text'
	 *	 | 'time'
	 *	 | 'url'`} — The type of input. Works the same as a native `<input>` element, but only a subset of types are supported. Defaults
	 * to `text`.
	 * @property `value` — The current value of the input, submitted as a name/value pair with form data.
	 * @property `defaultValue` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The input's size.
	 * @property `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The input's visual appearance.
	 * @property `pill` — {`boolean`} — Draws a pill-style input with rounded edges.
	 * @property `label` — {`string`} — The input's label. If you need to display HTML, use the `label` slot instead.
	 * @property `hint` — {`string`} — The input's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `withClear` — {`boolean`} — Adds a clear button when the input is not empty.
	 * @property `placeholder` — {`string`} — Placeholder text to show as a hint when the input is empty.
	 * @property `readonly` — {`boolean`} — Makes the input readonly.
	 * @property `passwordToggle` — {`boolean`} — Adds a button to toggle the password's visibility. Only applies to password types.
	 * @property `passwordVisible` — {`boolean`} — Determines whether or not the password is currently visible. Only applies to password input types.
	 * @property `withoutSpinButtons` — {`boolean`} — Hides the browser's built-in increment/decrement spin buttons for number inputs.
	 * @property `required` — {`boolean`} — Makes the input a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `pattern` — {`string`} — A regular expression pattern to validate input against.
	 * @property `minlength` — {`number`} — The minimum length of input that will be considered valid.
	 * @property `maxlength` — {`number`} — The maximum length of input that will be considered valid.
	 * @property `min` — {`number | string`} — The input's minimum value. Only applies to date and number input types.
	 * @property `max` — {`number | string`} — The input's maximum value. Only applies to date and number input types.
	 * @property `step` — {`number | 'any'`} — Specifies the granularity that the value must adhere to, or the special value `any` which means no stepping is
	 * implied, allowing any numeric value. Only applies to date and number input types.
	 * @property `autocapitalize` — {`'off' | 'none' | 'on' | 'sentences' | 'words' | 'characters'`} — Controls whether and how text input is automatically capitalized as it is entered by the user.
	 * @property `autocorrect` — {`boolean`} — Indicates whether the browser's autocorrect feature is on or off. When set as an attribute, use `"off"` or `"on"`.
	 * When set as a property, use `true` or `false`.
	 * @property `autocomplete` — {`string`} — Specifies what permission the browser has to provide assistance in filling out form field values. Refer to
	 * [this page on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) for available values.
	 * @property `autofocus` — {`boolean`} — Indicates that the input should receive focus on page load.
	 * @property `enterkeyhint` — {`'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send'`} — Used to customize the label or icon of the Enter key on virtual keyboards.
	 * @property `spellcheck` — {`boolean`} — Enables spell checking on the input.
	 * @property `inputmode` — {`'none' | 'text' | 'decimal' | 'numeric' | 'tel' | 'search' | 'email' | 'url'`} — Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
	 * keyboard on supportive devices.
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `name` — {`string | null`} — The name of the input, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `disabled` — {`boolean`} — Disables the form control. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `label` — The label
	 * @cssPart `hint` — The hint's wrapper.
	 * @cssPart `base` — The wrapper being rendered as an input
	 * @cssPart `input` — The internal `<input>` control.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `clear-button` — The clear button.
	 * @cssPart `password-toggle-button` — The password toggle button.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssState `blank` — The input is empty.
	 */
	'wa-input': {
		elementClass: HTMLElementTagNameMap['wa-input'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-input']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-input']['input'];
			/**
			 * @attribute `title`
			 */
			title?: HTMLElementTagNameMap['wa-input']['title'];
			/**
			 * @attribute `type`
			 * @reflects
			 */
			type?: HTMLElementTagNameMap['wa-input']['type'];
			value?: HTMLElementTagNameMap['wa-input']['value'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			defaultValue?: HTMLElementTagNameMap['wa-input']['defaultValue'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-input']['size'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-input']['appearance'];
			/**
			 * @attribute `pill`
			 * @reflects
			 */
			pill?: HTMLElementTagNameMap['wa-input']['pill'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-input']['label'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-input']['hint'];
			/**
			 * @attribute `with-clear`
			 */
			withClear?: HTMLElementTagNameMap['wa-input']['withClear'];
			/**
			 * @attribute `placeholder`
			 */
			placeholder?: HTMLElementTagNameMap['wa-input']['placeholder'];
			/**
			 * @attribute `readonly`
			 * @reflects
			 */
			readonly?: HTMLElementTagNameMap['wa-input']['readonly'];
			/**
			 * @attribute `password-toggle`
			 */
			passwordToggle?: HTMLElementTagNameMap['wa-input']['passwordToggle'];
			/**
			 * @attribute `password-visible`
			 */
			passwordVisible?: HTMLElementTagNameMap['wa-input']['passwordVisible'];
			/**
			 * @attribute `without-spin-buttons`
			 * @reflects
			 */
			withoutSpinButtons?: HTMLElementTagNameMap['wa-input']['withoutSpinButtons'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-input']['required'];
			/**
			 * @attribute `pattern`
			 */
			pattern?: HTMLElementTagNameMap['wa-input']['pattern'];
			/**
			 * @attribute `minlength`
			 */
			minlength?: HTMLElementTagNameMap['wa-input']['minlength'];
			/**
			 * @attribute `maxlength`
			 */
			maxlength?: HTMLElementTagNameMap['wa-input']['maxlength'];
			/**
			 * @attribute `min`
			 */
			min?: HTMLElementTagNameMap['wa-input']['min'];
			/**
			 * @attribute `max`
			 */
			max?: HTMLElementTagNameMap['wa-input']['max'];
			/**
			 * @attribute `step`
			 */
			step?: HTMLElementTagNameMap['wa-input']['step'];
			/**
			 * @attribute `autocapitalize`
			 */
			autocapitalize?: HTMLElementTagNameMap['wa-input']['autocapitalize'];
			/**
			 * @attribute `autocorrect`
			 */
			autocorrect?: HTMLElementTagNameMap['wa-input']['autocorrect'];
			/**
			 * @attribute `autocomplete`
			 */
			autocomplete?: HTMLElementTagNameMap['wa-input']['autocomplete'];
			/**
			 * @attribute `autofocus`
			 */
			autofocus?: HTMLElementTagNameMap['wa-input']['autofocus'];
			/**
			 * @attribute `enterkeyhint`
			 */
			enterkeyhint?: HTMLElementTagNameMap['wa-input']['enterkeyhint'];
			/**
			 * @attribute `spellcheck`
			 */
			spellcheck?: HTMLElementTagNameMap['wa-input']['spellcheck'];
			/**
			 * @attribute `inputmode`
			 */
			inputmode?: HTMLElementTagNameMap['wa-input']['inputmode'];
			/**
			 * @attribute `with-label`
			 */
			withLabel?: HTMLElementTagNameMap['wa-input']['withLabel'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-input']['withHint'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-input']['name'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-input']['disabled'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-input']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-input']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-input']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-input']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-input']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-input']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-input']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-input']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-input']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-input']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-input']['didSSR'];
		};
		attributes: {
			/**
			 * @property `title`
			 */
			title?: string;
			/**
			 * @property `type`
			 */
			type?:
				| 'date'
				| 'datetime-local'
				| 'email'
				| 'number'
				| 'password'
				| 'search'
				| 'tel'
				| 'text'
				| 'time'
				| 'url';
			/**
			 * @property `defaultValue`
			 */
			value?: string | null;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `appearance`
			 */
			appearance?: 'filled' | 'outlined' | 'filled-outlined';
			/**
			 * @property `pill`
			 */
			pill?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `withClear`
			 */
			'with-clear'?: boolean;
			/**
			 * @property `placeholder`
			 */
			placeholder?: string;
			/**
			 * @property `readonly`
			 */
			readonly?: boolean;
			/**
			 * @property `passwordToggle`
			 */
			'password-toggle'?: boolean;
			/**
			 * @property `passwordVisible`
			 */
			'password-visible'?: boolean;
			/**
			 * @property `withoutSpinButtons`
			 */
			'without-spin-buttons'?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `pattern`
			 */
			pattern?: string;
			/**
			 * @property `minlength`
			 */
			minlength?: number;
			/**
			 * @property `maxlength`
			 */
			maxlength?: number;
			/**
			 * @property `min`
			 */
			min?: number | string;
			/**
			 * @property `max`
			 */
			max?: number | string;
			/**
			 * @property `step`
			 */
			step?: number | 'any';
			/**
			 * @property `autocapitalize`
			 */
			autocapitalize?:
				| 'off'
				| 'none'
				| 'on'
				| 'sentences'
				| 'words'
				| 'characters';
			/**
			 * @property `autocorrect`
			 */
			autocorrect?: boolean;
			/**
			 * @property `autocomplete`
			 */
			autocomplete?: string;
			/**
			 * @property `autofocus`
			 */
			autofocus?: boolean;
			/**
			 * @property `enterkeyhint`
			 */
			enterkeyhint?:
				| 'enter'
				| 'done'
				| 'go'
				| 'next'
				| 'previous'
				| 'search'
				| 'send';
			/**
			 * @property `spellcheck`
			 */
			spellcheck?: boolean;
			/**
			 * @property `inputmode`
			 */
			inputmode?:
				| 'none'
				| 'text'
				| 'decimal'
				| 'numeric'
				| 'tel'
				| 'search'
				| 'email'
				| 'url';
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			input?: GlobalEventHandlersEventMap['input'];
			change?: GlobalEventHandlersEventMap['change'];
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			'wa-clear'?: GlobalEventHandlersEventMap['wa-clear'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {};
		cssParts: {
			label?: string;
			hint?: string;
			base?: string;
			input?: string;
			start?: string;
			'clear-button'?: string;
			'password-toggle-button'?: string;
			end?: string;
		};
		cssStates: {
			blank?: string;
		};
		slots: {
			label?: string;
			start?: string;
			end?: string;
			'clear-icon'?: string;
			'show-password-icon'?: string;
			'hide-password-icon'?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaPopup extends WebAwesomeElement (<wa-popup>)
	 * ```
	 * ---
	 * @summary Popups declaratively anchor one element to another and keep them positioned together as the page scrolls or
	 * resizes. Primarily a low-level building block for popovers, dropdowns, and tooltips.
	 * @slot [***default***] — The popup's content.
	 * @slot `anchor` — The element the popup will be anchored to. If the anchor lives outside of the popup, you can use the `anchor` attribute or property instead.
	 * @fires `wa-reposition` — {`Event`} — Emitted when the popup is repositioned. This event can fire a lot, so avoid putting expensive operations in your listener or consider debouncing it.
	 * @attribute `anchor` — {`Element | string | VirtualElement`} — The element the popup will be anchored to. If the anchor lives outside of the popup, you can provide the anchor
	 * element `id`, a DOM element reference, or a `VirtualElement`. If the anchor lives inside the popup, use the
	 * `anchor` slot instead.
	 * @attribute `active` — {`boolean`} — Activates the positioning logic and shows the popup. When this attribute is removed, the positioning logic is torn
	 * down and the popup will be hidden.
	 * @attribute `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the popup. Note that the actual placement will vary as configured to keep the
	 * panel inside of the viewport.
	 * @attribute `boundary` — {`'viewport' | 'scroll'`} — The bounding box to use for flipping, shifting, and auto-sizing.
	 * @attribute `distance` — {`number`} — The distance in pixels from which to offset the panel away from its anchor.
	 * @attribute `skidding` — {`number`} — The distance in pixels from which to offset the panel along its anchor.
	 * @attribute `arrow` — {`boolean`} — Attaches an arrow to the popup. The arrow's size and color can be customized using the `--arrow-size` and
	 * `--arrow-color` custom properties. For additional customizations, you can also target the arrow using
	 * `::part(arrow)` in your stylesheet.
	 * @attribute `arrow-placement` — {`'start' | 'end' | 'center' | 'anchor'`} — The placement of the arrow. The default is `anchor`, which will align the arrow as close to the center of the
	 * anchor as possible, considering available space and `arrow-padding`. A value of `start`, `end`, or `center` will
	 * align the arrow to the start, end, or center of the popover instead.
	 * @attribute `arrow-padding` — {`number`} — The amount of padding between the arrow and the edges of the popup. If the popup has a border-radius, for example,
	 * this will prevent it from overflowing the corners.
	 * @attribute `flip` — {`boolean`} — When set, placement of the popup will flip to the opposite site to keep it in view. You can use
	 * `flipFallbackPlacements` to further configure how the fallback placement is determined.
	 * @attribute `flip-fallback-placements` — {`string`} — If the preferred placement doesn't fit, popup will be tested in these fallback placements until one fits. Must be a
	 * string of any number of placements separated by a space, e.g. "top bottom left". If no placement fits, the flip
	 * fallback strategy will be used instead.
	 * @attribute `flip-fallback-strategy` — {`'best-fit' | 'initial'`} — When neither the preferred placement nor the fallback placements fit, this value will be used to determine whether
	 * the popup should be positioned using the best available fit based on available space or as it was initially
	 * preferred.
	 * @attribute `flipBoundary` — {`Element | Element[]`} — The flip boundary describes clipping element(s) that overflow will be checked relative to when flipping. By
	 * default, the boundary includes overflow ancestors that will cause the element to be clipped. If needed, you can
	 * change the boundary by passing a reference to one or more elements to this property.
	 * @attribute `flip-padding` — {`number`} — The amount of padding, in pixels, to exceed before the flip behavior will occur.
	 * @attribute `shift` — {`boolean`} — Moves the popup along the axis to keep it in view when clipped.
	 * @attribute `shiftBoundary` — {`Element | Element[]`} — The shift boundary describes clipping element(s) that overflow will be checked relative to when shifting. By
	 * default, the boundary includes overflow ancestors that will cause the element to be clipped. If needed, you can
	 * change the boundary by passing a reference to one or more elements to this property.
	 * @attribute `shift-padding` — {`number`} — The amount of padding, in pixels, to exceed before the shift behavior will occur.
	 * @attribute `auto-size` — {`'horizontal' | 'vertical' | 'both'`} — When set, this will cause the popup to automatically resize itself to prevent it from overflowing.
	 * @attribute `sync` — {`'width' | 'height' | 'both'`} — Syncs the popup's width or height to that of the anchor element.
	 * @attribute `autoSizeBoundary` — {`Element | Element[]`} — The auto-size boundary describes clipping element(s) that overflow will be checked relative to when resizing. By
	 * default, the boundary includes overflow ancestors that will cause the element to be clipped. If needed, you can
	 * change the boundary by passing a reference to one or more elements to this property.
	 * @attribute `auto-size-padding` — {`number`} — The amount of padding, in pixels, to exceed before the auto-size behavior will occur.
	 * @attribute `hover-bridge` — {`boolean`} — When a gap exists between the anchor and the popup element, this option will add a "hover bridge" that fills the
	 * gap using an invisible element. This makes listening for events such as `mouseenter` and `mouseleave` more sane
	 * because the pointer never technically leaves the element. The hover bridge will only be drawn when the popover is
	 * active.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `popup` — {`HTMLElement`} — A reference to the internal popup container. Useful for animating and styling the popup with JavaScript.
	 * @property `anchor` — {`Element | string | VirtualElement`} — The element the popup will be anchored to. If the anchor lives outside of the popup, you can provide the anchor
	 * element `id`, a DOM element reference, or a `VirtualElement`. If the anchor lives inside the popup, use the
	 * `anchor` slot instead.
	 * @property `active` — {`boolean`} — Activates the positioning logic and shows the popup. When this attribute is removed, the positioning logic is torn
	 * down and the popup will be hidden.
	 * @property `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the popup. Note that the actual placement will vary as configured to keep the
	 * panel inside of the viewport.
	 * @property `boundary` — {`'viewport' | 'scroll'`} — The bounding box to use for flipping, shifting, and auto-sizing.
	 * @property `distance` — {`number`} — The distance in pixels from which to offset the panel away from its anchor.
	 * @property `skidding` — {`number`} — The distance in pixels from which to offset the panel along its anchor.
	 * @property `arrow` — {`boolean`} — Attaches an arrow to the popup. The arrow's size and color can be customized using the `--arrow-size` and
	 * `--arrow-color` custom properties. For additional customizations, you can also target the arrow using
	 * `::part(arrow)` in your stylesheet.
	 * @property `arrowPlacement` — {`'start' | 'end' | 'center' | 'anchor'`} — The placement of the arrow. The default is `anchor`, which will align the arrow as close to the center of the
	 * anchor as possible, considering available space and `arrow-padding`. A value of `start`, `end`, or `center` will
	 * align the arrow to the start, end, or center of the popover instead.
	 * @property `arrowPadding` — {`number`} — The amount of padding between the arrow and the edges of the popup. If the popup has a border-radius, for example,
	 * this will prevent it from overflowing the corners.
	 * @property `flip` — {`boolean`} — When set, placement of the popup will flip to the opposite site to keep it in view. You can use
	 * `flipFallbackPlacements` to further configure how the fallback placement is determined.
	 * @property `flipFallbackPlacements` — {`string`} — If the preferred placement doesn't fit, popup will be tested in these fallback placements until one fits. Must be a
	 * string of any number of placements separated by a space, e.g. "top bottom left". If no placement fits, the flip
	 * fallback strategy will be used instead.
	 * @property `flipFallbackStrategy` — {`'best-fit' | 'initial'`} — When neither the preferred placement nor the fallback placements fit, this value will be used to determine whether
	 * the popup should be positioned using the best available fit based on available space or as it was initially
	 * preferred.
	 * @property `flipBoundary` — {`Element | Element[]`} — The flip boundary describes clipping element(s) that overflow will be checked relative to when flipping. By
	 * default, the boundary includes overflow ancestors that will cause the element to be clipped. If needed, you can
	 * change the boundary by passing a reference to one or more elements to this property.
	 * @property `flipPadding` — {`number`} — The amount of padding, in pixels, to exceed before the flip behavior will occur.
	 * @property `shift` — {`boolean`} — Moves the popup along the axis to keep it in view when clipped.
	 * @property `shiftBoundary` — {`Element | Element[]`} — The shift boundary describes clipping element(s) that overflow will be checked relative to when shifting. By
	 * default, the boundary includes overflow ancestors that will cause the element to be clipped. If needed, you can
	 * change the boundary by passing a reference to one or more elements to this property.
	 * @property `shiftPadding` — {`number`} — The amount of padding, in pixels, to exceed before the shift behavior will occur.
	 * @property `autoSize` — {`'horizontal' | 'vertical' | 'both'`} — When set, this will cause the popup to automatically resize itself to prevent it from overflowing.
	 * @property `sync` — {`'width' | 'height' | 'both'`} — Syncs the popup's width or height to that of the anchor element.
	 * @property `autoSizeBoundary` — {`Element | Element[]`} — The auto-size boundary describes clipping element(s) that overflow will be checked relative to when resizing. By
	 * default, the boundary includes overflow ancestors that will cause the element to be clipped. If needed, you can
	 * change the boundary by passing a reference to one or more elements to this property.
	 * @property `autoSizePadding` — {`number`} — The amount of padding, in pixels, to exceed before the auto-size behavior will occur.
	 * @property `hoverBridge` — {`boolean`} — When a gap exists between the anchor and the popup element, this option will add a "hover bridge" that fills the
	 * gap using an invisible element. This makes listening for events such as `mouseenter` and `mouseleave` more sane
	 * because the pointer never technically leaves the element. The hover bridge will only be drawn when the popover is
	 * active.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `arrow` — The arrow's container. Avoid setting `top|bottom|left|right` properties, as these values are assigned dynamically as the popup moves. This is most useful for applying a background color to match the popup, and maybe a border or box shadow.
	 * @cssPart `popup` — The popup's container. Useful for setting a background color, box shadow, etc.
	 * @cssPart `hover-bridge` — The hover bridge element. Only available when the `hover-bridge` option is enabled.
	 * @cssProperty `--arrow-size` — The size of the arrow. Note that an arrow won't be shown unless the `arrow` attribute is used.
	 * @cssProperty `--popup-border-width` — The width of any custom border applied to the popup. This is used to reposition the arrow to overlap to the inside edge of the popup border.
	 * @cssProperty `--arrow-color` — The color of the arrow.
	 * @cssProperty `--auto-size-available-width` — A read-only custom property that determines the amount of width the popup can be before overflowing. Useful for positioning child elements that need to overflow. This property is only available when using `auto-size`.
	 * @cssProperty `--auto-size-available-height` — A read-only custom property that determines the amount of height the popup can be before overflowing. Useful for positioning child elements that need to overflow. This property is only available when using `auto-size`.
	 * @cssProperty `--show-duration` — The show duration to use when applying built-in animation classes.
	 * @cssProperty `--hide-duration` — The hide duration to use when applying built-in animation classes.
	 */
	'wa-popup': {
		elementClass: HTMLElementTagNameMap['wa-popup'];
		properties: {
			popup?: HTMLElementTagNameMap['wa-popup']['popup'];
			/**
			 * @attribute `anchor`
			 */
			anchor?: HTMLElementTagNameMap['wa-popup']['anchor'];
			/**
			 * @attribute `active`
			 * @reflects
			 */
			active?: HTMLElementTagNameMap['wa-popup']['active'];
			/**
			 * @attribute `placement`
			 * @reflects
			 */
			placement?: HTMLElementTagNameMap['wa-popup']['placement'];
			/**
			 * @attribute `boundary`
			 */
			boundary?: HTMLElementTagNameMap['wa-popup']['boundary'];
			/**
			 * @attribute `distance`
			 */
			distance?: HTMLElementTagNameMap['wa-popup']['distance'];
			/**
			 * @attribute `skidding`
			 */
			skidding?: HTMLElementTagNameMap['wa-popup']['skidding'];
			/**
			 * @attribute `arrow`
			 */
			arrow?: HTMLElementTagNameMap['wa-popup']['arrow'];
			/**
			 * @attribute `arrow-placement`
			 */
			arrowPlacement?: HTMLElementTagNameMap['wa-popup']['arrowPlacement'];
			/**
			 * @attribute `arrow-padding`
			 */
			arrowPadding?: HTMLElementTagNameMap['wa-popup']['arrowPadding'];
			/**
			 * @attribute `flip`
			 */
			flip?: HTMLElementTagNameMap['wa-popup']['flip'];
			/**
			 * @attribute `flip-fallback-placements`
			 */
			flipFallbackPlacements?: HTMLElementTagNameMap['wa-popup']['flipFallbackPlacements'];
			/**
			 * @attribute `flip-fallback-strategy`
			 */
			flipFallbackStrategy?: HTMLElementTagNameMap['wa-popup']['flipFallbackStrategy'];
			/**
			 * @attribute `flipBoundary`
			 */
			flipBoundary?: HTMLElementTagNameMap['wa-popup']['flipBoundary'];
			/**
			 * @attribute `flip-padding`
			 */
			flipPadding?: HTMLElementTagNameMap['wa-popup']['flipPadding'];
			/**
			 * @attribute `shift`
			 */
			shift?: HTMLElementTagNameMap['wa-popup']['shift'];
			/**
			 * @attribute `shiftBoundary`
			 */
			shiftBoundary?: HTMLElementTagNameMap['wa-popup']['shiftBoundary'];
			/**
			 * @attribute `shift-padding`
			 */
			shiftPadding?: HTMLElementTagNameMap['wa-popup']['shiftPadding'];
			/**
			 * @attribute `auto-size`
			 */
			autoSize?: HTMLElementTagNameMap['wa-popup']['autoSize'];
			/**
			 * @attribute `sync`
			 */
			sync?: HTMLElementTagNameMap['wa-popup']['sync'];
			/**
			 * @attribute `autoSizeBoundary`
			 */
			autoSizeBoundary?: HTMLElementTagNameMap['wa-popup']['autoSizeBoundary'];
			/**
			 * @attribute `auto-size-padding`
			 */
			autoSizePadding?: HTMLElementTagNameMap['wa-popup']['autoSizePadding'];
			/**
			 * @attribute `hover-bridge`
			 */
			hoverBridge?: HTMLElementTagNameMap['wa-popup']['hoverBridge'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-popup']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-popup']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-popup']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-popup']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-popup']['didSSR'];
		};
		attributes: {
			/**
			 * @property `anchor`
			 */
			anchor?: unknown | string | unknown;
			/**
			 * @property `active`
			 */
			active?: boolean;
			/**
			 * @property `placement`
			 */
			placement?:
				| 'top'
				| 'top-start'
				| 'top-end'
				| 'bottom'
				| 'bottom-start'
				| 'bottom-end'
				| 'right'
				| 'right-start'
				| 'right-end'
				| 'left'
				| 'left-start'
				| 'left-end';
			/**
			 * @property `boundary`
			 */
			boundary?: 'viewport' | 'scroll';
			/**
			 * @property `distance`
			 */
			distance?: number;
			/**
			 * @property `skidding`
			 */
			skidding?: number;
			/**
			 * @property `arrow`
			 */
			arrow?: boolean;
			/**
			 * @property `arrowPlacement`
			 */
			'arrow-placement'?: 'start' | 'end' | 'center' | 'anchor';
			/**
			 * @property `arrowPadding`
			 */
			'arrow-padding'?: number;
			/**
			 * @property `flip`
			 */
			flip?: boolean;
			/**
			 * @property `flipFallbackPlacements`
			 */
			'flip-fallback-placements'?: string;
			/**
			 * @property `flipFallbackStrategy`
			 */
			'flip-fallback-strategy'?: 'best-fit' | 'initial';
			/**
			 * @property `flipBoundary`
			 */
			flipBoundary?: unknown | unknown;
			/**
			 * @property `flipPadding`
			 */
			'flip-padding'?: number;
			/**
			 * @property `shift`
			 */
			shift?: boolean;
			/**
			 * @property `shiftBoundary`
			 */
			shiftBoundary?: unknown | unknown;
			/**
			 * @property `shiftPadding`
			 */
			'shift-padding'?: number;
			/**
			 * @property `autoSize`
			 */
			'auto-size'?: 'horizontal' | 'vertical' | 'both';
			/**
			 * @property `sync`
			 */
			sync?: 'width' | 'height' | 'both';
			/**
			 * @property `autoSizeBoundary`
			 */
			autoSizeBoundary?: unknown | unknown;
			/**
			 * @property `autoSizePadding`
			 */
			'auto-size-padding'?: number;
			/**
			 * @property `hoverBridge`
			 */
			'hover-bridge'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-reposition'?: GlobalEventHandlersEventMap['wa-reposition'];
		};
		cssProperties: {
			'--arrow-size'?: string;
			'--popup-border-width'?: string;
			'--arrow-color'?: string;
			'--auto-size-available-width'?: string;
			'--auto-size-available-height'?: string;
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			arrow?: string;
			popup?: string;
			'hover-bridge'?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			anchor?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaColorPicker extends WebAwesomeFormAssociatedElement (<wa-color-picker>)
	 * ```
	 * ---
	 * @summary Color pickers let users choose a color from a visual palette or by entering a value. They support HEX, RGB,
	 * HSL, and HSV formats with optional alpha channel and swatch presets.
	 * @slot `label` — The color picker's form label. Alternatively, you can use the `label` attribute.
	 * @slot `hint` — The color picker's form hint. Alternatively, you can use the `hint` attribute.
	 * @fires `change` — {`Event`} — Emitted when the color picker's value changes.
	 * @fires `input` — {`InputEvent`} — Emitted when the color picker receives input.
	 * @fires `wa-show` — {`CustomEvent`}
	 * @fires `wa-after-show` — {`CustomEvent`}
	 * @fires `wa-hide` — {`CustomEvent`}
	 * @fires `wa-after-hide` — {`CustomEvent`}
	 * @fires `blur` — {`Event`} — Emitted when the color picker loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the color picker receives focus.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `value` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `label` — {`string`} — The color picker's label. This will not be displayed, but it will be announced by assistive devices. If you need to
	 * display HTML, you can use the `label` slot` instead.
	 * @attribute `hint` — {`string`} — The color picker's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `format` — {`'hex' | 'rgb' | 'hsl' | 'hsv'`} — The format to use. If opacity is enabled, these will translate to HEXA, RGBA, HSLA, and HSVA respectively. The color
	 * picker will accept user input in any format (including CSS color names) and convert it to the desired format.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — Determines the size of the color picker's trigger
	 * @attribute `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the color picker's popup. Note that the actual placement will vary as configured to
	 * keep the panel inside of the viewport.
	 * @attribute `without-format-toggle` — {`boolean`} — Removes the button that lets users toggle between format.
	 * @attribute `name` — {`string | null`} — The name of the form control, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `disabled` — {`boolean`} — Disables the color picker. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `open` — {`boolean`} — Indicates whether or not the popup is open. You can toggle this attribute to show and hide the popup, or you
	 * can use the `show()` and `hide()` methods and this attribute will reflect the popup's open state.
	 * @attribute `opacity` — {`boolean`} — Shows the opacity slider. Enabling this will cause the formatted value to be HEXA, RGBA, or HSLA.
	 * @attribute `uppercase` — {`boolean`} — By default, values are lowercase. With this attribute, values will be uppercase instead.
	 * @attribute `swatches` — {`string | string[] | WaColorPickerSwatch[]`} — One or more predefined color swatches to display as presets in the color picker. Can include any format the color
	 * picker can parse, including HEX(A), RGB(A), HSL(A), HSV(A), and CSS color names. Each color must be separated by a
	 * semicolon (`;`). Alternatively, you can pass an array of color values or an array of `{ color, label }` objects to
	 * this property using JavaScript. When using objects with labels, the label will be used for the swatch's accessible
	 * name instead of the raw color value.
	 * @attribute `required` — {`boolean`} — Makes the color picker a required field.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `base` — {`HTMLElement`}
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `triggerLabel` — {`HTMLElement`}
	 * @property `triggerButton` — {`HTMLButtonElement`}
	 * @property `popup` — {`WaPopup`}
	 * @property `previewButton` — {`HTMLButtonElement`}
	 * @property `trigger` — {`HTMLButtonElement`}
	 * @property `value` — The current value of the color picker. The value's format will vary based the `format` attribute. To get the value
	 * in a specific format, use the `getFormattedValue()` method. The value is submitted as a name/value pair with form
	 * data.
	 * @property `defaultValue` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `label` — {`string`} — The color picker's label. This will not be displayed, but it will be announced by assistive devices. If you need to
	 * display HTML, you can use the `label` slot` instead.
	 * @property `hint` — {`string`} — The color picker's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `format` — {`'hex' | 'rgb' | 'hsl' | 'hsv'`} — The format to use. If opacity is enabled, these will translate to HEXA, RGBA, HSLA, and HSVA respectively. The color
	 * picker will accept user input in any format (including CSS color names) and convert it to the desired format.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — Determines the size of the color picker's trigger
	 * @property `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the color picker's popup. Note that the actual placement will vary as configured to
	 * keep the panel inside of the viewport.
	 * @property `withoutFormatToggle` — {`boolean`} — Removes the button that lets users toggle between format.
	 * @property `name` — {`string | null`} — The name of the form control, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `disabled` — {`boolean`} — Disables the color picker. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `open` — {`boolean`} — Indicates whether or not the popup is open. You can toggle this attribute to show and hide the popup, or you
	 * can use the `show()` and `hide()` methods and this attribute will reflect the popup's open state.
	 * @property `opacity` — {`boolean`} — Shows the opacity slider. Enabling this will cause the formatted value to be HEXA, RGBA, or HSLA.
	 * @property `uppercase` — {`boolean`} — By default, values are lowercase. With this attribute, values will be uppercase instead.
	 * @property `swatches` — {`string | string[] | WaColorPickerSwatch[]`} — One or more predefined color swatches to display as presets in the color picker. Can include any format the color
	 * picker can parse, including HEX(A), RGB(A), HSL(A), HSV(A), and CSS color names. Each color must be separated by a
	 * semicolon (`;`). Alternatively, you can pass an array of color values or an array of `{ color, label }` objects to
	 * this property using JavaScript. When using objects with labels, the label will be used for the swatch's accessible
	 * name instead of the raw color value.
	 * @property `required` — {`boolean`} — Makes the color picker a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `trigger` — The color picker's dropdown trigger.
	 * @cssPart `swatches` — The container that holds the swatches.
	 * @cssPart `swatch` — Each individual swatch.
	 * @cssPart `grid` — The color grid.
	 * @cssPart `grid-handle` — The color grid's handle.
	 * @cssPart `slider` — Hue and opacity sliders.
	 * @cssPart `slider-handle` — Hue and opacity slider handles.
	 * @cssPart `hue-slider` — The hue slider.
	 * @cssPart `hue-slider-handle` — The hue slider's handle.
	 * @cssPart `opacity-slider` — The opacity slider.
	 * @cssPart `opacity-slider-handle` — The opacity slider's handle.
	 * @cssPart `preview` — The preview color.
	 * @cssPart `input` — The text input.
	 * @cssPart `eyedropper-button` — The eye dropper button.
	 * @cssPart `eyedropper-button__base` — The eye dropper button's exported `button` part.
	 * @cssPart `eyedropper-button__start` — The eye dropper button's exported `start` part.
	 * @cssPart `eyedropper-button__label` — The eye dropper button's exported `label` part.
	 * @cssPart `eyedropper-button__end` — The eye dropper button's exported `end` part.
	 * @cssPart `eyedropper-button__caret` — The eye dropper button's exported `caret` part.
	 * @cssPart `format-button` — The format button.
	 * @cssPart `format-button__base` — The format button's exported `button` part.
	 * @cssPart `format-button__start` — The format button's exported `start` part.
	 * @cssPart `format-button__label` — The format button's exported `label` part.
	 * @cssPart `format-button__end` — The format button's exported `end` part.
	 * @cssPart `format-button__caret` — The format button's exported `caret` part.
	 * @cssProperty `--grid-width` — The width of the color grid.
	 * @cssProperty `--grid-height` — The height of the color grid.
	 * @cssProperty `--grid-handle-size` — The size of the color grid's handle.
	 * @cssProperty `--slider-height` — The height of the hue and alpha sliders.
	 * @cssProperty `--slider-handle-size` — The diameter of the slider's handle.
	 */
	'wa-color-picker': {
		elementClass: HTMLElementTagNameMap['wa-color-picker'];
		properties: {
			base?: HTMLElementTagNameMap['wa-color-picker']['base'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-color-picker']['input'];
			triggerLabel?: HTMLElementTagNameMap['wa-color-picker']['triggerLabel'];
			triggerButton?: HTMLElementTagNameMap['wa-color-picker']['triggerButton'];
			popup?: HTMLElementTagNameMap['wa-color-picker']['popup'];
			previewButton?: HTMLElementTagNameMap['wa-color-picker']['previewButton'];
			trigger?: HTMLElementTagNameMap['wa-color-picker']['trigger'];
			value?: HTMLElementTagNameMap['wa-color-picker']['value'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			defaultValue?: HTMLElementTagNameMap['wa-color-picker']['defaultValue'];
			/**
			 * @attribute `with-label`
			 * @reflects
			 */
			withLabel?: HTMLElementTagNameMap['wa-color-picker']['withLabel'];
			/**
			 * @attribute `with-hint`
			 * @reflects
			 */
			withHint?: HTMLElementTagNameMap['wa-color-picker']['withHint'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-color-picker']['label'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-color-picker']['hint'];
			/**
			 * @attribute `format`
			 */
			format?: HTMLElementTagNameMap['wa-color-picker']['format'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-color-picker']['size'];
			/**
			 * @attribute `placement`
			 * @reflects
			 */
			placement?: HTMLElementTagNameMap['wa-color-picker']['placement'];
			/**
			 * @attribute `without-format-toggle`
			 */
			withoutFormatToggle?: HTMLElementTagNameMap['wa-color-picker']['withoutFormatToggle'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-color-picker']['name'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-color-picker']['disabled'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-color-picker']['open'];
			/**
			 * @attribute `opacity`
			 */
			opacity?: HTMLElementTagNameMap['wa-color-picker']['opacity'];
			/**
			 * @attribute `uppercase`
			 */
			uppercase?: HTMLElementTagNameMap['wa-color-picker']['uppercase'];
			/**
			 * @attribute `swatches`
			 */
			swatches?: HTMLElementTagNameMap['wa-color-picker']['swatches'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-color-picker']['required'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-color-picker']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-color-picker']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-color-picker']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-color-picker']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-color-picker']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-color-picker']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-color-picker']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-color-picker']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-color-picker']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-color-picker']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-color-picker']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-color-picker']['didSSR'];
		};
		attributes: {
			/**
			 * @property `defaultValue`
			 */
			value?: string | null;
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `format`
			 */
			format?: 'hex' | 'rgb' | 'hsl' | 'hsv';
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `placement`
			 */
			placement?:
				| 'top'
				| 'top-start'
				| 'top-end'
				| 'bottom'
				| 'bottom-start'
				| 'bottom-end'
				| 'right'
				| 'right-start'
				| 'right-end'
				| 'left'
				| 'left-start'
				| 'left-end';
			/**
			 * @property `withoutFormatToggle`
			 */
			'without-format-toggle'?: boolean;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `opacity`
			 */
			opacity?: boolean;
			/**
			 * @property `uppercase`
			 */
			uppercase?: boolean;
			/**
			 * @property `swatches`
			 */
			swatches?: string | unknown | unknown;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			change?: GlobalEventHandlersEventMap['change'];
			input?: GlobalEventHandlersEventMap['input'];
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {
			'--grid-width'?: string;
			'--grid-height'?: string;
			'--grid-handle-size'?: string;
			'--slider-height'?: string;
			'--slider-handle-size'?: string;
		};
		cssParts: {
			base?: string;
			trigger?: string;
			swatches?: string;
			swatch?: string;
			grid?: string;
			'grid-handle'?: string;
			slider?: string;
			'slider-handle'?: string;
			'hue-slider'?: string;
			'hue-slider-handle'?: string;
			'opacity-slider'?: string;
			'opacity-slider-handle'?: string;
			preview?: string;
			input?: string;
			'eyedropper-button'?: string;
			'eyedropper-button__base'?: string;
			'eyedropper-button__start'?: string;
			'eyedropper-button__label'?: string;
			'eyedropper-button__end'?: string;
			'eyedropper-button__caret'?: string;
			'format-button'?: string;
			'format-button__base'?: string;
			'format-button__start'?: string;
			'format-button__label'?: string;
			'format-button__end'?: string;
			'format-button__caret'?: string;
		};
		cssStates: {};
		slots: {
			label?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaComparison extends WebAwesomeElement (<wa-comparison>)
	 * ```
	 * ---
	 * @summary Comparisons show the visual differences between two pieces of similar content using a draggable divider. Use
	 * them for before/after images, design revisions, or side-by-side previews.
	 * @slot `before` — The before content, often an `<img>` or `<svg>` element.
	 * @slot `after` — The after content, often an `<img>` or `<svg>` element.
	 * @slot `handle` — The icon used inside the handle.
	 * @fires `change` — {`Event`} — Emitted when the position changes.
	 * @attribute `position` — {`number`} — The position of the divider as a percentage.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `handle` — {`HTMLElement`}
	 * @property `position` — {`number`} — The position of the divider as a percentage.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The container that wraps the before and after content.
	 * @cssPart `before` — The container that wraps the before content.
	 * @cssPart `after` — The container that wraps the after content.
	 * @cssPart `divider` — The divider that separates the before and after content.
	 * @cssPart `handle` — The handle that the user drags to expose the after content.
	 * @cssProperty `--divider-width` — The width of the dividing line.
	 * @cssProperty `--handle-size` — The size of the compare handle.
	 * @cssState `dragging` — Applied when the comparison is being dragged.
	 */
	'wa-comparison': {
		elementClass: HTMLElementTagNameMap['wa-comparison'];
		properties: {
			handle?: HTMLElementTagNameMap['wa-comparison']['handle'];
			/**
			 * @attribute `position`
			 * @reflects
			 */
			position?: HTMLElementTagNameMap['wa-comparison']['position'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-comparison']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-comparison']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-comparison']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-comparison']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-comparison']['didSSR'];
		};
		attributes: {
			/**
			 * @property `position`
			 */
			position?: number;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			change?: GlobalEventHandlersEventMap['change'];
		};
		cssProperties: {
			'--divider-width'?: string;
			'--handle-size'?: string;
		};
		cssParts: {
			base?: string;
			before?: string;
			after?: string;
			divider?: string;
			handle?: string;
		};
		cssStates: {
			dragging?: string;
		};
		slots: {
			before?: string;
			after?: string;
			handle?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTooltip extends WebAwesomeElement (<wa-tooltip>)
	 * ```
	 * ---
	 * @summary Tooltips display brief contextual information when the user hovers, focuses, or taps a target element.
	 * @slot [***default***] — The tooltip's default slot where any content should live. Interactive content should be avoided.
	 * @fires `wa-show` — {`Event`} — Emitted when the tooltip begins to show.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the tooltip has shown and all animations are complete.
	 * @fires `wa-hide` — {`Event`} — Emitted when the tooltip begins to hide.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the tooltip has hidden and all animations are complete.
	 * @attribute `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the tooltip. Note that the actual placement may vary as needed to keep the tooltip
	 * inside of the viewport.
	 * @attribute `disabled` — {`boolean`} — Disables the tooltip so it won't show when triggered.
	 * @attribute `distance` — {`number`} — The distance in pixels from which to offset the tooltip away from its target.
	 * @attribute `open` — {`boolean`} — Indicates whether or not the tooltip is open. You can use this in lieu of the show/hide methods.
	 * @attribute `skidding` — {`number`} — The distance in pixels from which to offset the tooltip along its target.
	 * @attribute `show-delay` — {`number`} — The amount of time to wait before showing the tooltip when the user mouses in.
	 * @attribute `hide-delay` — {`number`} — The amount of time to wait before hiding the tooltip when the user mouses out.
	 * @attribute `trigger` — {`string`} — Controls how the tooltip is activated. Possible options include `click`, `hover`, `focus`, and `manual`. Multiple
	 * options can be passed by separating them with a space. When manual is used, the tooltip must be activated
	 * programmatically.
	 * @attribute `without-arrow` — {`boolean`} — Removes the arrow from the tooltip.
	 * @attribute `for` — {`string | null`}
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `body` — {`HTMLElement`}
	 * @property `popup` — {`WaPopup`}
	 * @property `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the tooltip. Note that the actual placement may vary as needed to keep the tooltip
	 * inside of the viewport.
	 * @property `disabled` — {`boolean`} — Disables the tooltip so it won't show when triggered.
	 * @property `distance` — {`number`} — The distance in pixels from which to offset the tooltip away from its target.
	 * @property `open` — {`boolean`} — Indicates whether or not the tooltip is open. You can use this in lieu of the show/hide methods.
	 * @property `skidding` — {`number`} — The distance in pixels from which to offset the tooltip along its target.
	 * @property `showDelay` — {`number`} — The amount of time to wait before showing the tooltip when the user mouses in.
	 * @property `hideDelay` — {`number`} — The amount of time to wait before hiding the tooltip when the user mouses out.
	 * @property `trigger` — {`string`} — Controls how the tooltip is activated. Possible options include `click`, `hover`, `focus`, and `manual`. Multiple
	 * options can be passed by separating them with a space. When manual is used, the tooltip must be activated
	 * programmatically.
	 * @property `withoutArrow` — {`boolean`} — Removes the arrow from the tooltip.
	 * @property `for` — {`string | null`}
	 * @property `anchor` — {`null | Element`}
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper, an `<wa-popup>` element.
	 * @cssPart `base__popup` — The popup's exported `popup` part. Use this to target the tooltip's popup container.
	 * @cssPart `base__arrow` — The popup's exported `arrow` part. Use this to target the tooltip's arrow.
	 * @cssPart `body` — The tooltip's body where its content is rendered.
	 * @cssProperty `--max-width` — The maximum width of the tooltip before its content will wrap.
	 */
	'wa-tooltip': {
		elementClass: HTMLElementTagNameMap['wa-tooltip'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-tooltip']['defaultSlot'];
			body?: HTMLElementTagNameMap['wa-tooltip']['body'];
			popup?: HTMLElementTagNameMap['wa-tooltip']['popup'];
			/**
			 * @attribute `placement`
			 */
			placement?: HTMLElementTagNameMap['wa-tooltip']['placement'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-tooltip']['disabled'];
			/**
			 * @attribute `distance`
			 */
			distance?: HTMLElementTagNameMap['wa-tooltip']['distance'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-tooltip']['open'];
			/**
			 * @attribute `skidding`
			 */
			skidding?: HTMLElementTagNameMap['wa-tooltip']['skidding'];
			/**
			 * @attribute `show-delay`
			 */
			showDelay?: HTMLElementTagNameMap['wa-tooltip']['showDelay'];
			/**
			 * @attribute `hide-delay`
			 */
			hideDelay?: HTMLElementTagNameMap['wa-tooltip']['hideDelay'];
			/**
			 * @attribute `trigger`
			 */
			trigger?: HTMLElementTagNameMap['wa-tooltip']['trigger'];
			/**
			 * @attribute `without-arrow`
			 * @reflects
			 */
			withoutArrow?: HTMLElementTagNameMap['wa-tooltip']['withoutArrow'];
			/**
			 * @attribute `for`
			 */
			for?: HTMLElementTagNameMap['wa-tooltip']['for'];
			anchor?: HTMLElementTagNameMap['wa-tooltip']['anchor'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tooltip']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tooltip']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tooltip']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tooltip']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tooltip']['didSSR'];
		};
		attributes: {
			/**
			 * @property `placement`
			 */
			placement?:
				| 'top'
				| 'top-start'
				| 'top-end'
				| 'right'
				| 'right-start'
				| 'right-end'
				| 'bottom'
				| 'bottom-start'
				| 'bottom-end'
				| 'left'
				| 'left-start'
				| 'left-end';
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `distance`
			 */
			distance?: number;
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `skidding`
			 */
			skidding?: number;
			/**
			 * @property `showDelay`
			 */
			'show-delay'?: number;
			/**
			 * @property `hideDelay`
			 */
			'hide-delay'?: number;
			/**
			 * @property `trigger`
			 */
			trigger?: string;
			/**
			 * @property `withoutArrow`
			 */
			'without-arrow'?: boolean;
			/**
			 * @property `for`
			 */
			for?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
		};
		cssProperties: {
			'--max-width'?: string;
		};
		cssParts: {
			base?: string;
			base__popup?: string;
			base__arrow?: string;
			body?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaCopyButton extends WebAwesomeElement (<wa-copy-button>)
	 * ```
	 * ---
	 * @summary Copy buttons copy text to the clipboard when the user activates them. They provide built-in success and
	 * error feedback so users know the copy worked.
	 * @slot [***default***] — The trigger element. By default, a copy icon button is rendered so this is optional. If desired, you can slot in a custom element such as `<wa-button>` or `<button>`.
	 * @slot `copy-icon` — The icon to show in the default copy state. Works best with `<wa-icon>`.
	 * @slot `success-icon` — The icon to show when the content is copied. Works best with `<wa-icon>`.
	 * @slot `error-icon` — The icon to show when a copy error occurs. Works best with `<wa-icon>`.
	 * @fires `wa-copy` — {`Event`} — Emitted when the data has been copied.
	 * @fires `wa-error` — {`Event`} — Emitted when the data could not be copied.
	 * @attribute `value` — {`string`} — The text value to copy.
	 * @attribute `from` — {`string`} — An id that references an element in the same document from which data will be copied. If both this and `value` are
	 * present, this value will take precedence. By default, the target element's `textContent` will be copied. To copy an
	 * attribute, append the attribute name wrapped in square brackets, e.g. `from="el[value]"`. To copy a property,
	 * append a dot and the property name, e.g. `from="el.value"`.
	 * @attribute `disabled` — {`boolean`} — Disables the copy button.
	 * @attribute `copy-label` — {`string`} — A custom label to show in the tooltip.
	 * @attribute `success-label` — {`string`} — A custom label to show in the tooltip after copying.
	 * @attribute `error-label` — {`string`} — A custom label to show in the tooltip when a copy error occurs.
	 * @attribute `feedback-duration` — {`number`} — The length of time to show feedback before restoring the default trigger.
	 * @attribute `tooltip-placement` — {`'top' | 'right' | 'bottom' | 'left'`} — The preferred placement of the tooltip.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `copyIcon` — {`HTMLSlotElement`}
	 * @property `successIcon` — {`HTMLSlotElement`}
	 * @property `errorIcon` — {`HTMLSlotElement`}
	 * @property `tooltip` — {`WaTooltip`}
	 * @property `isCopying` — {`boolean`}
	 * @property `status` — {`'rest' | 'success' | 'error'`}
	 * @property `value` — {`string`} — The text value to copy.
	 * @property `from` — {`string`} — An id that references an element in the same document from which data will be copied. If both this and `value` are
	 * present, this value will take precedence. By default, the target element's `textContent` will be copied. To copy an
	 * attribute, append the attribute name wrapped in square brackets, e.g. `from="el[value]"`. To copy a property,
	 * append a dot and the property name, e.g. `from="el.value"`.
	 * @property `disabled` — {`boolean`} — Disables the copy button.
	 * @property `copyLabel` — {`string`} — A custom label to show in the tooltip.
	 * @property `successLabel` — {`string`} — A custom label to show in the tooltip after copying.
	 * @property `errorLabel` — {`string`} — A custom label to show in the tooltip when a copy error occurs.
	 * @property `feedbackDuration` — {`number`} — The length of time to show feedback before restoring the default trigger.
	 * @property `tooltipPlacement` — {`'top' | 'right' | 'bottom' | 'left'`} — The preferred placement of the tooltip.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `button` — The internal `<button>` element.
	 * @cssPart `copy-icon` — The container that holds the copy icon.
	 * @cssPart `success-icon` — The container that holds the success icon.
	 * @cssPart `error-icon` — The container that holds the error icon.
	 * @cssPart `tooltip__base` — The tooltip's exported `base` part.
	 * @cssPart `tooltip__base__popup` — The tooltip's exported `popup` part.
	 * @cssPart `tooltip__base__arrow` — The tooltip's exported `arrow` part.
	 * @cssPart `tooltip__body` — The tooltip's exported `body` part.
	 * @cssState `success` — Applied when the copy operation succeeds.
	 * @cssState `error` — Applied when the copy operation fails.
	 */
	'wa-copy-button': {
		elementClass: HTMLElementTagNameMap['wa-copy-button'];
		properties: {
			copyIcon?: HTMLElementTagNameMap['wa-copy-button']['copyIcon'];
			successIcon?: HTMLElementTagNameMap['wa-copy-button']['successIcon'];
			errorIcon?: HTMLElementTagNameMap['wa-copy-button']['errorIcon'];
			tooltip?: HTMLElementTagNameMap['wa-copy-button']['tooltip'];
			isCopying?: HTMLElementTagNameMap['wa-copy-button']['isCopying'];
			status?: HTMLElementTagNameMap['wa-copy-button']['status'];
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-copy-button']['value'];
			/**
			 * @attribute `from`
			 */
			from?: HTMLElementTagNameMap['wa-copy-button']['from'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-copy-button']['disabled'];
			/**
			 * @attribute `copy-label`
			 */
			copyLabel?: HTMLElementTagNameMap['wa-copy-button']['copyLabel'];
			/**
			 * @attribute `success-label`
			 */
			successLabel?: HTMLElementTagNameMap['wa-copy-button']['successLabel'];
			/**
			 * @attribute `error-label`
			 */
			errorLabel?: HTMLElementTagNameMap['wa-copy-button']['errorLabel'];
			/**
			 * @attribute `feedback-duration`
			 */
			feedbackDuration?: HTMLElementTagNameMap['wa-copy-button']['feedbackDuration'];
			/**
			 * @attribute `tooltip-placement`
			 */
			tooltipPlacement?: HTMLElementTagNameMap['wa-copy-button']['tooltipPlacement'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-copy-button']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-copy-button']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-copy-button']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-copy-button']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-copy-button']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: string;
			/**
			 * @property `from`
			 */
			from?: string;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `copyLabel`
			 */
			'copy-label'?: string;
			/**
			 * @property `successLabel`
			 */
			'success-label'?: string;
			/**
			 * @property `errorLabel`
			 */
			'error-label'?: string;
			/**
			 * @property `feedbackDuration`
			 */
			'feedback-duration'?: number;
			/**
			 * @property `tooltipPlacement`
			 */
			'tooltip-placement'?: 'top' | 'right' | 'bottom' | 'left';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-copy'?: GlobalEventHandlersEventMap['wa-copy'];
			'wa-error'?: GlobalEventHandlersEventMap['wa-error'];
		};
		cssProperties: {};
		cssParts: {
			button?: string;
			'copy-icon'?: string;
			'success-icon'?: string;
			'error-icon'?: string;
			tooltip__base?: string;
			tooltip__base__popup?: string;
			tooltip__base__arrow?: string;
			tooltip__body?: string;
		};
		cssStates: {
			success?: string;
			error?: string;
		};
		slots: {
			''?: string;
			'copy-icon'?: string;
			'success-icon'?: string;
			'error-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaDetails extends WebAwesomeElement (<wa-details>)
	 * ```
	 * ---
	 * @summary Details display a brief summary and expand to reveal additional content. Use them to progressively disclose
	 * information, group related FAQs, or hide advanced options.
	 * @slot [***default***] — The details' main content.
	 * @slot `summary` — The details' summary. Alternatively, you can use the `summary` attribute.
	 * @slot `expand-icon` — Optional expand icon to use instead of the default. Works best with `<wa-icon>`.
	 * @slot `collapse-icon` — Optional collapse icon to use instead of the default. Works best with `<wa-icon>`.
	 * @fires `wa-show` — {`Event`} — Emitted when the details opens.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the details opens and all animations are complete.
	 * @fires `wa-hide` — {`Event`} — Emitted when the details closes.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the details closes and all animations are complete.
	 * @attribute `open` — {`boolean`} — Indicates whether or not the details is open. You can toggle this attribute to show and hide the details, or you
	 * can use the `show()` and `hide()` methods and this attribute will reflect the details' open state.
	 * @attribute `summary` — {`string`} — The summary to show in the header. If you need to display HTML, use the `summary` slot instead.
	 * @attribute `name` — {`string`} — Groups related details elements. When one opens, others with the same name will close.
	 * @attribute `disabled` — {`boolean`} — Disables the details so it can't be toggled.
	 * @attribute `appearance` — {`'filled' | 'outlined' | 'filled-outlined' | 'plain'`} — The element's visual appearance.
	 * @attribute `icon-placement` — {`'start' | 'end'`} — The location of the expand/collapse icon.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `details` — {`HTMLDetailsElement`}
	 * @property `header` — {`HTMLElement`}
	 * @property `body` — {`HTMLElement`}
	 * @property `expandIconSlot` — {`HTMLSlotElement`}
	 * @property `isAnimating` — {`boolean`}
	 * @property `open` — {`boolean`} — Indicates whether or not the details is open. You can toggle this attribute to show and hide the details, or you
	 * can use the `show()` and `hide()` methods and this attribute will reflect the details' open state.
	 * @property `summary` — {`string`} — The summary to show in the header. If you need to display HTML, use the `summary` slot instead.
	 * @property `name` — {`string`} — Groups related details elements. When one opens, others with the same name will close.
	 * @property `disabled` — {`boolean`} — Disables the details so it can't be toggled.
	 * @property `appearance` — {`'filled' | 'outlined' | 'filled-outlined' | 'plain'`} — The element's visual appearance.
	 * @property `iconPlacement` — {`'start' | 'end'`} — The location of the expand/collapse icon.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The inner `<details>` element used to render the component. Styles you apply to the component are automatically applied to this part, so you usually don't need to deal with it unless you need to set the `display` property.
	 * @cssPart `header` — The header that wraps both the summary and the expand/collapse icon.
	 * @cssPart `summary` — The container that wraps the summary.
	 * @cssPart `icon` — The container that wraps the expand/collapse icons.
	 * @cssPart `content` — The details content.
	 * @cssProperty `--spacing` — The amount of space around and between the details' content. Expects a single value.
	 * @cssProperty `--show-duration` — The show duration to use when applying built-in animation classes.
	 * @cssProperty `--hide-duration` — The hide duration to use when applying built-in animation classes.
	 * @cssState `animating` — Applied when the details is animating expand/collapse.
	 */
	'wa-details': {
		elementClass: HTMLElementTagNameMap['wa-details'];
		properties: {
			details?: HTMLElementTagNameMap['wa-details']['details'];
			header?: HTMLElementTagNameMap['wa-details']['header'];
			body?: HTMLElementTagNameMap['wa-details']['body'];
			expandIconSlot?: HTMLElementTagNameMap['wa-details']['expandIconSlot'];
			isAnimating?: HTMLElementTagNameMap['wa-details']['isAnimating'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-details']['open'];
			/**
			 * @attribute `summary`
			 */
			summary?: HTMLElementTagNameMap['wa-details']['summary'];
			/**
			 * @attribute `name`
			 * @reflects
			 */
			name?: HTMLElementTagNameMap['wa-details']['name'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-details']['disabled'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-details']['appearance'];
			/**
			 * @attribute `icon-placement`
			 * @reflects
			 */
			iconPlacement?: HTMLElementTagNameMap['wa-details']['iconPlacement'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-details']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-details']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-details']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-details']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-details']['didSSR'];
		};
		attributes: {
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `summary`
			 */
			summary?: string;
			/**
			 * @property `name`
			 */
			name?: string;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `appearance`
			 */
			appearance?: 'filled' | 'outlined' | 'filled-outlined' | 'plain';
			/**
			 * @property `iconPlacement`
			 */
			'icon-placement'?: 'start' | 'end';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
		};
		cssProperties: {
			'--spacing'?: string;
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			base?: string;
			header?: string;
			summary?: string;
			icon?: string;
			content?: string;
		};
		cssStates: {
			animating?: string;
		};
		slots: {
			''?: string;
			summary?: string;
			'expand-icon'?: string;
			'collapse-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaDialog extends WebAwesomeElement (<wa-dialog>)
	 * ```
	 * ---
	 * @summary Dialogs appear above the page and require the user's immediate attention. Use them for confirmations, forms,
	 * or focused tasks that interrupt the main flow.
	 * @slot [***default***] — The dialog's main content.
	 * @slot `label` — The dialog's label. Alternatively, you can use the `label` attribute.
	 * @slot `header-actions` — Optional actions to add to the header. Works best with `<wa-button>`.
	 * @slot `footer` — The dialog's footer, usually one or more buttons representing various options.
	 * @fires `wa-show` — {`Event`} — Emitted when the dialog opens.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the dialog opens and all animations are complete.
	 * @fires `wa-hide` — {`{ source: Element }`} — Emitted when the dialog is requested to close. Calling `event.preventDefault()` will prevent the dialog from closing. You can inspect `event.detail.source` to see which element caused the dialog to close. If the source is the dialog element itself, the user has pressed [[Escape]] or the dialog has been closed programmatically. Avoid using this unless closing the dialog will result in destructive behavior such as data loss.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the dialog closes and all animations are complete.
	 * @attribute `open` — {`boolean`} — Indicates whether or not the dialog is open. Toggle this attribute to show and hide the dialog.
	 * @attribute `label` — {`string`} — The dialog's label as displayed in the header. You should always include a relevant label, as it is required for
	 * proper accessibility. If you need to display HTML, use the `label` slot instead.
	 * @attribute `without-header` — {`boolean`} — Disables the header. This will also remove the default close button.
	 * @attribute `light-dismiss` — {`boolean`} — When enabled, the dialog will be closed when the user clicks outside of it.
	 * @attribute `with-footer` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `footer` element so the server-rendered markup
	 * includes the footer before the component hydrates on the client.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `dialog` — {`HTMLDialogElement`}
	 * @property `open` — {`boolean`} — Indicates whether or not the dialog is open. Toggle this attribute to show and hide the dialog.
	 * @property `label` — {`string`} — The dialog's label as displayed in the header. You should always include a relevant label, as it is required for
	 * proper accessibility. If you need to display HTML, use the `label` slot instead.
	 * @property `withoutHeader` — {`boolean`} — Disables the header. This will also remove the default close button.
	 * @property `lightDismiss` — {`boolean`} — When enabled, the dialog will be closed when the user clicks outside of it.
	 * @property `withFooter` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `footer` element so the server-rendered markup
	 * includes the footer before the component hydrates on the client.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `dialog` — The dialog's internal `<dialog>` element.
	 * @cssPart `header` — The dialog's header. This element wraps the title and header actions.
	 * @cssPart `header-actions` — Optional actions to add to the header. Works best with `<wa-button>`.
	 * @cssPart `title` — The dialog's title.
	 * @cssPart `close-button` — The close button, a `<wa-button>`.
	 * @cssPart `close-button__base` — The close button's exported `base` part.
	 * @cssPart `body` — The dialog's body.
	 * @cssPart `footer` — The dialog's footer.
	 * @cssProperty `--spacing` — The amount of space around and between the dialog's content.
	 * @cssProperty `--width` — The preferred width of the dialog. Note that the dialog will shrink to accommodate smaller screens.
	 * @cssProperty `--backdrop-filter` — A filter to apply to the backdrop behind the dialog.
	 * @cssProperty `--show-duration` — The animation duration when showing the dialog.
	 * @cssProperty `--hide-duration` — The animation duration when hiding the dialog.
	 */
	'wa-dialog': {
		elementClass: HTMLElementTagNameMap['wa-dialog'];
		properties: {
			dialog?: HTMLElementTagNameMap['wa-dialog']['dialog'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-dialog']['open'];
			/**
			 * @attribute `label`
			 * @reflects
			 */
			label?: HTMLElementTagNameMap['wa-dialog']['label'];
			/**
			 * @attribute `without-header`
			 * @reflects
			 */
			withoutHeader?: HTMLElementTagNameMap['wa-dialog']['withoutHeader'];
			/**
			 * @attribute `light-dismiss`
			 */
			lightDismiss?: HTMLElementTagNameMap['wa-dialog']['lightDismiss'];
			/**
			 * @attribute `with-footer`
			 */
			withFooter?: HTMLElementTagNameMap['wa-dialog']['withFooter'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-dialog']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-dialog']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-dialog']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-dialog']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-dialog']['didSSR'];
		};
		attributes: {
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `withoutHeader`
			 */
			'without-header'?: boolean;
			/**
			 * @property `lightDismiss`
			 */
			'light-dismiss'?: boolean;
			/**
			 * @property `withFooter`
			 */
			'with-footer'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
		};
		cssProperties: {
			'--spacing'?: string;
			'--width'?: string;
			'--backdrop-filter'?: string;
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			dialog?: string;
			header?: string;
			'header-actions'?: string;
			title?: string;
			'close-button'?: string;
			'close-button__base'?: string;
			body?: string;
			footer?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			label?: string;
			'header-actions'?: string;
			footer?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaDivider extends WebAwesomeElement (<wa-divider>)
	 * ```
	 * ---
	 * @summary Dividers visually separate or group adjacent elements with a horizontal or vertical line. Use them to
	 * establish rhythm and hierarchy within menus, toolbars, and layouts.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — Sets the divider's orientation.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — Sets the divider's orientation.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssProperty `--color` — The color of the divider.
	 * @cssProperty `--width` — The width of the divider.
	 * @cssProperty `--spacing` — The spacing of the divider.
	 */
	'wa-divider': {
		elementClass: HTMLElementTagNameMap['wa-divider'];
		properties: {
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-divider']['orientation'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-divider']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-divider']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-divider']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-divider']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-divider']['didSSR'];
		};
		attributes: {
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--color'?: string;
			'--width'?: string;
			'--spacing'?: string;
		};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaDrawer extends WebAwesomeElement (<wa-drawer>)
	 * ```
	 * ---
	 * @summary Drawers slide in from the edge of a container to expose additional options and information without
	 * navigating away. Useful for navigation menus, filters, and secondary content.
	 * @slot [***default***] — The drawer's main content.
	 * @slot `label` — The drawer's label. Alternatively, you can use the `label` attribute.
	 * @slot `header-actions` — Optional actions to add to the header. Works best with `<wa-button>`.
	 * @slot `footer` — The drawer's footer, usually one or more buttons representing various options.
	 * @fires `wa-show` — {`Event`} — Emitted when the drawer opens.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the drawer opens and all animations are complete.
	 * @fires `wa-hide` — {`{ source: Element }`} — Emitted when the drawer is requesting to close. Calling `event.preventDefault()` will prevent the drawer from closing. You can inspect `event.detail.source` to see which element caused the drawer to close. If the source is the drawer element itself, the user has pressed [[Escape]] or the drawer has been closed programmatically. Avoid using this unless closing the drawer will result in destructive behavior such as data loss.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the drawer closes and all animations are complete.
	 * @attribute `open` — {`boolean`} — Indicates whether or not the drawer is open. Toggle this attribute to show and hide the drawer.
	 * @attribute `label` — {`string`} — The drawer's label as displayed in the header. You should always include a relevant label, as it is required for
	 * proper accessibility. If you need to display HTML, use the `label` slot instead.
	 * @attribute `placement` — {`'top' | 'end' | 'bottom' | 'start'`} — The direction from which the drawer will open.
	 * @attribute `without-header` — {`boolean`} — Disables the header. This will also remove the default close button.
	 * @attribute `light-dismiss` — {`boolean`} — When enabled, the drawer will be closed when the user clicks outside of it.
	 * @attribute `with-footer` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `footer` element so the server-rendered markup
	 * includes the footer before the component hydrates on the client.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `drawer` — {`HTMLDialogElement`}
	 * @property `open` — {`boolean`} — Indicates whether or not the drawer is open. Toggle this attribute to show and hide the drawer.
	 * @property `label` — {`string`} — The drawer's label as displayed in the header. You should always include a relevant label, as it is required for
	 * proper accessibility. If you need to display HTML, use the `label` slot instead.
	 * @property `placement` — {`'top' | 'end' | 'bottom' | 'start'`} — The direction from which the drawer will open.
	 * @property `withoutHeader` — {`boolean`} — Disables the header. This will also remove the default close button.
	 * @property `lightDismiss` — {`boolean`} — When enabled, the drawer will be closed when the user clicks outside of it.
	 * @property `withFooter` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `footer` element so the server-rendered markup
	 * includes the footer before the component hydrates on the client.
	 * @property `modal` — Exposes the internal modal utility that controls focus trapping. To temporarily disable focus trapping and allow third-party modals spawned from an active Shoelace modal, call `modal.activateExternal()` when the third-party modal opens. Upon closing, call `modal.deactivateExternal()` to restore Shoelace's focus trapping.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `dialog` — The drawer's internal `<dialog>` element.
	 * @cssPart `header` — The drawer's header. This element wraps the title and header actions.
	 * @cssPart `header-actions` — Optional actions to add to the header. Works best with `<wa-button>`.
	 * @cssPart `title` — The drawer's title.
	 * @cssPart `close-button` — The close button, a `<wa-button>`.
	 * @cssPart `close-button__base` — The close button's exported `base` part.
	 * @cssPart `body` — The drawer's body.
	 * @cssPart `footer` — The drawer's footer.
	 * @cssProperty `--spacing` — The amount of space around and between the drawer's content.
	 * @cssProperty `--size` — The preferred size of the drawer. This will be applied to the drawer's width or height depending on its `placement`. Note that the drawer will shrink to accommodate smaller screens.
	 * @cssProperty `--backdrop-filter` — A filter to apply to the backdrop behind the drawer.
	 * @cssProperty `--show-duration` — The animation duration when showing the drawer.
	 * @cssProperty `--hide-duration` — The animation duration when hiding the drawer.
	 */
	'wa-drawer': {
		elementClass: HTMLElementTagNameMap['wa-drawer'];
		properties: {
			drawer?: HTMLElementTagNameMap['wa-drawer']['drawer'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-drawer']['open'];
			/**
			 * @attribute `label`
			 * @reflects
			 */
			label?: HTMLElementTagNameMap['wa-drawer']['label'];
			/**
			 * @attribute `placement`
			 * @reflects
			 */
			placement?: HTMLElementTagNameMap['wa-drawer']['placement'];
			/**
			 * @attribute `without-header`
			 * @reflects
			 */
			withoutHeader?: HTMLElementTagNameMap['wa-drawer']['withoutHeader'];
			/**
			 * @attribute `light-dismiss`
			 */
			lightDismiss?: HTMLElementTagNameMap['wa-drawer']['lightDismiss'];
			/**
			 * @attribute `with-footer`
			 */
			withFooter?: HTMLElementTagNameMap['wa-drawer']['withFooter'];
			modal?: HTMLElementTagNameMap['wa-drawer']['modal'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-drawer']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-drawer']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-drawer']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-drawer']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-drawer']['didSSR'];
		};
		attributes: {
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `placement`
			 */
			placement?: 'top' | 'end' | 'bottom' | 'start';
			/**
			 * @property `withoutHeader`
			 */
			'without-header'?: boolean;
			/**
			 * @property `lightDismiss`
			 */
			'light-dismiss'?: boolean;
			/**
			 * @property `withFooter`
			 */
			'with-footer'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
		};
		cssProperties: {
			'--spacing'?: string;
			'--size'?: string;
			'--backdrop-filter'?: string;
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			dialog?: string;
			header?: string;
			'header-actions'?: string;
			title?: string;
			'close-button'?: string;
			'close-button__base'?: string;
			body?: string;
			footer?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			label?: string;
			'header-actions'?: string;
			footer?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaDropdownItem extends WebAwesomeElement (<wa-dropdown-item>)
	 * ```
	 * ---
	 * @summary Dropdown items represent selectable entries within a dropdown menu, including standard actions, checkable
	 * items, and submenu triggers.
	 * @slot [***default***] — The dropdown item's label.
	 * @slot `icon` — An optional icon to display before the label.
	 * @slot `details` — Additional content or details to display after the label.
	 * @slot `submenu` — Submenu items, typically `<wa-dropdown-item>` elements, to create a nested menu.
	 * @fires `blur` — {`Event`} — Emitted when the dropdown item loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the dropdown item gains focus.
	 * @attribute `variant` — {`'danger' | 'default'`} — The type of menu item to render.
	 * @attribute `value` — {`string`} — An optional value for the menu item. This is useful for determining which item was selected when listening to the
	 * dropdown's `wa-select` event.
	 * @attribute `type` — {`'normal' | 'checkbox'`} — Set to `checkbox` to make the item a checkbox.
	 * @attribute `checked` — {`boolean`} — Set to true to check the dropdown item. Only valid when `type` is `checkbox`.
	 * @attribute `disabled` — {`boolean`} — Disables the dropdown item.
	 * @attribute `submenuOpen` — {`boolean`} — Whether the submenu is currently open.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `submenuElement` — {`HTMLDivElement`}
	 * @property `variant` — {`'danger' | 'default'`} — The type of menu item to render.
	 * @property `value` — {`string`} — An optional value for the menu item. This is useful for determining which item was selected when listening to the
	 * dropdown's `wa-select` event.
	 * @property `type` — {`'normal' | 'checkbox'`} — Set to `checkbox` to make the item a checkbox.
	 * @property `checked` — {`boolean`} — Set to true to check the dropdown item. Only valid when `type` is `checkbox`.
	 * @property `disabled` — {`boolean`} — Disables the dropdown item.
	 * @property `submenuOpen` — {`boolean`} — Whether the submenu is currently open.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `checkmark` — The checkmark icon (a `<wa-icon>` element) when the item is a checkbox.
	 * @cssPart `icon` — The container for the icon slot.
	 * @cssPart `label` — The container for the label slot.
	 * @cssPart `details` — The container for the details slot.
	 * @cssPart `submenu-icon` — The submenu indicator icon (a `<wa-icon>` element).
	 * @cssPart `submenu` — The submenu container.
	 */
	'wa-dropdown-item': {
		elementClass: HTMLElementTagNameMap['wa-dropdown-item'];
		properties: {
			submenuElement?: HTMLElementTagNameMap['wa-dropdown-item']['submenuElement'];
			/**
			 * @attribute `variant`
			 * @reflects
			 */
			variant?: HTMLElementTagNameMap['wa-dropdown-item']['variant'];
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-dropdown-item']['value'];
			/**
			 * @attribute `type`
			 * @reflects
			 */
			type?: HTMLElementTagNameMap['wa-dropdown-item']['type'];
			/**
			 * @attribute `checked`
			 */
			checked?: HTMLElementTagNameMap['wa-dropdown-item']['checked'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-dropdown-item']['disabled'];
			/**
			 * @attribute `submenuOpen`
			 * @reflects
			 */
			submenuOpen?: HTMLElementTagNameMap['wa-dropdown-item']['submenuOpen'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-dropdown-item']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-dropdown-item']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-dropdown-item']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-dropdown-item']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-dropdown-item']['didSSR'];
		};
		attributes: {
			/**
			 * @property `variant`
			 */
			variant?: 'danger' | 'default';
			/**
			 * @property `value`
			 */
			value?: string;
			/**
			 * @property `type`
			 */
			type?: 'normal' | 'checkbox';
			/**
			 * @property `checked`
			 */
			checked?: boolean;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `submenuOpen`
			 */
			submenuOpen?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
		};
		cssProperties: {};
		cssParts: {
			checkmark?: string;
			icon?: string;
			label?: string;
			details?: string;
			'submenu-icon'?: string;
			submenu?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			icon?: string;
			details?: string;
			submenu?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaDropdown extends WebAwesomeElement (<wa-dropdown>)
	 * ```
	 * ---
	 * @summary Dropdowns display a list of options triggered by a button or other element. They support keyboard
	 * navigation, submenus, and checkable items for building menus and context actions.
	 * @slot [***default***] — The dropdown's items, typically `<wa-dropdown-item>` elements.
	 * @slot `trigger` — The element that triggers the dropdown, such as a `<wa-button>` or `<button>`.
	 * @fires `wa-show` — {`Event`} — Emitted when the dropdown is about to show.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the dropdown has been shown.
	 * @fires `wa-hide` — {`Event`} — Emitted when the dropdown is about to hide.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the dropdown has been hidden.
	 * @fires `wa-select` — {`Event`} — Emitted when an item in the dropdown is selected.
	 * @attribute `open` — {`boolean`} — Opens or closes the dropdown.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The dropdown's size.
	 * @attribute `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The placement of the dropdown menu in reference to the trigger. The menu will shift to a more optimal location if
	 * the preferred placement doesn't have enough room.
	 * @attribute `distance` — {`number`} — The distance of the dropdown menu from its trigger.
	 * @attribute `skidding` — {`number`} — The offset of the dropdown menu along its trigger.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `open` — {`boolean`} — Opens or closes the dropdown.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The dropdown's size.
	 * @property `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The placement of the dropdown menu in reference to the trigger. The menu will shift to a more optimal location if
	 * the preferred placement doesn't have enough room.
	 * @property `distance` — {`number`} — The distance of the dropdown menu from its trigger.
	 * @property `skidding` — {`number`} — The offset of the dropdown menu along its trigger.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's host element.
	 * @cssPart `menu` — The dropdown menu container.
	 * @cssProperty `--show-duration` — The duration of the show animation.
	 * @cssProperty `--hide-duration` — The duration of the hide animation.
	 */
	'wa-dropdown': {
		elementClass: HTMLElementTagNameMap['wa-dropdown'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-dropdown']['defaultSlot'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-dropdown']['open'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-dropdown']['size'];
			/**
			 * @attribute `placement`
			 * @reflects
			 */
			placement?: HTMLElementTagNameMap['wa-dropdown']['placement'];
			/**
			 * @attribute `distance`
			 */
			distance?: HTMLElementTagNameMap['wa-dropdown']['distance'];
			/**
			 * @attribute `skidding`
			 */
			skidding?: HTMLElementTagNameMap['wa-dropdown']['skidding'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-dropdown']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-dropdown']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-dropdown']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-dropdown']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-dropdown']['didSSR'];
		};
		attributes: {
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `placement`
			 */
			placement?:
				| 'top'
				| 'top-start'
				| 'top-end'
				| 'bottom'
				| 'bottom-start'
				| 'bottom-end'
				| 'right'
				| 'right-start'
				| 'right-end'
				| 'left'
				| 'left-start'
				| 'left-end';
			/**
			 * @property `distance`
			 */
			distance?: number;
			/**
			 * @property `skidding`
			 */
			skidding?: number;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
			'wa-select'?: GlobalEventHandlersEventMap['wa-select'];
		};
		cssProperties: {
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			base?: string;
			menu?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			trigger?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaFormatBytes extends WebAwesomeElement (<wa-format-bytes>)
	 * ```
	 * ---
	 * @summary Formats a number of bytes as a human-readable string with the appropriate unit, such as kB, MB, or GB.
	 * Supports both byte and bit units with configurable locale.
	 * @attribute `value` — {`number`} — The number to format in bytes.
	 * @attribute `unit` — {`'byte' | 'bit'`} — The type of unit to display.
	 * @attribute `display` — {`'long' | 'short' | 'narrow'`} — Determines how to display the result, e.g. "100 bytes", "100 b", or "100b".
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `value` — {`number`} — The number to format in bytes.
	 * @property `unit` — {`'byte' | 'bit'`} — The type of unit to display.
	 * @property `display` — {`'long' | 'short' | 'narrow'`} — Determines how to display the result, e.g. "100 bytes", "100 b", or "100b".
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-format-bytes': {
		elementClass: HTMLElementTagNameMap['wa-format-bytes'];
		properties: {
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-format-bytes']['value'];
			/**
			 * @attribute `unit`
			 */
			unit?: HTMLElementTagNameMap['wa-format-bytes']['unit'];
			/**
			 * @attribute `display`
			 */
			display?: HTMLElementTagNameMap['wa-format-bytes']['display'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-format-bytes']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-format-bytes']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-format-bytes']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-format-bytes']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-format-bytes']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: number;
			/**
			 * @property `unit`
			 */
			unit?: 'byte' | 'bit';
			/**
			 * @property `display`
			 */
			display?: 'long' | 'short' | 'narrow';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaFormatDate extends WebAwesomeElement (<wa-format-date>)
	 * ```
	 * ---
	 * @summary Formats a date or time for display using the specified locale and options. Powered by the
	 * Intl.DateTimeFormat API for consistent, localized output.
	 * @attribute `date` — {`Date | string`} — The date/time to format. If not set, the current date and time will be used. When passing a string, it's strongly
	 * recommended to use the ISO 8601 format to ensure timezones are handled correctly. To convert a date to this format
	 * in JavaScript, use [`date.toISOString()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString).
	 * @attribute `weekday` — {`'narrow' | 'short' | 'long'`} — The format for displaying the weekday.
	 * @attribute `era` — {`'narrow' | 'short' | 'long'`} — The format for displaying the era.
	 * @attribute `year` — {`'numeric' | '2-digit'`} — The format for displaying the year.
	 * @attribute `month` — {`'numeric' | '2-digit' | 'narrow' | 'short' | 'long'`} — The format for displaying the month.
	 * @attribute `day` — {`'numeric' | '2-digit'`} — The format for displaying the day.
	 * @attribute `hour` — {`'numeric' | '2-digit'`} — The format for displaying the hour.
	 * @attribute `minute` — {`'numeric' | '2-digit'`} — The format for displaying the minute.
	 * @attribute `second` — {`'numeric' | '2-digit'`} — The format for displaying the second.
	 * @attribute `time-zone-name` — {`'short' | 'long'`} — The format for displaying the time.
	 * @attribute `time-zone` — {`string`} — The time zone to express the time in.
	 * @attribute `hour-format` — {`'auto' | '12' | '24'`} — The format for displaying the hour.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `date` — {`Date | string`} — The date/time to format. If not set, the current date and time will be used. When passing a string, it's strongly
	 * recommended to use the ISO 8601 format to ensure timezones are handled correctly. To convert a date to this format
	 * in JavaScript, use [`date.toISOString()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString).
	 * @property `weekday` — {`'narrow' | 'short' | 'long'`} — The format for displaying the weekday.
	 * @property `era` — {`'narrow' | 'short' | 'long'`} — The format for displaying the era.
	 * @property `year` — {`'numeric' | '2-digit'`} — The format for displaying the year.
	 * @property `month` — {`'numeric' | '2-digit' | 'narrow' | 'short' | 'long'`} — The format for displaying the month.
	 * @property `day` — {`'numeric' | '2-digit'`} — The format for displaying the day.
	 * @property `hour` — {`'numeric' | '2-digit'`} — The format for displaying the hour.
	 * @property `minute` — {`'numeric' | '2-digit'`} — The format for displaying the minute.
	 * @property `second` — {`'numeric' | '2-digit'`} — The format for displaying the second.
	 * @property `timeZoneName` — {`'short' | 'long'`} — The format for displaying the time.
	 * @property `timeZone` — {`string`} — The time zone to express the time in.
	 * @property `hourFormat` — {`'auto' | '12' | '24'`} — The format for displaying the hour.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-format-date': {
		elementClass: HTMLElementTagNameMap['wa-format-date'];
		properties: {
			/**
			 * @attribute `date`
			 */
			date?: HTMLElementTagNameMap['wa-format-date']['date'];
			/**
			 * @attribute `weekday`
			 */
			weekday?: HTMLElementTagNameMap['wa-format-date']['weekday'];
			/**
			 * @attribute `era`
			 */
			era?: HTMLElementTagNameMap['wa-format-date']['era'];
			/**
			 * @attribute `year`
			 */
			year?: HTMLElementTagNameMap['wa-format-date']['year'];
			/**
			 * @attribute `month`
			 */
			month?: HTMLElementTagNameMap['wa-format-date']['month'];
			/**
			 * @attribute `day`
			 */
			day?: HTMLElementTagNameMap['wa-format-date']['day'];
			/**
			 * @attribute `hour`
			 */
			hour?: HTMLElementTagNameMap['wa-format-date']['hour'];
			/**
			 * @attribute `minute`
			 */
			minute?: HTMLElementTagNameMap['wa-format-date']['minute'];
			/**
			 * @attribute `second`
			 */
			second?: HTMLElementTagNameMap['wa-format-date']['second'];
			/**
			 * @attribute `time-zone-name`
			 */
			timeZoneName?: HTMLElementTagNameMap['wa-format-date']['timeZoneName'];
			/**
			 * @attribute `time-zone`
			 */
			timeZone?: HTMLElementTagNameMap['wa-format-date']['timeZone'];
			/**
			 * @attribute `hour-format`
			 */
			hourFormat?: HTMLElementTagNameMap['wa-format-date']['hourFormat'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-format-date']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-format-date']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-format-date']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-format-date']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-format-date']['didSSR'];
		};
		attributes: {
			/**
			 * @property `date`
			 */
			date?: unknown | string;
			/**
			 * @property `weekday`
			 */
			weekday?: 'narrow' | 'short' | 'long';
			/**
			 * @property `era`
			 */
			era?: 'narrow' | 'short' | 'long';
			/**
			 * @property `year`
			 */
			year?: 'numeric' | '2-digit';
			/**
			 * @property `month`
			 */
			month?: 'numeric' | '2-digit' | 'narrow' | 'short' | 'long';
			/**
			 * @property `day`
			 */
			day?: 'numeric' | '2-digit';
			/**
			 * @property `hour`
			 */
			hour?: 'numeric' | '2-digit';
			/**
			 * @property `minute`
			 */
			minute?: 'numeric' | '2-digit';
			/**
			 * @property `second`
			 */
			second?: 'numeric' | '2-digit';
			/**
			 * @property `timeZoneName`
			 */
			'time-zone-name'?: 'short' | 'long';
			/**
			 * @property `timeZone`
			 */
			'time-zone'?: string;
			/**
			 * @property `hourFormat`
			 */
			'hour-format'?: 'auto' | '12' | '24';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaFormatNumber extends WebAwesomeElement (<wa-format-number>)
	 * ```
	 * ---
	 * @summary Formats a number for display using the specified locale and options, including currency, percent, and unit
	 * styles. Powered by the Intl.NumberFormat API.
	 * @attribute `value` — {`number`} — The number to format.
	 * @attribute `type` — {`'currency' | 'decimal' | 'percent'`} — The formatting style to use.
	 * @attribute `without-grouping` — {`boolean`} — Turns off grouping separators.
	 * @attribute `currency` — {`string`} — The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code to use when formatting.
	 * @attribute `currency-display` — {`'symbol' | 'narrowSymbol' | 'code' | 'name'`} — How to display the currency.
	 * @attribute `minimum-integer-digits` — {`number`} — The minimum number of integer digits to use. Possible values are 1-21.
	 * @attribute `minimum-fraction-digits` — {`number`} — The minimum number of fraction digits to use. Possible values are 0-100.
	 * @attribute `maximum-fraction-digits` — {`number`} — The maximum number of fraction digits to use. Possible values are 0-100.
	 * @attribute `minimum-significant-digits` — {`number`} — The minimum number of significant digits to use. Possible values are 1-21.
	 * @attribute `maximum-significant-digits` — {`number`} — The maximum number of significant digits to use,. Possible values are 1-21.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `value` — {`number`} — The number to format.
	 * @property `type` — {`'currency' | 'decimal' | 'percent'`} — The formatting style to use.
	 * @property `withoutGrouping` — {`boolean`} — Turns off grouping separators.
	 * @property `currency` — {`string`} — The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code to use when formatting.
	 * @property `currencyDisplay` — {`'symbol' | 'narrowSymbol' | 'code' | 'name'`} — How to display the currency.
	 * @property `minimumIntegerDigits` — {`number`} — The minimum number of integer digits to use. Possible values are 1-21.
	 * @property `minimumFractionDigits` — {`number`} — The minimum number of fraction digits to use. Possible values are 0-100.
	 * @property `maximumFractionDigits` — {`number`} — The maximum number of fraction digits to use. Possible values are 0-100.
	 * @property `minimumSignificantDigits` — {`number`} — The minimum number of significant digits to use. Possible values are 1-21.
	 * @property `maximumSignificantDigits` — {`number`} — The maximum number of significant digits to use,. Possible values are 1-21.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-format-number': {
		elementClass: HTMLElementTagNameMap['wa-format-number'];
		properties: {
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-format-number']['value'];
			/**
			 * @attribute `type`
			 */
			type?: HTMLElementTagNameMap['wa-format-number']['type'];
			/**
			 * @attribute `without-grouping`
			 */
			withoutGrouping?: HTMLElementTagNameMap['wa-format-number']['withoutGrouping'];
			/**
			 * @attribute `currency`
			 */
			currency?: HTMLElementTagNameMap['wa-format-number']['currency'];
			/**
			 * @attribute `currency-display`
			 */
			currencyDisplay?: HTMLElementTagNameMap['wa-format-number']['currencyDisplay'];
			/**
			 * @attribute `minimum-integer-digits`
			 */
			minimumIntegerDigits?: HTMLElementTagNameMap['wa-format-number']['minimumIntegerDigits'];
			/**
			 * @attribute `minimum-fraction-digits`
			 */
			minimumFractionDigits?: HTMLElementTagNameMap['wa-format-number']['minimumFractionDigits'];
			/**
			 * @attribute `maximum-fraction-digits`
			 */
			maximumFractionDigits?: HTMLElementTagNameMap['wa-format-number']['maximumFractionDigits'];
			/**
			 * @attribute `minimum-significant-digits`
			 */
			minimumSignificantDigits?: HTMLElementTagNameMap['wa-format-number']['minimumSignificantDigits'];
			/**
			 * @attribute `maximum-significant-digits`
			 */
			maximumSignificantDigits?: HTMLElementTagNameMap['wa-format-number']['maximumSignificantDigits'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-format-number']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-format-number']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-format-number']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-format-number']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-format-number']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: number;
			/**
			 * @property `type`
			 */
			type?: 'currency' | 'decimal' | 'percent';
			/**
			 * @property `withoutGrouping`
			 */
			'without-grouping'?: boolean;
			/**
			 * @property `currency`
			 */
			currency?: string;
			/**
			 * @property `currencyDisplay`
			 */
			'currency-display'?: 'symbol' | 'narrowSymbol' | 'code' | 'name';
			/**
			 * @property `minimumIntegerDigits`
			 */
			'minimum-integer-digits'?: number;
			/**
			 * @property `minimumFractionDigits`
			 */
			'minimum-fraction-digits'?: number;
			/**
			 * @property `maximumFractionDigits`
			 */
			'maximum-fraction-digits'?: number;
			/**
			 * @property `minimumSignificantDigits`
			 */
			'minimum-significant-digits'?: number;
			/**
			 * @property `maximumSignificantDigits`
			 */
			'maximum-significant-digits'?: number;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaInclude extends WebAwesomeElement (<wa-include>)
	 * ```
	 * ---
	 * @summary Fetches an external HTML file and embeds its contents inline on the page. Useful for reusing shared markup
	 * like headers, footers, and partials across multiple pages.
	 * @fires `wa-load` — {`Event`} — Emitted when the included file is loaded.
	 * @fires `wa-include-error` — {`{ status: number }`} — Emitted when the included file fails to load due to an error.
	 * @attribute `src` — {`string`} — The location of the HTML file to include. Be sure you trust the content you are including as it will be executed as
	 * code and can result in XSS attacks.
	 * @attribute `mode` — {`'cors' | 'no-cors' | 'same-origin'`} — The fetch mode to use.
	 * @attribute `allow-scripts` — {`boolean`} — Allows included scripts to be executed. Be sure you trust the content you are including as it will be executed as
	 * code and can result in XSS attacks.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `src` — {`string`} — The location of the HTML file to include. Be sure you trust the content you are including as it will be executed as
	 * code and can result in XSS attacks.
	 * @property `mode` — {`'cors' | 'no-cors' | 'same-origin'`} — The fetch mode to use.
	 * @property `allowScripts` — {`boolean`} — Allows included scripts to be executed. Be sure you trust the content you are including as it will be executed as
	 * code and can result in XSS attacks.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-include': {
		elementClass: HTMLElementTagNameMap['wa-include'];
		properties: {
			/**
			 * @attribute `src`
			 */
			src?: HTMLElementTagNameMap['wa-include']['src'];
			/**
			 * @attribute `mode`
			 */
			mode?: HTMLElementTagNameMap['wa-include']['mode'];
			/**
			 * @attribute `allow-scripts`
			 */
			allowScripts?: HTMLElementTagNameMap['wa-include']['allowScripts'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-include']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-include']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-include']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-include']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-include']['didSSR'];
		};
		attributes: {
			/**
			 * @property `src`
			 */
			src?: string;
			/**
			 * @property `mode`
			 */
			mode?: 'cors' | 'no-cors' | 'same-origin';
			/**
			 * @property `allowScripts`
			 */
			'allow-scripts'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-load'?: GlobalEventHandlersEventMap['wa-load'];
			'wa-include-error'?: GlobalEventHandlersEventMap['wa-include-error'];
		};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaIntersectionObserver extends WebAwesomeElement (<wa-intersection-observer>)
	 * ```
	 * ---
	 * @summary Tracks immediate child elements and fires events as they move in and out of view. Useful for lazy loading,
	 * scroll-triggered animations, and viewport-aware interactions.
	 * @slot [***default***] — Elements to track. Only immediate children of the host are monitored.
	 * @fires `wa-intersect` — {`{ entry: IntersectionObserverEntry }`} — Fired when a tracked element begins or ceases intersecting.
	 * @attribute `root` — {`string | null`} — Element ID to define the viewport boundaries for tracked targets.
	 * @attribute `root-margin` — {`string`} — Offset space around the root boundary. Accepts values like CSS margin syntax.
	 * @attribute `threshold` — {`string`} — One or more space-separated values representing visibility percentages that trigger the observer callback.
	 * @attribute `intersect-class` — {`string`} — CSS class applied to elements during intersection. Automatically removed when elements leave
	 * the viewport, enabling pure CSS styling based on visibility state.
	 * @attribute `once` — {`boolean`} — If enabled, observation ceases after initial intersection.
	 * @attribute `disabled` — {`boolean`} — Deactivates the intersection observer functionality.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `root` — {`string | null`} — Element ID to define the viewport boundaries for tracked targets.
	 * @property `rootMargin` — {`string`} — Offset space around the root boundary. Accepts values like CSS margin syntax.
	 * @property `threshold` — {`string`} — One or more space-separated values representing visibility percentages that trigger the observer callback.
	 * @property `intersectClass` — {`string`} — CSS class applied to elements during intersection. Automatically removed when elements leave
	 * the viewport, enabling pure CSS styling based on visibility state.
	 * @property `once` — {`boolean`} — If enabled, observation ceases after initial intersection.
	 * @property `disabled` — {`boolean`} — Deactivates the intersection observer functionality.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-intersection-observer': {
		elementClass: HTMLElementTagNameMap['wa-intersection-observer'];
		properties: {
			/**
			 * @attribute `root`
			 */
			root?: HTMLElementTagNameMap['wa-intersection-observer']['root'];
			/**
			 * @attribute `root-margin`
			 */
			rootMargin?: HTMLElementTagNameMap['wa-intersection-observer']['rootMargin'];
			/**
			 * @attribute `threshold`
			 */
			threshold?: HTMLElementTagNameMap['wa-intersection-observer']['threshold'];
			/**
			 * @attribute `intersect-class`
			 */
			intersectClass?: HTMLElementTagNameMap['wa-intersection-observer']['intersectClass'];
			/**
			 * @attribute `once`
			 * @reflects
			 */
			once?: HTMLElementTagNameMap['wa-intersection-observer']['once'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-intersection-observer']['disabled'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-intersection-observer']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-intersection-observer']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-intersection-observer']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-intersection-observer']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-intersection-observer']['didSSR'];
		};
		attributes: {
			/**
			 * @property `root`
			 */
			root?: string | null;
			/**
			 * @property `rootMargin`
			 */
			'root-margin'?: string;
			/**
			 * @property `threshold`
			 */
			threshold?: string;
			/**
			 * @property `intersectClass`
			 */
			'intersect-class'?: string;
			/**
			 * @property `once`
			 */
			once?: boolean;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-intersect'?: GlobalEventHandlersEventMap['wa-intersect'];
		};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaMutationObserver extends WebAwesomeElement (<wa-mutation-observer>)
	 * ```
	 * ---
	 * @summary Mutation observers watch for changes to an element's DOM tree and emit an event when they occur. Provides a
	 * thin, declarative interface to the browser's MutationObserver API.
	 * @slot [***default***] — The content to watch for mutations.
	 * @fires `wa-mutation` — {`{ mutationList: MutationRecord[] }`} — Emitted when a mutation occurs.
	 * @attribute `attr` — {`string`} — Watches for changes to attributes. To watch only specific attributes, separate them by a space, e.g.
	 * `attr="class id title"`. To watch all attributes, use `*`.
	 * @attribute `attr-old-value` — {`boolean`} — Indicates whether or not the attribute's previous value should be recorded when monitoring changes.
	 * @attribute `char-data` — {`boolean`} — Watches for changes to the character data contained within the node.
	 * @attribute `char-data-old-value` — {`boolean`} — Indicates whether or not the previous value of the node's text should be recorded.
	 * @attribute `child-list` — {`boolean`} — Watches for the addition or removal of new child nodes.
	 * @attribute `disabled` — {`boolean`} — Disables the observer.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `attr` — {`string`} — Watches for changes to attributes. To watch only specific attributes, separate them by a space, e.g.
	 * `attr="class id title"`. To watch all attributes, use `*`.
	 * @property `attrOldValue` — {`boolean`} — Indicates whether or not the attribute's previous value should be recorded when monitoring changes.
	 * @property `charData` — {`boolean`} — Watches for changes to the character data contained within the node.
	 * @property `charDataOldValue` — {`boolean`} — Indicates whether or not the previous value of the node's text should be recorded.
	 * @property `childList` — {`boolean`} — Watches for the addition or removal of new child nodes.
	 * @property `disabled` — {`boolean`} — Disables the observer.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-mutation-observer': {
		elementClass: HTMLElementTagNameMap['wa-mutation-observer'];
		properties: {
			/**
			 * @attribute `attr`
			 * @reflects
			 */
			attr?: HTMLElementTagNameMap['wa-mutation-observer']['attr'];
			/**
			 * @attribute `attr-old-value`
			 * @reflects
			 */
			attrOldValue?: HTMLElementTagNameMap['wa-mutation-observer']['attrOldValue'];
			/**
			 * @attribute `char-data`
			 * @reflects
			 */
			charData?: HTMLElementTagNameMap['wa-mutation-observer']['charData'];
			/**
			 * @attribute `char-data-old-value`
			 * @reflects
			 */
			charDataOldValue?: HTMLElementTagNameMap['wa-mutation-observer']['charDataOldValue'];
			/**
			 * @attribute `child-list`
			 * @reflects
			 */
			childList?: HTMLElementTagNameMap['wa-mutation-observer']['childList'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-mutation-observer']['disabled'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-mutation-observer']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-mutation-observer']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-mutation-observer']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-mutation-observer']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-mutation-observer']['didSSR'];
		};
		attributes: {
			/**
			 * @property `attr`
			 */
			attr?: string;
			/**
			 * @property `attrOldValue`
			 */
			'attr-old-value'?: boolean;
			/**
			 * @property `charData`
			 */
			'char-data'?: boolean;
			/**
			 * @property `charDataOldValue`
			 */
			'char-data-old-value'?: boolean;
			/**
			 * @property `childList`
			 */
			'child-list'?: boolean;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-mutation'?: GlobalEventHandlersEventMap['wa-mutation'];
		};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaNumberInput extends WebAwesomeFormAssociatedElement (<wa-number-input>)
	 * ```
	 * ---
	 * @summary Number inputs let users enter and edit numeric values, with optional stepper buttons for incrementing and
	 * decrementing. Use them for quantities, measurements, and other numeric form fields.
	 * @slot `label` — The input's label. Alternatively, you can use the `label` attribute.
	 * @slot `start` — An element, such as `<wa-icon>`, placed at the start of the input control.
	 * @slot `end` — An element, such as `<wa-icon>`, placed at the end of the input control (before steppers).
	 * @slot `increment-icon` — An icon to use in lieu of the default increment icon.
	 * @slot `decrement-icon` — An icon to use in lieu of the default decrement icon.
	 * @slot `hint` — Text that describes how to use the input. Alternatively, you can use the `hint` attribute.
	 * @fires `input` — {`InputEvent`} — Emitted when the control receives input.
	 * @fires `change` — {`Event`} — Emitted when an alteration to the control's value is committed by the user.
	 * @fires `blur` — {`Event`} — Emitted when the control loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the control gains focus.
	 * @fires `beforeinput` — {`Event`} — Emitted before the value changes. Can be cancelled with `event.preventDefault()` to prevent the value from changing.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `title` — {`string`}
	 * @attribute `value` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The input's size.
	 * @attribute `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The input's visual appearance.
	 * @attribute `pill` — {`boolean`} — Draws a pill-style input with rounded edges.
	 * @attribute `label` — {`string`} — The input's label. If you need to display HTML, use the `label` slot instead.
	 * @attribute `hint` — {`string`} — The input's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `placeholder` — {`string`} — Placeholder text to show as a hint when the input is empty.
	 * @attribute `readonly` — {`boolean`} — Makes the input readonly.
	 * @attribute `required` — {`boolean`} — Makes the input a required field.
	 * @attribute `min` — {`number`} — The input's minimum value.
	 * @attribute `max` — {`number`} — The input's maximum value.
	 * @attribute `step` — {`number | 'any'`} — Specifies the granularity that the value must adhere to, or the special value `any` which means no stepping is
	 * implied, allowing any numeric value.
	 * @attribute `without-steppers` — {`boolean`} — Hides the increment/decrement stepper buttons.
	 * @attribute `autocomplete` — {`string`} — Specifies what permission the browser has to provide assistance in filling out form field values. Refer to
	 * [this page on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) for available values.
	 * @attribute `autofocus` — {`boolean`} — Indicates that the input should receive focus on page load.
	 * @attribute `enterkeyhint` — {`'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send'`} — Used to customize the label or icon of the Enter key on virtual keyboards.
	 * @attribute `inputmode` — {`'numeric' | 'decimal'`} — Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
	 * keyboard on supportive devices.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `name` — {`string | null`} — The name of the input, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `disabled` — {`boolean`} — Disables the form control. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `title` — {`string`}
	 * @property `value` — The current value of the input, submitted as a name/value pair with form data.
	 * @property `defaultValue` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The input's size.
	 * @property `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The input's visual appearance.
	 * @property `pill` — {`boolean`} — Draws a pill-style input with rounded edges.
	 * @property `label` — {`string`} — The input's label. If you need to display HTML, use the `label` slot instead.
	 * @property `hint` — {`string`} — The input's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `placeholder` — {`string`} — Placeholder text to show as a hint when the input is empty.
	 * @property `readonly` — {`boolean`} — Makes the input readonly.
	 * @property `required` — {`boolean`} — Makes the input a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `min` — {`number`} — The input's minimum value.
	 * @property `max` — {`number`} — The input's maximum value.
	 * @property `step` — {`number | 'any'`} — Specifies the granularity that the value must adhere to, or the special value `any` which means no stepping is
	 * implied, allowing any numeric value.
	 * @property `withoutSteppers` — {`boolean`} — Hides the increment/decrement stepper buttons.
	 * @property `autocomplete` — {`string`} — Specifies what permission the browser has to provide assistance in filling out form field values. Refer to
	 * [this page on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) for available values.
	 * @property `autofocus` — {`boolean`} — Indicates that the input should receive focus on page load.
	 * @property `enterkeyhint` — {`'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send'`} — Used to customize the label or icon of the Enter key on virtual keyboards.
	 * @property `inputmode` — {`'numeric' | 'decimal'`} — Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
	 * keyboard on supportive devices.
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `name` — {`string | null`} — The name of the input, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `disabled` — {`boolean`} — Disables the form control. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `label` — The label element.
	 * @cssPart `form-control-label` — Alias for the label element.
	 * @cssPart `hint` — The hint element.
	 * @cssPart `base` — The wrapper containing the input and steppers.
	 * @cssPart `input` — The internal `<input>` control.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssPart `stepper` — Both stepper buttons (for shared styling).
	 * @cssPart `stepper-increment` — The increment (+) button on the end side.
	 * @cssPart `stepper-decrement` — The decrement (-) button on the start side.
	 * @cssState `blank` — The input is empty.
	 * @cssState `focused` — The input has focus.
	 */
	'wa-number-input': {
		elementClass: HTMLElementTagNameMap['wa-number-input'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-number-input']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-number-input']['input'];
			/**
			 * @attribute `title`
			 */
			title?: HTMLElementTagNameMap['wa-number-input']['title'];
			value?: HTMLElementTagNameMap['wa-number-input']['value'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			defaultValue?: HTMLElementTagNameMap['wa-number-input']['defaultValue'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-number-input']['size'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-number-input']['appearance'];
			/**
			 * @attribute `pill`
			 * @reflects
			 */
			pill?: HTMLElementTagNameMap['wa-number-input']['pill'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-number-input']['label'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-number-input']['hint'];
			/**
			 * @attribute `placeholder`
			 */
			placeholder?: HTMLElementTagNameMap['wa-number-input']['placeholder'];
			/**
			 * @attribute `readonly`
			 * @reflects
			 */
			readonly?: HTMLElementTagNameMap['wa-number-input']['readonly'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-number-input']['required'];
			/**
			 * @attribute `min`
			 */
			min?: HTMLElementTagNameMap['wa-number-input']['min'];
			/**
			 * @attribute `max`
			 */
			max?: HTMLElementTagNameMap['wa-number-input']['max'];
			/**
			 * @attribute `step`
			 */
			step?: HTMLElementTagNameMap['wa-number-input']['step'];
			/**
			 * @attribute `without-steppers`
			 */
			withoutSteppers?: HTMLElementTagNameMap['wa-number-input']['withoutSteppers'];
			/**
			 * @attribute `autocomplete`
			 */
			autocomplete?: HTMLElementTagNameMap['wa-number-input']['autocomplete'];
			/**
			 * @attribute `autofocus`
			 */
			autofocus?: HTMLElementTagNameMap['wa-number-input']['autofocus'];
			/**
			 * @attribute `enterkeyhint`
			 */
			enterkeyhint?: HTMLElementTagNameMap['wa-number-input']['enterkeyhint'];
			/**
			 * @attribute `inputmode`
			 */
			inputmode?: HTMLElementTagNameMap['wa-number-input']['inputmode'];
			/**
			 * @attribute `with-label`
			 */
			withLabel?: HTMLElementTagNameMap['wa-number-input']['withLabel'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-number-input']['withHint'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-number-input']['name'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-number-input']['disabled'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-number-input']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-number-input']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-number-input']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-number-input']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-number-input']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-number-input']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-number-input']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-number-input']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-number-input']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-number-input']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-number-input']['didSSR'];
		};
		attributes: {
			/**
			 * @property `title`
			 */
			title?: string;
			/**
			 * @property `defaultValue`
			 */
			value?: string | null;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `appearance`
			 */
			appearance?: 'filled' | 'outlined' | 'filled-outlined';
			/**
			 * @property `pill`
			 */
			pill?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `placeholder`
			 */
			placeholder?: string;
			/**
			 * @property `readonly`
			 */
			readonly?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `min`
			 */
			min?: number;
			/**
			 * @property `max`
			 */
			max?: number;
			/**
			 * @property `step`
			 */
			step?: number | 'any';
			/**
			 * @property `withoutSteppers`
			 */
			'without-steppers'?: boolean;
			/**
			 * @property `autocomplete`
			 */
			autocomplete?: string;
			/**
			 * @property `autofocus`
			 */
			autofocus?: boolean;
			/**
			 * @property `enterkeyhint`
			 */
			enterkeyhint?:
				| 'enter'
				| 'done'
				| 'go'
				| 'next'
				| 'previous'
				| 'search'
				| 'send';
			/**
			 * @property `inputmode`
			 */
			inputmode?: 'numeric' | 'decimal';
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			input?: GlobalEventHandlersEventMap['input'];
			change?: GlobalEventHandlersEventMap['change'];
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			beforeinput?: GlobalEventHandlersEventMap['beforeinput'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {};
		cssParts: {
			label?: string;
			'form-control-label'?: string;
			hint?: string;
			base?: string;
			input?: string;
			start?: string;
			end?: string;
			stepper?: string;
			'stepper-increment'?: string;
			'stepper-decrement'?: string;
		};
		cssStates: {
			blank?: string;
			focused?: string;
		};
		slots: {
			label?: string;
			start?: string;
			end?: string;
			'increment-icon'?: string;
			'decrement-icon'?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTag extends WebAwesomeElement (<wa-tag>)
	 * ```
	 * ---
	 * @summary Tags label, categorize, or represent selections with a compact visual marker. Use them for status
	 * indicators, filters, or removable chips.
	 * @slot [***default***] — The tag's content.
	 * @fires `wa-remove` — {`Event`} — Emitted when the remove button is activated.
	 * @attribute `variant` — {`'brand' | 'neutral' | 'success' | 'warning' | 'danger'`} — The tag's theme variant. Defaults to `neutral` if not within another element with a variant.
	 * @attribute `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined'`} — The tag's visual appearance.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The tag's size.
	 * @attribute `pill` — {`boolean`} — Draws a pill-style tag with rounded edges.
	 * @attribute `with-remove` — {`boolean`} — Makes the tag removable and shows a remove button.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `variant` — {`'brand' | 'neutral' | 'success' | 'warning' | 'danger'`} — The tag's theme variant. Defaults to `neutral` if not within another element with a variant.
	 * @property `appearance` — {`'accent' | 'filled' | 'outlined' | 'filled-outlined'`} — The tag's visual appearance.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The tag's size.
	 * @property `pill` — {`boolean`} — Draws a pill-style tag with rounded edges.
	 * @property `withRemove` — {`boolean`} — Makes the tag removable and shows a remove button.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `content` — The tag's content.
	 * @cssPart `remove-button` — The tag's remove button, a `<wa-button>`.
	 * @cssPart `remove-button__base` — The remove button's exported `base` part.
	 */
	'wa-tag': {
		elementClass: HTMLElementTagNameMap['wa-tag'];
		properties: {
			/**
			 * @attribute `variant`
			 * @reflects
			 */
			variant?: HTMLElementTagNameMap['wa-tag']['variant'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-tag']['appearance'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-tag']['size'];
			/**
			 * @attribute `pill`
			 * @reflects
			 */
			pill?: HTMLElementTagNameMap['wa-tag']['pill'];
			/**
			 * @attribute `with-remove`
			 */
			withRemove?: HTMLElementTagNameMap['wa-tag']['withRemove'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tag']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tag']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tag']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tag']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tag']['didSSR'];
		};
		attributes: {
			/**
			 * @property `variant`
			 */
			variant?: 'brand' | 'neutral' | 'success' | 'warning' | 'danger';
			/**
			 * @property `appearance`
			 */
			appearance?: 'accent' | 'filled' | 'outlined' | 'filled-outlined';
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `pill`
			 */
			pill?: boolean;
			/**
			 * @property `withRemove`
			 */
			'with-remove'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-remove'?: GlobalEventHandlersEventMap['wa-remove'];
		};
		cssProperties: {};
		cssParts: {
			base?: string;
			content?: string;
			'remove-button'?: string;
			'remove-button__base'?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaSelect extends WebAwesomeFormAssociatedElement (<wa-select>)
	 * ```
	 * ---
	 * @summary Selects let users choose one or more values from a dropdown list of predefined options. Use them in forms
	 * when a fixed set of choices needs to fit in limited space.
	 * @slot [***default***] — The listbox options. Must be `<wa-option>` elements. You can use `<wa-divider>` to group items visually.
	 * @slot `label` — The input's label. Alternatively, you can use the `label` attribute.
	 * @slot `start` — An element, such as `<wa-icon>`, placed at the start of the combobox.
	 * @slot `end` — An element, such as `<wa-icon>`, placed at the end of the combobox.
	 * @slot `clear-icon` — An icon to use in lieu of the default clear icon.
	 * @slot `expand-icon` — The icon to show when the control is expanded and collapsed. Rotates on open and close.
	 * @slot `hint` — Text that describes how to use the input. Alternatively, you can use the `hint` attribute.
	 * @fires `input` — {`InputEvent`} — Emitted when the control receives input.
	 * @fires `change` — {`Event`} — Emitted when the control's value changes.
	 * @fires `focus` — {`Event`} — Emitted when the control gains focus.
	 * @fires `blur` — {`Event`} — Emitted when the control loses focus.
	 * @fires `wa-clear` — {`Event`} — Emitted when the control's value is cleared.
	 * @fires `wa-show` — {`Event`} — Emitted when the select's menu opens.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the select's menu opens and all animations are complete.
	 * @fires `wa-hide` — {`Event`} — Emitted when the select's menu closes.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the select's menu closes and all animations are complete.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `name` — {`string | null`} — The name of the select, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `value` — The select's value. This will be a string for single select or an array for multi-select.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The select's size.
	 * @attribute `placeholder` — {`string`} — Placeholder text to show as a hint when the select is empty.
	 * @attribute `multiple` — {`boolean`} — Allows more than one option to be selected.
	 * @attribute `max-options-visible` — {`number`} — The maximum number of selected options to show when `multiple` is true. After the maximum, "+n" will be shown to
	 * indicate the number of additional items that are selected. Set to 0 to remove the limit.
	 * @attribute `disabled` — {`boolean`} — Disables the select control. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `with-clear` — {`boolean`} — Adds a clear button when the select is not empty.
	 * @attribute `open` — {`boolean`} — Indicates whether or not the select is open. You can toggle this attribute to show and hide the menu, or you can
	 * use the `show()` and `hide()` methods and this attribute will reflect the select's open state.
	 * @attribute `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The select's visual appearance.
	 * @attribute `pill` — {`boolean`} — Draws a pill-style select with rounded edges.
	 * @attribute `label` — {`string`} — The select's label. If you need to display HTML, use the `label` slot instead.
	 * @attribute `placement` — {`'top' | 'bottom'`} — The preferred placement of the select's menu. Note that the actual placement may vary as needed to keep the listbox
	 * inside of the viewport.
	 * @attribute `hint` — {`string`} — The select's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `required` — {`boolean`} — The select's required attribute.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `popup` — {`WaPopup`}
	 * @property `combobox` — {`HTMLSlotElement`}
	 * @property `displayInput` — {`HTMLInputElement`}
	 * @property `valueInput` — {`HTMLInputElement`}
	 * @property `listbox` — {`HTMLSlotElement`}
	 * @property `displayLabel` — {`string`}
	 * @property `currentOption` — {`WaOption`}
	 * @property `selectedOptions` — {`WaOption[]`}
	 * @property `name` — {`string | null`} — The name of the select, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `defaultValue`
	 * @property `value` — The select's value. This will be a string for single select or an array for multi-select.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The select's size.
	 * @property `placeholder` — {`string`} — Placeholder text to show as a hint when the select is empty.
	 * @property `multiple` — {`boolean`} — Allows more than one option to be selected.
	 * @property `maxOptionsVisible` — {`number`} — The maximum number of selected options to show when `multiple` is true. After the maximum, "+n" will be shown to
	 * indicate the number of additional items that are selected. Set to 0 to remove the limit.
	 * @property `disabled` — {`boolean`} — Disables the select control. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `withClear` — {`boolean`} — Adds a clear button when the select is not empty.
	 * @property `open` — {`boolean`} — Indicates whether or not the select is open. You can toggle this attribute to show and hide the menu, or you can
	 * use the `show()` and `hide()` methods and this attribute will reflect the select's open state.
	 * @property `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The select's visual appearance.
	 * @property `pill` — {`boolean`} — Draws a pill-style select with rounded edges.
	 * @property `label` — {`string`} — The select's label. If you need to display HTML, use the `label` slot instead.
	 * @property `placement` — {`'top' | 'bottom'`} — The preferred placement of the select's menu. Note that the actual placement may vary as needed to keep the listbox
	 * inside of the viewport.
	 * @property `hint` — {`string`} — The select's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `required` — {`boolean`} — The select's required attribute. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `getTag` — {`(option: WaOption, index: number) => TemplateResult | string | HTMLElement`} — A function that customizes the tags to be rendered when multiple=true. The first argument is the option, the second
	 * is the current tag's index.  The function should return either a Lit TemplateResult or a string containing trusted
	 * HTML of the symbol to render at the specified value.
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `form-control` — The form control that wraps the label, input, and hint.
	 * @cssPart `form-control-label` — The label's wrapper.
	 * @cssPart `form-control-input` — The select's wrapper.
	 * @cssPart `hint` — The hint's wrapper.
	 * @cssPart `combobox` — The container the wraps the start, end, value, clear icon, and expand button.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssPart `display-input` — The element that displays the selected option's label, an `<input>` element.
	 * @cssPart `listbox` — The listbox container where options are slotted.
	 * @cssPart `tags` — The container that houses option tags when `multiselect` is used.
	 * @cssPart `tag` — The individual tags that represent each multiselect option.
	 * @cssPart `tag__content` — The tag's content part.
	 * @cssPart `tag__remove-button` — The tag's remove button.
	 * @cssPart `tag__remove-button__base` — The tag's remove button base part.
	 * @cssPart `clear-button` — The clear button.
	 * @cssPart `expand-icon` — The container that wraps the expand icon.
	 * @cssProperty `--show-duration` — The duration of the show animation.
	 * @cssProperty `--hide-duration` — The duration of the hide animation.
	 * @cssProperty `--tag-max-size` — When using `multiple`, the max size of tags before their content is truncated.
	 * @cssState `blank` — The select is empty.
	 */
	'wa-select': {
		elementClass: HTMLElementTagNameMap['wa-select'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-select']['assumeInteractionOn'];
			popup?: HTMLElementTagNameMap['wa-select']['popup'];
			combobox?: HTMLElementTagNameMap['wa-select']['combobox'];
			displayInput?: HTMLElementTagNameMap['wa-select']['displayInput'];
			valueInput?: HTMLElementTagNameMap['wa-select']['valueInput'];
			listbox?: HTMLElementTagNameMap['wa-select']['listbox'];
			displayLabel?: HTMLElementTagNameMap['wa-select']['displayLabel'];
			currentOption?: HTMLElementTagNameMap['wa-select']['currentOption'];
			selectedOptions?: HTMLElementTagNameMap['wa-select']['selectedOptions'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-select']['name'];
			defaultValue?: HTMLElementTagNameMap['wa-select']['defaultValue'];
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-select']['value'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-select']['size'];
			/**
			 * @attribute `placeholder`
			 */
			placeholder?: HTMLElementTagNameMap['wa-select']['placeholder'];
			/**
			 * @attribute `multiple`
			 * @reflects
			 */
			multiple?: HTMLElementTagNameMap['wa-select']['multiple'];
			/**
			 * @attribute `max-options-visible`
			 */
			maxOptionsVisible?: HTMLElementTagNameMap['wa-select']['maxOptionsVisible'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-select']['disabled'];
			/**
			 * @attribute `with-clear`
			 */
			withClear?: HTMLElementTagNameMap['wa-select']['withClear'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-select']['open'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-select']['appearance'];
			/**
			 * @attribute `pill`
			 * @reflects
			 */
			pill?: HTMLElementTagNameMap['wa-select']['pill'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-select']['label'];
			/**
			 * @attribute `placement`
			 * @reflects
			 */
			placement?: HTMLElementTagNameMap['wa-select']['placement'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-select']['hint'];
			/**
			 * @attribute `with-label`
			 */
			withLabel?: HTMLElementTagNameMap['wa-select']['withLabel'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-select']['withHint'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-select']['required'];
			getTag?: HTMLElementTagNameMap['wa-select']['getTag'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-select']['input'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-select']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-select']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-select']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-select']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-select']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-select']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-select']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-select']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-select']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-select']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-select']['didSSR'];
		};
		attributes: {
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `value`
			 */
			value?: unknown;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `placeholder`
			 */
			placeholder?: string;
			/**
			 * @property `multiple`
			 */
			multiple?: boolean;
			/**
			 * @property `maxOptionsVisible`
			 */
			'max-options-visible'?: number;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `withClear`
			 */
			'with-clear'?: boolean;
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `appearance`
			 */
			appearance?: 'filled' | 'outlined' | 'filled-outlined';
			/**
			 * @property `pill`
			 */
			pill?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `placement`
			 */
			placement?: 'top' | 'bottom';
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			input?: GlobalEventHandlersEventMap['input'];
			change?: GlobalEventHandlersEventMap['change'];
			focus?: GlobalEventHandlersEventMap['focus'];
			blur?: GlobalEventHandlersEventMap['blur'];
			'wa-clear'?: GlobalEventHandlersEventMap['wa-clear'];
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {
			'--show-duration'?: string;
			'--hide-duration'?: string;
			'--tag-max-size'?: string;
		};
		cssParts: {
			'form-control'?: string;
			'form-control-label'?: string;
			'form-control-input'?: string;
			hint?: string;
			combobox?: string;
			start?: string;
			end?: string;
			'display-input'?: string;
			listbox?: string;
			tags?: string;
			tag?: string;
			tag__content?: string;
			'tag__remove-button'?: string;
			'tag__remove-button__base'?: string;
			'clear-button'?: string;
			'expand-icon'?: string;
		};
		cssStates: {
			blank?: string;
		};
		slots: {
			''?: string;
			label?: string;
			start?: string;
			end?: string;
			'clear-icon'?: string;
			'expand-icon'?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaOption extends WebAwesomeElement (<wa-option>)
	 * ```
	 * ---
	 * @summary Options represent the individual choices inside a select or similar form control. Each option holds a value
	 * and the label shown to the user.
	 * @slot [***default***] — The option's label.
	 * @slot `start` — An element, such as `<wa-icon>`, placed before the label.
	 * @slot `end` — An element, such as `<wa-icon>`, placed after the label.
	 * @attribute `value` — {`string`} — The option's value. When selected, the containing form control will receive this value. The value must be unique
	 * from other options in the same group. Values may not contain spaces, as spaces are used as delimiters when listing
	 * multiple values.
	 * @attribute `disabled` — {`boolean`} — Draws the option in a disabled state, preventing selection.
	 * @attribute `selected` — {`boolean`} — Selects an option initially.
	 * @attribute `label` — {`string`} — The option’s plain text label.
	 * Usually automatically generated, but can be useful to provide manually for cases involving complex content.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `current` — {`boolean`}
	 * @property `value` — {`string`} — The option's value. When selected, the containing form control will receive this value. The value must be unique
	 * from other options in the same group. Values may not contain spaces, as spaces are used as delimiters when listing
	 * multiple values.
	 * @property `disabled` — {`boolean`} — Draws the option in a disabled state, preventing selection.
	 * @property `defaultSelected` — {`boolean`} — Selects an option initially.
	 * @property `_label` — {`string`}
	 * @property `label` — {`string`} — The option’s plain text label.
	 * Usually automatically generated, but can be useful to provide manually for cases involving complex content.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `checked-icon` — The checked icon, a `<wa-icon>` element.
	 * @cssPart `label` — The option's label.
	 * @cssPart `start` — The container that wraps the `start` slot.
	 * @cssPart `end` — The container that wraps the `end` slot.
	 * @cssState `current` — The user has keyed into the option, but hasn't selected it yet (shows a highlight)
	 * @cssState `selected` — The option is selected and has aria-selected="true"
	 * @cssState `disabled` — Applied when the option is disabled
	 * @cssState `hover` — Like `:hover` but works while dragging in Safari
	 */
	'wa-option': {
		elementClass: HTMLElementTagNameMap['wa-option'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-option']['defaultSlot'];
			current?: HTMLElementTagNameMap['wa-option']['current'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-option']['value'];
			/**
			 * @attribute `disabled`
			 */
			disabled?: HTMLElementTagNameMap['wa-option']['disabled'];
			/**
			 * @attribute `selected`
			 */
			defaultSelected?: HTMLElementTagNameMap['wa-option']['defaultSelected'];
			_label?: HTMLElementTagNameMap['wa-option']['_label'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-option']['label'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-option']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-option']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-option']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-option']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-option']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: string;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `defaultSelected`
			 */
			selected?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			'checked-icon'?: string;
			label?: string;
			start?: string;
			end?: string;
		};
		cssStates: {
			current?: string;
			selected?: string;
			disabled?: string;
			hover?: string;
		};
		slots: {
			''?: string;
			start?: string;
			end?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaPopover extends WebAwesomeElement (<wa-popover>)
	 * ```
	 * ---
	 * @summary Popovers display contextual content and interactive elements in a floating panel anchored to a trigger. Use
	 * them for rich tooltips, menus, or any content that appears on demand without navigating away.
	 * @slot [***default***] — The popover's content. Interactive elements such as buttons and links are supported.
	 * @fires `wa-show` — {`Event`} — Emitted when the popover begins to show. Canceling this event will stop the popover from showing.
	 * @fires `wa-after-show` — {`Event`} — Emitted after the popover has shown and all animations are complete.
	 * @fires `wa-hide` — {`Event`} — Emitted when the popover begins to hide. Canceling this event will stop the popover from hiding.
	 * @fires `wa-after-hide` — {`Event`} — Emitted after the popover has hidden and all animations are complete.
	 * @attribute `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the popover. Note that the actual placement may vary as needed to keep the popover
	 * inside of the viewport.
	 * @attribute `open` — {`boolean`} — Shows or hides the popover.
	 * @attribute `distance` — {`number`} — The distance in pixels from which to offset the popover away from its target.
	 * @attribute `skidding` — {`number`} — The distance in pixels from which to offset the popover along its target.
	 * @attribute `for` — {`string | null`} — The ID of the popover's anchor element. This must be an interactive/focusable element such as a button.
	 * @attribute `without-arrow` — {`boolean`} — Removes the arrow from the popover.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `dialog` — {`HTMLDialogElement`}
	 * @property `body` — {`HTMLElement`}
	 * @property `popup` — {`WaPopup`}
	 * @property `anchor` — {`null | Element`}
	 * @property `placement` — {`| 'top'
	 *	 | 'top-start'
	 *	 | 'top-end'
	 *	 | 'right'
	 *	 | 'right-start'
	 *	 | 'right-end'
	 *	 | 'bottom'
	 *	 | 'bottom-start'
	 *	 | 'bottom-end'
	 *	 | 'left'
	 *	 | 'left-start'
	 *	 | 'left-end'`} — The preferred placement of the popover. Note that the actual placement may vary as needed to keep the popover
	 * inside of the viewport.
	 * @property `open` — {`boolean`} — Shows or hides the popover.
	 * @property `distance` — {`number`} — The distance in pixels from which to offset the popover away from its target.
	 * @property `skidding` — {`number`} — The distance in pixels from which to offset the popover along its target.
	 * @property `for` — {`string | null`} — The ID of the popover's anchor element. This must be an interactive/focusable element such as a button.
	 * @property `withoutArrow` — {`boolean`} — Removes the arrow from the popover.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `dialog` — The native dialog element that contains the popover content.
	 * @cssPart `body` — The popover's body where its content is rendered.
	 * @cssPart `popup` — The internal `<wa-popup>` element that positions the popover.
	 * @cssPart `popup__popup` — The popup's exported `popup` part. Use this to target the popover's popup container.
	 * @cssPart `popup__arrow` — The popup's exported `arrow` part. Use this to target the popover's arrow.
	 * @cssProperty `--arrow-size` — The size of the tiny arrow that points to the popover (set to zero to remove).
	 * @cssProperty `--max-width` — The maximum width of the popover's body content.
	 * @cssProperty `--show-duration` — The speed of the show animation.
	 * @cssProperty `--hide-duration` — The speed of the hide animation.
	 * @cssState `open` — Applied when the popover is open.
	 */
	'wa-popover': {
		elementClass: HTMLElementTagNameMap['wa-popover'];
		properties: {
			dialog?: HTMLElementTagNameMap['wa-popover']['dialog'];
			body?: HTMLElementTagNameMap['wa-popover']['body'];
			popup?: HTMLElementTagNameMap['wa-popover']['popup'];
			anchor?: HTMLElementTagNameMap['wa-popover']['anchor'];
			/**
			 * @attribute `placement`
			 */
			placement?: HTMLElementTagNameMap['wa-popover']['placement'];
			/**
			 * @attribute `open`
			 * @reflects
			 */
			open?: HTMLElementTagNameMap['wa-popover']['open'];
			/**
			 * @attribute `distance`
			 */
			distance?: HTMLElementTagNameMap['wa-popover']['distance'];
			/**
			 * @attribute `skidding`
			 */
			skidding?: HTMLElementTagNameMap['wa-popover']['skidding'];
			/**
			 * @attribute `for`
			 */
			for?: HTMLElementTagNameMap['wa-popover']['for'];
			/**
			 * @attribute `without-arrow`
			 * @reflects
			 */
			withoutArrow?: HTMLElementTagNameMap['wa-popover']['withoutArrow'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-popover']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-popover']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-popover']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-popover']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-popover']['didSSR'];
		};
		attributes: {
			/**
			 * @property `placement`
			 */
			placement?:
				| 'top'
				| 'top-start'
				| 'top-end'
				| 'right'
				| 'right-start'
				| 'right-end'
				| 'bottom'
				| 'bottom-start'
				| 'bottom-end'
				| 'left'
				| 'left-start'
				| 'left-end';
			/**
			 * @property `open`
			 */
			open?: boolean;
			/**
			 * @property `distance`
			 */
			distance?: number;
			/**
			 * @property `skidding`
			 */
			skidding?: number;
			/**
			 * @property `for`
			 */
			for?: string | null;
			/**
			 * @property `withoutArrow`
			 */
			'without-arrow'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-show'?: GlobalEventHandlersEventMap['wa-show'];
			'wa-after-show'?: GlobalEventHandlersEventMap['wa-after-show'];
			'wa-hide'?: GlobalEventHandlersEventMap['wa-hide'];
			'wa-after-hide'?: GlobalEventHandlersEventMap['wa-after-hide'];
		};
		cssProperties: {
			'--arrow-size'?: string;
			'--max-width'?: string;
			'--show-duration'?: string;
			'--hide-duration'?: string;
		};
		cssParts: {
			dialog?: string;
			body?: string;
			popup?: string;
			popup__popup?: string;
			popup__arrow?: string;
		};
		cssStates: {
			open?: string;
		};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaProgressBar extends WebAwesomeElement (<wa-progress-bar>)
	 * ```
	 * ---
	 * @summary Progress bars show how far along an ongoing operation is as a horizontal fill. Use them for file uploads,
	 * multi-step flows, or any task with measurable progress.
	 * @slot [***default***] — A label to show inside the progress indicator.
	 * @attribute `value` — {`number`} — The current progress as a percentage, 0 to 100.
	 * @attribute `indeterminate` — {`boolean`} — When true, percentage is ignored, the label is hidden, and the progress bar is drawn in an indeterminate state.
	 * @attribute `label` — {`string`} — A custom label for assistive devices.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `value` — {`number`} — The current progress as a percentage, 0 to 100.
	 * @property `indeterminate` — {`boolean`} — When true, percentage is ignored, the label is hidden, and the progress bar is drawn in an indeterminate state.
	 * @property `label` — {`string`} — A custom label for assistive devices.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `indicator` — The progress bar's indicator.
	 * @cssPart `label` — The progress bar's label.
	 * @cssProperty `--track-height` — The color of the track.
	 * @cssProperty `--track-color` — The color of the track.
	 * @cssProperty `--indicator-color` — The color of the indicator.
	 */
	'wa-progress-bar': {
		elementClass: HTMLElementTagNameMap['wa-progress-bar'];
		properties: {
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-progress-bar']['value'];
			/**
			 * @attribute `indeterminate`
			 * @reflects
			 */
			indeterminate?: HTMLElementTagNameMap['wa-progress-bar']['indeterminate'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-progress-bar']['label'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-progress-bar']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-progress-bar']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-progress-bar']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-progress-bar']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-progress-bar']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: number;
			/**
			 * @property `indeterminate`
			 */
			indeterminate?: boolean;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--track-height'?: string;
			'--track-color'?: string;
			'--indicator-color'?: string;
		};
		cssParts: {
			base?: string;
			indicator?: string;
			label?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaProgressRing extends WebAwesomeElement (<wa-progress-ring>)
	 * ```
	 * ---
	 * @summary Progress rings show how far along a determinate operation is using a circular indicator. Use them as a
	 * compact alternative to progress bars when horizontal space is limited.
	 * @slot [***default***] — A label to show inside the ring.
	 * @attribute `value` — {`number`} — The current progress as a percentage, 0 to 100.
	 * @attribute `label` — {`string`} — A custom label for assistive devices.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `indicator` — {`SVGCircleElement`}
	 * @property `indicatorOffset` — {`string`}
	 * @property `value` — {`number`} — The current progress as a percentage, 0 to 100.
	 * @property `label` — {`string`} — A custom label for assistive devices.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `label` — The progress ring label.
	 * @cssPart `track` — The progress ring's track.
	 * @cssPart `indicator` — The progress ring's indicator.
	 * @cssProperty `--size` — The diameter of the progress ring (cannot be a percentage).
	 * @cssProperty `--track-width` — The width of the track.
	 * @cssProperty `--track-color` — The color of the track.
	 * @cssProperty `--indicator-width` — The width of the indicator. Defaults to the track width.
	 * @cssProperty `--indicator-color` — The color of the indicator.
	 * @cssProperty `--indicator-transition-duration` — The duration of the indicator's transition when the value changes.
	 */
	'wa-progress-ring': {
		elementClass: HTMLElementTagNameMap['wa-progress-ring'];
		properties: {
			indicator?: HTMLElementTagNameMap['wa-progress-ring']['indicator'];
			indicatorOffset?: HTMLElementTagNameMap['wa-progress-ring']['indicatorOffset'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-progress-ring']['value'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-progress-ring']['label'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-progress-ring']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-progress-ring']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-progress-ring']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-progress-ring']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-progress-ring']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: number;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--size'?: string;
			'--track-width'?: string;
			'--track-color'?: string;
			'--indicator-width'?: string;
			'--indicator-color'?: string;
			'--indicator-transition-duration'?: string;
		};
		cssParts: {
			base?: string;
			label?: string;
			track?: string;
			indicator?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaQrCode extends WebAwesomeElement (<wa-qr-code>)
	 * ```
	 * ---
	 * @summary QR codes encode a URL or other short text into a scannable image, rendered client-side using the Canvas API.
	 * Use them to share links, contact info, or Wi-Fi credentials that visitors can scan with a phone.
	 * @attribute `value` — {`string`} — The QR code's value.
	 * @attribute `label` — {`string`} — The label for assistive devices to announce. If unspecified, the value will be used instead.
	 * @attribute `size` — {`number`} — The size of the QR code, in pixels.
	 * @attribute `fill` — {`string`} — The fill color. This can be any valid CSS color, but not a CSS custom property.
	 * @attribute `background` — {`string`} — The background color. This can be any valid CSS color or `transparent`. It cannot be a CSS custom property.
	 * @attribute `radius` — {`number`} — The edge radius of each module. Must be between 0 and 0.5.
	 * @attribute `error-correction` — {`'L' | 'M' | 'Q' | 'H'`} — The level of error correction to use. [Learn more](https://www.qrcode.com/en/about/error_correction.html)
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `canvas` — {`HTMLElement`}
	 * @property `value` — {`string`} — The QR code's value.
	 * @property `label` — {`string`} — The label for assistive devices to announce. If unspecified, the value will be used instead.
	 * @property `size` — {`number`} — The size of the QR code, in pixels.
	 * @property `fill` — {`string`} — The fill color. This can be any valid CSS color, but not a CSS custom property.
	 * @property `background` — {`string`} — The background color. This can be any valid CSS color or `transparent`. It cannot be a CSS custom property.
	 * @property `radius` — {`number`} — The edge radius of each module. Must be between 0 and 0.5.
	 * @property `errorCorrection` — {`'L' | 'M' | 'Q' | 'H'`} — The level of error correction to use. [Learn more](https://www.qrcode.com/en/about/error_correction.html)
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 */
	'wa-qr-code': {
		elementClass: HTMLElementTagNameMap['wa-qr-code'];
		properties: {
			canvas?: HTMLElementTagNameMap['wa-qr-code']['canvas'];
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-qr-code']['value'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-qr-code']['label'];
			/**
			 * @attribute `size`
			 */
			size?: HTMLElementTagNameMap['wa-qr-code']['size'];
			/**
			 * @deprecated Use the `color` CSS property instead.
			 * @attribute `fill`
			 */
			fill?: HTMLElementTagNameMap['wa-qr-code']['fill'];
			/**
			 * @deprecated Use the `background` or `background-color` CSS property on the host element instead.
			 * @attribute `background`
			 */
			background?: HTMLElementTagNameMap['wa-qr-code']['background'];
			/**
			 * @attribute `radius`
			 */
			radius?: HTMLElementTagNameMap['wa-qr-code']['radius'];
			/**
			 * @attribute `error-correction`
			 */
			errorCorrection?: HTMLElementTagNameMap['wa-qr-code']['errorCorrection'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-qr-code']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-qr-code']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-qr-code']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-qr-code']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-qr-code']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: string;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `size`
			 */
			size?: number;
			/**
			 * @property `fill`
			 * @deprecated Use the `color` CSS property instead.
			 */
			fill?: string;
			/**
			 * @property `background`
			 * @deprecated Use the `background` or `background-color` CSS property on the host element instead.
			 */
			background?: string;
			/**
			 * @property `radius`
			 */
			radius?: number;
			/**
			 * @property `errorCorrection`
			 */
			'error-correction'?: 'L' | 'M' | 'Q' | 'H';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaRadio extends WebAwesomeFormAssociatedElement (<wa-radio>)
	 * ```
	 * ---
	 * @summary Radios represent a single option within a mutually exclusive set. Use them inside a radio group when users
	 * must pick exactly one choice from a small list.
	 * @slot [***default***] — The radio's label.
	 * @fires `blur` — {`Event`} — Emitted when the control loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the control gains focus.
	 * @attribute `value` — {`string`} — The radio's value. When selected, the radio group will receive this value.
	 * @attribute `appearance` — {`'default' | 'button'`} — The radio's visual appearance.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The radio's size. When used inside a radio group, the size will be determined by the radio group's size, which will
	 * override this attribute.
	 * @attribute `disabled` — {`boolean`} — Disables the radio. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `name` — {`string | null`} — The name of the input, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `checked` — {`boolean`}
	 * @property `value` — {`string`} — The radio's value. When selected, the radio group will receive this value.
	 * @property `appearance` — {`'default' | 'button'`} — The radio's visual appearance.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The radio's size. When used inside a radio group, the size will be determined by the radio group's size, which will
	 * override this attribute.
	 * @property `disabled` — {`boolean`} — Disables the radio. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `name` — {`string | null`} — The name of the input, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `required` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `control` — The circular container that wraps the radio's checked state.
	 * @cssPart `checked-icon` — The checked icon.
	 * @cssPart `label` — The container that wraps the radio's label.
	 * @cssProperty `--checked-icon-color` — The color of the checked icon.
	 * @cssProperty `--checked-icon-scale` — The size of the checked icon relative to the radio.
	 * @cssState `checked` — Applied when the control is checked.
	 * @cssState `disabled` — Applied when the control is disabled.
	 */
	'wa-radio': {
		elementClass: HTMLElementTagNameMap['wa-radio'];
		properties: {
			checked?: HTMLElementTagNameMap['wa-radio']['checked'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-radio']['value'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-radio']['appearance'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-radio']['size'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-radio']['disabled'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-radio']['name'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-radio']['required'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-radio']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-radio']['input'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-radio']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-radio']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-radio']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-radio']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-radio']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-radio']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-radio']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-radio']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-radio']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-radio']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-radio']['didSSR'];
		};
		attributes: {
			/**
			 * @property `value`
			 */
			value?: string;
			/**
			 * @property `appearance`
			 */
			appearance?: 'default' | 'button';
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
		};
		cssProperties: {
			'--checked-icon-color'?: string;
			'--checked-icon-scale'?: string;
		};
		cssParts: {
			control?: string;
			'checked-icon'?: string;
			label?: string;
		};
		cssStates: {
			checked?: string;
			disabled?: string;
		};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaRadioGroup extends WebAwesomeFormAssociatedElement (<wa-radio-group>)
	 * ```
	 * ---
	 * @summary Radio groups wrap a set of radios so they function as a single form control with one shared value. They
	 * handle keyboard navigation, labeling, and validation for the group as a whole.
	 * @slot [***default***] — The default slot where `<wa-radio>` elements are placed.
	 * @slot `label` — The radio group's label. Required for proper accessibility. Alternatively, you can use the `label` attribute.
	 * @slot `hint` — Text that describes how to use the radio group. Alternatively, you can use the `hint` attribute.
	 * @fires `input` — {`InputEvent`} — Emitted when the radio group receives user input.
	 * @fires `change` — {`Event`} — Emitted when the radio group's selected value changes.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `label` — {`string`} — The radio group's label. Required for proper accessibility. If you need to display HTML, use the `label` slot
	 * instead.
	 * @attribute `hint` — {`string`} — The radio groups's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `name` — {`string | null`} — The name of the radio group, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `disabled` — {`boolean`} — Disables the radio group and all child radios. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — The orientation in which to show radio items.
	 * @attribute `value` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The radio group's size. When present, this size will be applied to all `<wa-radio>` items inside.
	 * @attribute `required` — {`boolean`} — Ensures a child radio is checked before allowing the containing form to submit.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `label` — {`string`} — The radio group's label. Required for proper accessibility. If you need to display HTML, use the `label` slot
	 * instead.
	 * @property `hint` — {`string`} — The radio groups's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `name` — {`string | null`} — The name of the radio group, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `disabled` — {`boolean`} — Disables the radio group and all child radios. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — The orientation in which to show radio items.
	 * @property `value` — The current value of the radio group, submitted as a name/value pair with form data.
	 * @property `defaultValue` — {`string | null`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The radio group's size. When present, this size will be applied to all `<wa-radio>` items inside.
	 * @property `required` — {`boolean`} — Ensures a child radio is checked before allowing the containing form to submit. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `form-control` — The form control that wraps the label, input, and hint.
	 * @cssPart `form-control-label` — The label's wrapper.
	 * @cssPart `form-control-input` — The input's wrapper.
	 * @cssPart `radios` — The wrapper than surrounds radio items, styled as a flex container by default.
	 * @cssPart `hint` — The hint's wrapper.
	 */
	'wa-radio-group': {
		elementClass: HTMLElementTagNameMap['wa-radio-group'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-radio-group']['defaultSlot'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-radio-group']['label'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-radio-group']['hint'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-radio-group']['name'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-radio-group']['disabled'];
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-radio-group']['orientation'];
			value?: HTMLElementTagNameMap['wa-radio-group']['value'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			defaultValue?: HTMLElementTagNameMap['wa-radio-group']['defaultValue'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-radio-group']['size'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-radio-group']['required'];
			/**
			 * @attribute `with-label`
			 */
			withLabel?: HTMLElementTagNameMap['wa-radio-group']['withLabel'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-radio-group']['withHint'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-radio-group']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-radio-group']['input'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-radio-group']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-radio-group']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-radio-group']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-radio-group']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-radio-group']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-radio-group']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-radio-group']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-radio-group']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-radio-group']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-radio-group']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-radio-group']['didSSR'];
		};
		attributes: {
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `defaultValue`
			 */
			value?: string | null;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			input?: GlobalEventHandlersEventMap['input'];
			change?: GlobalEventHandlersEventMap['change'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {};
		cssParts: {
			'form-control'?: string;
			'form-control-label'?: string;
			'form-control-input'?: string;
			radios?: string;
			hint?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			label?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaRating extends WebAwesomeFormAssociatedElement (<wa-rating>)
	 * ```
	 * ---
	 * @summary Ratings display a numeric score as a row of selectable symbols, typically stars. Use them to capture quick
	 * feedback or show an average rating for a product or piece of content.
	 * @fires `change` — {`Event`} — Emitted when the rating's value changes.
	 * @fires `wa-hover` — {`{ phase: 'start' | 'move' | 'end', value: number }`} — Emitted when the user hovers over a value. The `phase` property indicates when hovering starts, moves to a new value, or ends. The `value` property tells what the rating's value would be if the user were to commit to the hovered value.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `name` — {`string | null`} — The name of the rating, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `label` — {`string`} — A label that describes the rating to assistive devices.
	 * @attribute `value` — {`number`} — The current rating.
	 * @attribute `default-value` — {`number`} — The default value of the form control. Used to reset the rating to its initial value.
	 * @attribute `max` — {`number`} — The highest rating to show.
	 * @attribute `precision` — {`number`} — The precision at which the rating will increase and decrease. For example, to allow half-star ratings, set this
	 * attribute to `0.5`.
	 * @attribute `readonly` — {`boolean`} — Makes the rating readonly.
	 * @attribute `disabled` — {`boolean`} — Disables the rating. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `required` — {`boolean`} — Makes the rating a required field.
	 * @attribute `getSymbol` — {`(value: number, isSelected: boolean) => string`} — A function that customizes the symbol to be rendered. The first and only argument is the rating's current value.
	 * The function should return a string containing trusted HTML of the symbol to render at the specified value. Works
	 * well with `<wa-icon>` elements.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The component's size.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `name` — {`string | null`} — The name of the rating, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `label` — {`string`} — A label that describes the rating to assistive devices.
	 * @property `value` — {`number`} — The current rating.
	 * @property `defaultValue` — {`number`} — The default value of the form control. Used to reset the rating to its initial value.
	 * @property `max` — {`number`} — The highest rating to show.
	 * @property `precision` — {`number`} — The precision at which the rating will increase and decrease. For example, to allow half-star ratings, set this
	 * attribute to `0.5`.
	 * @property `readonly` — {`boolean`} — Makes the rating readonly.
	 * @property `disabled` — {`boolean`} — Disables the rating. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `required` — {`boolean`} — Makes the rating a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `getSymbol` — {`(value: number, isSelected: boolean) => string`} — A function that customizes the symbol to be rendered. The first and only argument is the rating's current value.
	 * The function should return a string containing trusted HTML of the symbol to render at the specified value. Works
	 * well with `<wa-icon>` elements.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The component's size.
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssProperty `--symbol-color` — The inactive color for symbols.
	 * @cssProperty `--symbol-color-active` — The active color for symbols.
	 * @cssProperty `--symbol-spacing` — The spacing to use around symbols.
	 */
	'wa-rating': {
		elementClass: HTMLElementTagNameMap['wa-rating'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-rating']['assumeInteractionOn'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-rating']['name'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-rating']['label'];
			/**
			 * @attribute `value`
			 */
			value?: HTMLElementTagNameMap['wa-rating']['value'];
			/**
			 * @attribute `default-value`
			 */
			defaultValue?: HTMLElementTagNameMap['wa-rating']['defaultValue'];
			/**
			 * @attribute `max`
			 */
			max?: HTMLElementTagNameMap['wa-rating']['max'];
			/**
			 * @attribute `precision`
			 */
			precision?: HTMLElementTagNameMap['wa-rating']['precision'];
			/**
			 * @attribute `readonly`
			 * @reflects
			 */
			readonly?: HTMLElementTagNameMap['wa-rating']['readonly'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-rating']['disabled'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-rating']['required'];
			/**
			 * @attribute `getSymbol`
			 */
			getSymbol?: HTMLElementTagNameMap['wa-rating']['getSymbol'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-rating']['size'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-rating']['input'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-rating']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-rating']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-rating']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-rating']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-rating']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-rating']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-rating']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-rating']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-rating']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-rating']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-rating']['didSSR'];
		};
		attributes: {
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `value`
			 */
			value?: number;
			/**
			 * @property `defaultValue`
			 */
			'default-value'?: number;
			/**
			 * @property `max`
			 */
			max?: number;
			/**
			 * @property `precision`
			 */
			precision?: number;
			/**
			 * @property `readonly`
			 */
			readonly?: boolean;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `getSymbol`
			 */
			getSymbol?: unknown;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			change?: GlobalEventHandlersEventMap['change'];
			'wa-hover'?: GlobalEventHandlersEventMap['wa-hover'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {
			'--symbol-color'?: string;
			'--symbol-color-active'?: string;
			'--symbol-spacing'?: string;
		};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaRelativeTime extends WebAwesomeElement (<wa-relative-time>)
	 * ```
	 * ---
	 * @summary Relative times display a date as a localized phrase relative to now, such as "3 hours ago" or "in 2 days".
	 * The phrase updates automatically as time passes and respects the user's locale.
	 * @attribute `date` — {`Date | string`} — The date from which to calculate time from. If not set, the current date and time will be used. When passing a
	 * string, it's strongly recommended to use the ISO 8601 format to ensure timezones are handled correctly. To convert
	 * a date to this format in JavaScript, use [`date.toISOString()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString).
	 * @attribute `format` — {`'long' | 'short' | 'narrow'`} — The formatting style to use.
	 * @attribute `numeric` — {`'always' | 'auto'`} — When `auto`, values such as "yesterday" and "tomorrow" will be shown when possible. When `always`, values such as
	 * "1 day ago" and "in 1 day" will be shown.
	 * @attribute `sync` — {`boolean`} — Keep the displayed value up to date as time passes.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `date` — {`Date | string`} — The date from which to calculate time from. If not set, the current date and time will be used. When passing a
	 * string, it's strongly recommended to use the ISO 8601 format to ensure timezones are handled correctly. To convert
	 * a date to this format in JavaScript, use [`date.toISOString()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString).
	 * @property `format` — {`'long' | 'short' | 'narrow'`} — The formatting style to use.
	 * @property `numeric` — {`'always' | 'auto'`} — When `auto`, values such as "yesterday" and "tomorrow" will be shown when possible. When `always`, values such as
	 * "1 day ago" and "in 1 day" will be shown.
	 * @property `sync` — {`boolean`} — Keep the displayed value up to date as time passes.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-relative-time': {
		elementClass: HTMLElementTagNameMap['wa-relative-time'];
		properties: {
			/**
			 * @attribute `date`
			 */
			date?: HTMLElementTagNameMap['wa-relative-time']['date'];
			/**
			 * @attribute `format`
			 */
			format?: HTMLElementTagNameMap['wa-relative-time']['format'];
			/**
			 * @attribute `numeric`
			 */
			numeric?: HTMLElementTagNameMap['wa-relative-time']['numeric'];
			/**
			 * @attribute `sync`
			 */
			sync?: HTMLElementTagNameMap['wa-relative-time']['sync'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-relative-time']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-relative-time']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-relative-time']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-relative-time']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-relative-time']['didSSR'];
		};
		attributes: {
			/**
			 * @property `date`
			 */
			date?: unknown | string;
			/**
			 * @property `format`
			 */
			format?: 'long' | 'short' | 'narrow';
			/**
			 * @property `numeric`
			 */
			numeric?: 'always' | 'auto';
			/**
			 * @property `sync`
			 */
			sync?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaScroller extends WebAwesomeElement (<wa-scroller>)
	 * ```
	 * ---
	 * @summary Scrollers wrap overflowing content in an accessible container with visual cues that help users recognize and
	 * navigate scrollable regions.
	 * @slot [***default***] — The content to show inside the scroller.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — The scroller's orientation.
	 * @attribute `without-scrollbar` — {`boolean`} — Removes the visible scrollbar.
	 * @attribute `without-shadow` — {`boolean`} — Removes the shadows.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `content` — {`HTMLElement`}
	 * @property `canScroll` — {`boolean`}
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — The scroller's orientation.
	 * @property `withoutScrollbar` — {`boolean`} — Removes the visible scrollbar.
	 * @property `withoutShadow` — {`boolean`} — Removes the shadows.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `content` — The container that wraps the slotted content.
	 * @cssProperty `--shadow-color` — The base color of the shadow.
	 * @cssProperty `--shadow-size` — The size of the shadow.
	 */
	'wa-scroller': {
		elementClass: HTMLElementTagNameMap['wa-scroller'];
		properties: {
			content?: HTMLElementTagNameMap['wa-scroller']['content'];
			canScroll?: HTMLElementTagNameMap['wa-scroller']['canScroll'];
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-scroller']['orientation'];
			/**
			 * @attribute `without-scrollbar`
			 * @reflects
			 */
			withoutScrollbar?: HTMLElementTagNameMap['wa-scroller']['withoutScrollbar'];
			/**
			 * @attribute `without-shadow`
			 * @reflects
			 */
			withoutShadow?: HTMLElementTagNameMap['wa-scroller']['withoutShadow'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-scroller']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-scroller']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-scroller']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-scroller']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-scroller']['didSSR'];
		};
		attributes: {
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `withoutScrollbar`
			 */
			'without-scrollbar'?: boolean;
			/**
			 * @property `withoutShadow`
			 */
			'without-shadow'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--shadow-color'?: string;
			'--shadow-size'?: string;
		};
		cssParts: {
			content?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaResizeObserver extends WebAwesomeElement (<wa-resize-observer>)
	 * ```
	 * ---
	 * @summary Resize observers watch their slotted elements for size changes and emit an event when they occur. Provides a
	 * thin, declarative interface to the browser's ResizeObserver API.
	 * @slot [***default***] — One or more elements to watch for resizing.
	 * @fires `wa-resize` — {`{ entries: ResizeObserverEntry[] }`} — Emitted when the element is resized.
	 * @attribute `disabled` — {`boolean`} — Disables the observer.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `disabled` — {`boolean`} — Disables the observer.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-resize-observer': {
		elementClass: HTMLElementTagNameMap['wa-resize-observer'];
		properties: {
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-resize-observer']['disabled'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-resize-observer']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-resize-observer']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-resize-observer']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-resize-observer']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-resize-observer']['didSSR'];
		};
		attributes: {
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-resize'?: GlobalEventHandlersEventMap['wa-resize'];
		};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaSkeleton extends WebAwesomeElement (<wa-skeleton>)
	 * ```
	 * ---
	 * @summary Skeletons show placeholder shapes where content will appear once it finishes loading, reducing perceived
	 * wait time and preventing layout shift.
	 * @attribute `effect` — {`'pulse' | 'sheen' | 'none'`} — Determines which effect the skeleton will use.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `effect` — {`'pulse' | 'sheen' | 'none'`} — Determines which effect the skeleton will use.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `indicator` — The skeleton's indicator which is responsible for its color and animation.
	 * @cssProperty `--color` — The color of the skeleton.
	 * @cssProperty `--sheen-color` — The sheen color when the skeleton is in its loading state.
	 */
	'wa-skeleton': {
		elementClass: HTMLElementTagNameMap['wa-skeleton'];
		properties: {
			/**
			 * @attribute `effect`
			 * @reflects
			 */
			effect?: HTMLElementTagNameMap['wa-skeleton']['effect'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-skeleton']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-skeleton']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-skeleton']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-skeleton']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-skeleton']['didSSR'];
		};
		attributes: {
			/**
			 * @property `effect`
			 */
			effect?: 'pulse' | 'sheen' | 'none';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--color'?: string;
			'--sheen-color'?: string;
		};
		cssParts: {
			indicator?: string;
		};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaSlider extends WebAwesomeFormAssociatedElement (<wa-slider>)
	 * ```
	 * ---
	 * <wa-slider>
	 * @summary Sliders let users choose a numeric value within a defined range by dragging a thumb along a track.
	 * @slot `label` — The slider label. Alternatively, you can use the `label` attribute.
	 * @slot `hint` — Text that describes how to use the input. Alternatively, you can use the `hint` attribute. instead.
	 * @slot `reference` — One or more reference labels to show visually below the slider.
	 * @fires `change` — {`Event`} — Emitted when an alteration to the control's value is committed by the user.
	 * @fires `blur` — {`FocusEvent`} — Emitted when the control loses focus.
	 * @fires `focus` — {`FocusEvent`} — Emitted when the control gains focus.
	 * @fires `input` — {`InputEvent`} — Emitted when the control receives input.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `label` — {`string`} — The slider's label. If you need to provide HTML in the label, use the `label` slot instead.
	 * @attribute `hint` — {`string`} — The slider hint. If you need to display HTML, use the hint slot instead.
	 * @attribute `name` — {`string | null`} — The name of the slider. This will be submitted with the form as a name/value pair. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `min-value` — {`number`} — The minimum value of a range selection. Used only when range attribute is set.
	 * @attribute `max-value` — {`number`} — The maximum value of a range selection. Used only when range attribute is set.
	 * @attribute `value` — {`number`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `range` — {`boolean`} — Converts the slider to a range slider with two thumbs.
	 * @attribute `disabled` — {`boolean`} — Disables the slider. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `readonly` — {`boolean`} — Makes the slider a read-only field.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — The orientation of the slider.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The slider's size.
	 * @attribute `indicator-offset` — {`number`} — The starting value from which to draw the slider's fill, which is based on its current value.
	 * @attribute `min` — {`number`} — The minimum value allowed.
	 * @attribute `max` — {`number`} — The maximum value allowed.
	 * @attribute `step` — {`number`} — The granularity the value must adhere to when incrementing and decrementing.
	 * @attribute `autofocus` — {`boolean`} — Tells the browser to focus the slider when the page loads or a dialog is shown.
	 * @attribute `tooltip-distance` — {`number`} — The distance of the tooltip from the slider's thumb.
	 * @attribute `tooltip-placement` — {`'top' | 'right' | 'bottom' | 'left'`} — The placement of the tooltip in reference to the slider's thumb.
	 * @attribute `with-markers` — {`boolean`} — Draws markers at each step along the slider.
	 * @attribute `with-tooltip` — {`boolean`} — Draws a tooltip above the thumb when the control has focus or is dragged.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `slider` — {`HTMLElement`}
	 * @property `thumb` — {`HTMLElement`}
	 * @property `thumbMin` — {`HTMLElement`}
	 * @property `thumbMax` — {`HTMLElement`}
	 * @property `track` — {`HTMLElement`}
	 * @property `tooltip` — {`WaTooltip`}
	 * @property `label` — {`string`} — The slider's label. If you need to provide HTML in the label, use the `label` slot instead.
	 * @property `hint` — {`string`} — The slider hint. If you need to display HTML, use the hint slot instead.
	 * @property `name` — {`string | null`} — The name of the slider. This will be submitted with the form as a name/value pair. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `minValue` — {`number`} — The minimum value of a range selection. Used only when range attribute is set.
	 * @property `maxValue` — {`number`} — The maximum value of a range selection. Used only when range attribute is set.
	 * @property `defaultValue` — {`number`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `value` — {`number`} — The current value of the slider, submitted as a name/value pair with form data.
	 * @property `range` — {`boolean`} — Converts the slider to a range slider with two thumbs.
	 * @property `disabled` — {`boolean`} — Disables the slider. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `readonly` — {`boolean`} — Makes the slider a read-only field.
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — The orientation of the slider.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The slider's size.
	 * @property `indicatorOffset` — {`number`} — The starting value from which to draw the slider's fill, which is based on its current value.
	 * @property `min` — {`number`} — The minimum value allowed.
	 * @property `max` — {`number`} — The maximum value allowed.
	 * @property `step` — {`number`} — The granularity the value must adhere to when incrementing and decrementing.
	 * @property `autofocus` — {`boolean`} — Tells the browser to focus the slider when the page loads or a dialog is shown.
	 * @property `tooltipDistance` — {`number`} — The distance of the tooltip from the slider's thumb.
	 * @property `tooltipPlacement` — {`'top' | 'right' | 'bottom' | 'left'`} — The placement of the tooltip in reference to the slider's thumb.
	 * @property `withMarkers` — {`boolean`} — Draws markers at each step along the slider.
	 * @property `withTooltip` — {`boolean`} — Draws a tooltip above the thumb when the control has focus or is dragged.
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `valueFormatter` — {`(value: number) => string`} — A custom formatting function to apply to the value. This will be shown in the tooltip and announced by screen
	 * readers. Must be set with JavaScript. Property only.
	 * @property `required` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `label` — The element that contains the sliders's label.
	 * @cssPart `hint` — The element that contains the slider's description.
	 * @cssPart `slider` — The focusable element with `role="slider"`. Contains the track and reference slot.
	 * @cssPart `track` — The slider's track.
	 * @cssPart `indicator` — The colored indicator that shows from the start of the slider to the current value.
	 * @cssPart `markers` — The container that holds all the markers when `with-markers` is used.
	 * @cssPart `marker` — The individual markers that are shown when `with-markers` is used.
	 * @cssPart `references` — The container that holds references that get slotted in.
	 * @cssPart `thumb` — The slider's thumb.
	 * @cssPart `thumb-min` — The min value thumb in a range slider.
	 * @cssPart `thumb-max` — The max value thumb in a range slider.
	 * @cssPart `tooltip` — The tooltip, a `<wa-tooltip>` element.
	 * @cssPart `tooltip__tooltip` — The tooltip's `tooltip` part.
	 * @cssPart `tooltip__content` — The tooltip's `content` part.
	 * @cssPart `tooltip__arrow` — The tooltip's `arrow` part.
	 * @cssProperty `--track-size` — The height or width of the slider's track.
	 * @cssProperty `--marker-width` — The width of each individual marker.
	 * @cssProperty `--marker-height` — The height of each individual marker.
	 * @cssProperty `--thumb-width` — The width of the thumb.
	 * @cssProperty `--thumb-height` — The height of the thumb.
	 * @cssState `disabled` — Applied when the slider is disabled.
	 * @cssState `dragging` — Applied when the slider is being dragged.
	 * @cssState `focused` — Applied when the slider has focus.
	 * @cssState `user-valid` — Applied when the slider is valid and the user has sufficiently interacted with it.
	 * @cssState `user-invalid` — Applied when the slider is invalid and the user has sufficiently interacted with it.
	 */
	'wa-slider': {
		elementClass: HTMLElementTagNameMap['wa-slider'];
		properties: {
			slider?: HTMLElementTagNameMap['wa-slider']['slider'];
			thumb?: HTMLElementTagNameMap['wa-slider']['thumb'];
			thumbMin?: HTMLElementTagNameMap['wa-slider']['thumbMin'];
			thumbMax?: HTMLElementTagNameMap['wa-slider']['thumbMax'];
			track?: HTMLElementTagNameMap['wa-slider']['track'];
			tooltip?: HTMLElementTagNameMap['wa-slider']['tooltip'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-slider']['label'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-slider']['hint'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-slider']['name'];
			/**
			 * @attribute `min-value`
			 */
			minValue?: HTMLElementTagNameMap['wa-slider']['minValue'];
			/**
			 * @attribute `max-value`
			 */
			maxValue?: HTMLElementTagNameMap['wa-slider']['maxValue'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			defaultValue?: HTMLElementTagNameMap['wa-slider']['defaultValue'];
			value?: HTMLElementTagNameMap['wa-slider']['value'];
			/**
			 * @attribute `range`
			 * @reflects
			 */
			range?: HTMLElementTagNameMap['wa-slider']['range'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-slider']['disabled'];
			/**
			 * @attribute `readonly`
			 * @reflects
			 */
			readonly?: HTMLElementTagNameMap['wa-slider']['readonly'];
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-slider']['orientation'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-slider']['size'];
			/**
			 * @attribute `indicator-offset`
			 */
			indicatorOffset?: HTMLElementTagNameMap['wa-slider']['indicatorOffset'];
			/**
			 * @attribute `min`
			 */
			min?: HTMLElementTagNameMap['wa-slider']['min'];
			/**
			 * @attribute `max`
			 */
			max?: HTMLElementTagNameMap['wa-slider']['max'];
			/**
			 * @attribute `step`
			 */
			step?: HTMLElementTagNameMap['wa-slider']['step'];
			/**
			 * @attribute `autofocus`
			 */
			autofocus?: HTMLElementTagNameMap['wa-slider']['autofocus'];
			/**
			 * @attribute `tooltip-distance`
			 */
			tooltipDistance?: HTMLElementTagNameMap['wa-slider']['tooltipDistance'];
			/**
			 * @attribute `tooltip-placement`
			 * @reflects
			 */
			tooltipPlacement?: HTMLElementTagNameMap['wa-slider']['tooltipPlacement'];
			/**
			 * @attribute `with-markers`
			 */
			withMarkers?: HTMLElementTagNameMap['wa-slider']['withMarkers'];
			/**
			 * @attribute `with-tooltip`
			 */
			withTooltip?: HTMLElementTagNameMap['wa-slider']['withTooltip'];
			/**
			 * @attribute `with-label`
			 */
			withLabel?: HTMLElementTagNameMap['wa-slider']['withLabel'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-slider']['withHint'];
			valueFormatter?: HTMLElementTagNameMap['wa-slider']['valueFormatter'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-slider']['required'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-slider']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-slider']['input'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-slider']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-slider']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-slider']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-slider']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-slider']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-slider']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-slider']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-slider']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-slider']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-slider']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-slider']['didSSR'];
		};
		attributes: {
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `minValue`
			 */
			'min-value'?: number;
			/**
			 * @property `maxValue`
			 */
			'max-value'?: number;
			/**
			 * @property `defaultValue`
			 */
			value?: number;
			/**
			 * @property `range`
			 */
			range?: boolean;
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `readonly`
			 */
			readonly?: boolean;
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `indicatorOffset`
			 */
			'indicator-offset'?: number;
			/**
			 * @property `min`
			 */
			min?: number;
			/**
			 * @property `max`
			 */
			max?: number;
			/**
			 * @property `step`
			 */
			step?: number;
			/**
			 * @property `autofocus`
			 */
			autofocus?: boolean;
			/**
			 * @property `tooltipDistance`
			 */
			'tooltip-distance'?: number;
			/**
			 * @property `tooltipPlacement`
			 */
			'tooltip-placement'?: 'top' | 'right' | 'bottom' | 'left';
			/**
			 * @property `withMarkers`
			 */
			'with-markers'?: boolean;
			/**
			 * @property `withTooltip`
			 */
			'with-tooltip'?: boolean;
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			change?: GlobalEventHandlersEventMap['change'];
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			input?: GlobalEventHandlersEventMap['input'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {
			'--track-size'?: string;
			'--marker-width'?: string;
			'--marker-height'?: string;
			'--thumb-width'?: string;
			'--thumb-height'?: string;
		};
		cssParts: {
			label?: string;
			hint?: string;
			slider?: string;
			track?: string;
			indicator?: string;
			markers?: string;
			marker?: string;
			references?: string;
			thumb?: string;
			'thumb-min'?: string;
			'thumb-max'?: string;
			tooltip?: string;
			tooltip__tooltip?: string;
			tooltip__content?: string;
			tooltip__arrow?: string;
		};
		cssStates: {
			disabled?: string;
			dragging?: string;
			focused?: string;
			'user-valid'?: string;
			'user-invalid'?: string;
		};
		slots: {
			label?: string;
			hint?: string;
			reference?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaSwitch extends WebAwesomeFormAssociatedElement (<wa-switch>)
	 * ```
	 * ---
	 * @summary Switches toggle a single setting on or off and apply the change immediately, without requiring a form
	 * submission.
	 * @slot [***default***] — The switch's label.
	 * @slot `hint` — Text that describes how to use the switch. Alternatively, you can use the `hint` attribute.
	 * @fires `change` — {`Event`} — Emitted when the control's checked state changes.
	 * @fires `input` — {`InputEvent`} — Emitted when the control receives input.
	 * @fires `blur` — {`Event`} — Emitted when the control loses focus.
	 * @fires `focus` — {`Event`} — Emitted when the control gains focus.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `title` — {`string`}
	 * @attribute `name` — {`string | null`} — The name of the switch, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `value` — {`string | null`} — The value of the switch, submitted as a name/value pair with form data.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The switch's size.
	 * @attribute `disabled` — {`boolean`} — Disables the switch. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `checked` — {`boolean`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `required` — {`boolean`} — Makes the switch a required field.
	 * @attribute `hint` — {`string`} — The switch's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `title` — {`string`}
	 * @property `name` — {`string | null`} — The name of the switch, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `value` — {`string | null`} — The value of the switch, submitted as a name/value pair with form data.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The switch's size.
	 * @property `disabled` — {`boolean`} — Disables the switch. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `_checked` — {`boolean | null`}
	 * @property `checked` — Draws the checkbox in a checked state.
	 * @property `defaultChecked` — {`boolean`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `required` — {`boolean`} — Makes the switch a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hint` — {`string`} — The switch's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `control` — The control that houses the switch's thumb.
	 * @cssPart `thumb` — The switch's thumb.
	 * @cssPart `label` — The switch's label.
	 * @cssPart `hint` — The hint's wrapper.
	 * @cssProperty `--width` — The width of the switch.
	 * @cssProperty `--height` — The height of the switch.
	 * @cssProperty `--thumb-size` — The size of the thumb.
	 */
	'wa-switch': {
		elementClass: HTMLElementTagNameMap['wa-switch'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-switch']['input'];
			/**
			 * @attribute `title`
			 */
			title?: HTMLElementTagNameMap['wa-switch']['title'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-switch']['name'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			value?: HTMLElementTagNameMap['wa-switch']['value'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-switch']['size'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-switch']['disabled'];
			_checked?: HTMLElementTagNameMap['wa-switch']['_checked'];
			checked?: HTMLElementTagNameMap['wa-switch']['checked'];
			/**
			 * @attribute `checked`
			 * @reflects
			 */
			defaultChecked?: HTMLElementTagNameMap['wa-switch']['defaultChecked'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-switch']['required'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-switch']['hint'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-switch']['withHint'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-switch']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-switch']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-switch']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-switch']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-switch']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-switch']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-switch']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-switch']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-switch']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-switch']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-switch']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-switch']['didSSR'];
		};
		attributes: {
			/**
			 * @property `title`
			 */
			title?: string;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `value`
			 */
			value?: string | null;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `defaultChecked`
			 */
			checked?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			change?: GlobalEventHandlersEventMap['change'];
			input?: GlobalEventHandlersEventMap['input'];
			blur?: GlobalEventHandlersEventMap['blur'];
			focus?: GlobalEventHandlersEventMap['focus'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {
			'--width'?: string;
			'--height'?: string;
			'--thumb-size'?: string;
		};
		cssParts: {
			base?: string;
			control?: string;
			thumb?: string;
			label?: string;
			hint?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTab extends WebAwesomeElement (<wa-tab>)
	 * ```
	 * ---
	 * @summary Tabs label and activate an individual panel inside a tab group.
	 * @slot [***default***] — The tab's label.
	 * @attribute `panel` — {`string`} — The name of the tab panel this tab is associated with. The panel must be located in the same tab group.
	 * @attribute `disabled` — {`boolean`} — Disables the tab and prevents selection.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `tab` — {`HTMLElement`}
	 * @property `panel` — {`string`} — The name of the tab panel this tab is associated with. The panel must be located in the same tab group.
	 * @property `disabled` — {`boolean`} — Disables the tab and prevents selection.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 */
	'wa-tab': {
		elementClass: HTMLElementTagNameMap['wa-tab'];
		properties: {
			tab?: HTMLElementTagNameMap['wa-tab']['tab'];
			/**
			 * @attribute `panel`
			 * @reflects
			 */
			panel?: HTMLElementTagNameMap['wa-tab']['panel'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-tab']['disabled'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tab']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tab']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tab']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tab']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tab']['didSSR'];
		};
		attributes: {
			/**
			 * @property `panel`
			 */
			panel?: string;
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaSplitPanel extends WebAwesomeElement (<wa-split-panel>)
	 * ```
	 * ---
	 * @summary Split panels display two adjacent panels separated by a draggable divider, letting users resize each side to
	 * suit their workflow.
	 * @slot `start` — Content to place in the start panel.
	 * @slot `end` — Content to place in the end panel.
	 * @slot `divider` — The divider. Useful for slotting in a custom icon that renders as a handle.
	 * @fires `wa-reposition` — {`Event`} — Emitted when the divider's position changes.
	 * @attribute `position` — {`number`} — The current position of the divider from the primary panel's edge as a percentage 0-100. Defaults to 50% of the
	 * container's initial size.
	 * @attribute `position-in-pixels` — {`number`} — The current position of the divider from the primary panel's edge in pixels.
	 * @attribute `orientation` — {`'horizontal' | 'vertical'`} — Sets the split panel's orientation.
	 * @attribute `disabled` — {`boolean`} — Disables resizing. Note that the position may still change as a result of resizing the host element.
	 * @attribute `primary` — {`'start' | 'end' | undefined`} — If no primary panel is designated, both panels will resize proportionally when the host element is resized. If a
	 * primary panel is designated, it will maintain its size and the other panel will grow or shrink as needed when the
	 * host element is resized.
	 * @attribute `snap` — {`string | undefined`} — One or more space-separated values at which the divider should snap. Values can be in pixels or percentages, e.g.
	 * `"100px 50%"`.
	 * @attribute `snap-threshold` — {`number`} — How close the divider must be to a snap point until snapping occurs.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `divider` — {`HTMLElement`}
	 * @property `position` — {`number`} — The current position of the divider from the primary panel's edge as a percentage 0-100. Defaults to 50% of the
	 * container's initial size.
	 * @property `positionInPixels` — {`number`} — The current position of the divider from the primary panel's edge in pixels.
	 * @property `orientation` — {`'horizontal' | 'vertical'`} — Sets the split panel's orientation.
	 * @property `disabled` — {`boolean`} — Disables resizing. Note that the position may still change as a result of resizing the host element.
	 * @property `primary` — {`'start' | 'end' | undefined`} — If no primary panel is designated, both panels will resize proportionally when the host element is resized. If a
	 * primary panel is designated, it will maintain its size and the other panel will grow or shrink as needed when the
	 * host element is resized.
	 * @property `snap` — {`string | undefined`} — One or more space-separated values at which the divider should snap. Values can be in pixels or percentages, e.g.
	 * `"100px 50%"`.
	 * @property `snapThreshold` — {`number`} — How close the divider must be to a snap point until snapping occurs.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `start` — The start panel.
	 * @cssPart `end` — The end panel.
	 * @cssPart `panel` — Targets both the start and end panels.
	 * @cssPart `divider` — The divider that separates the start and end panels.
	 * @cssProperty `--divider-width` — The width of the visible divider.
	 * @cssProperty `--divider-hit-area` — The invisible region around the divider where dragging can occur. This is usually wider than the divider to facilitate easier dragging.
	 * @cssProperty `--min` — The minimum allowed size of the primary panel.
	 * @cssProperty `--max` — The maximum allowed size of the primary panel.
	 */
	'wa-split-panel': {
		elementClass: HTMLElementTagNameMap['wa-split-panel'];
		properties: {
			divider?: HTMLElementTagNameMap['wa-split-panel']['divider'];
			/**
			 * @attribute `position`
			 * @reflects
			 */
			position?: HTMLElementTagNameMap['wa-split-panel']['position'];
			/**
			 * @attribute `position-in-pixels`
			 */
			positionInPixels?: HTMLElementTagNameMap['wa-split-panel']['positionInPixels'];
			/**
			 * @attribute `orientation`
			 * @reflects
			 */
			orientation?: HTMLElementTagNameMap['wa-split-panel']['orientation'];
			/**
			 * @attribute `disabled`
			 * @reflects
			 */
			disabled?: HTMLElementTagNameMap['wa-split-panel']['disabled'];
			/**
			 * @attribute `primary`
			 */
			primary?: HTMLElementTagNameMap['wa-split-panel']['primary'];
			/**
			 * @attribute `snap`
			 */
			snap?: HTMLElementTagNameMap['wa-split-panel']['snap'];
			/**
			 * @attribute `snap-threshold`
			 */
			snapThreshold?: HTMLElementTagNameMap['wa-split-panel']['snapThreshold'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-split-panel']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-split-panel']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-split-panel']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-split-panel']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-split-panel']['didSSR'];
		};
		attributes: {
			/**
			 * @property `position`
			 */
			position?: number;
			/**
			 * @property `positionInPixels`
			 */
			'position-in-pixels'?: number;
			/**
			 * @property `orientation`
			 */
			orientation?: 'horizontal' | 'vertical';
			/**
			 * @property `disabled`
			 */
			disabled?: boolean;
			/**
			 * @property `primary`
			 */
			primary?: 'start' | 'end' | undefined;
			/**
			 * @property `snap`
			 */
			snap?: string | undefined;
			/**
			 * @property `snapThreshold`
			 */
			'snap-threshold'?: number;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-reposition'?: GlobalEventHandlersEventMap['wa-reposition'];
		};
		cssProperties: {
			'--divider-width'?: string;
			'--divider-hit-area'?: string;
			'--min'?: string;
			'--max'?: string;
		};
		cssParts: {
			start?: string;
			end?: string;
			panel?: string;
			divider?: string;
		};
		cssStates: {};
		slots: {
			start?: string;
			end?: string;
			divider?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTabPanel extends WebAwesomeElement (<wa-tab-panel>)
	 * ```
	 * ---
	 * @summary Tab panels hold the content shown for a single tab inside a tab group.
	 * @slot [***default***] — The tab panel's content.
	 * @attribute `name` — {`string`} — The tab panel's name.
	 * @attribute `active` — {`boolean`} — When true, the tab panel will be shown.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `name` — {`string`} — The tab panel's name.
	 * @property `active` — {`boolean`} — When true, the tab panel will be shown.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssProperty `--padding` — The tab panel's padding.
	 */
	'wa-tab-panel': {
		elementClass: HTMLElementTagNameMap['wa-tab-panel'];
		properties: {
			/**
			 * @attribute `name`
			 * @reflects
			 */
			name?: HTMLElementTagNameMap['wa-tab-panel']['name'];
			/**
			 * @attribute `active`
			 * @reflects
			 */
			active?: HTMLElementTagNameMap['wa-tab-panel']['active'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tab-panel']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tab-panel']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tab-panel']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tab-panel']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tab-panel']['didSSR'];
		};
		attributes: {
			/**
			 * @property `name`
			 */
			name?: string;
			/**
			 * @property `active`
			 */
			active?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--padding'?: string;
		};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTabGroup extends WebAwesomeElement (<wa-tab-group>)
	 * ```
	 * ---
	 * @summary Tab groups organize related content into a single container that displays one panel at a time, with tabs for
	 * switching between them.
	 * @slot [***default***] — Used for grouping tab panels in the tab group. Must be `<wa-tab-panel>` elements.
	 * @slot `nav` — Used for grouping tabs in the tab group. Must be `<wa-tab>` elements. Note that `<wa-tab>` will set this slot on itself automatically.
	 * @fires `wa-tab-show` — {`{ name: String }`} — Emitted when a tab is shown.
	 * @fires `wa-tab-hide` — {`{ name: String }`} — Emitted when a tab is hidden.
	 * @attribute `active` — {`string`} — Sets the active tab.
	 * @attribute `placement` — {`'top' | 'bottom' | 'start' | 'end'`} — The placement of the tabs.
	 * @attribute `activation` — {`'auto' | 'manual'`} — When set to auto, navigating tabs with the arrow keys will instantly show the corresponding tab panel. When set to
	 * manual, the tab will receive focus but will not show until the user presses spacebar or enter.
	 * @attribute `without-scroll-controls` — {`boolean`} — Disables the scroll arrows that appear when tabs overflow.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `tabGroup` — {`HTMLElement`}
	 * @property `defaultSlot` — {`HTMLSlotElement`} — Default slot for `<wa-tab-panel>` children (inside the `body` part container).
	 * @property `nav` — {`HTMLElement`}
	 * @property `active` — {`string`} — Sets the active tab.
	 * @property `placement` — {`'top' | 'bottom' | 'start' | 'end'`} — The placement of the tabs.
	 * @property `activation` — {`'auto' | 'manual'`} — When set to auto, navigating tabs with the arrow keys will instantly show the corresponding tab panel. When set to
	 * manual, the tab will receive focus but will not show until the user presses spacebar or enter.
	 * @property `withoutScrollControls` — {`boolean`} — Disables the scroll arrows that appear when tabs overflow.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `nav` — The tab group's navigation container where tabs are slotted in.
	 * @cssPart `tabs` — The container that wraps the tabs.
	 * @cssPart `body` — The tab group's body where tab panels are slotted in.
	 * @cssPart `scroll-button` — The previous/next scroll buttons that show when tabs are scrollable, a `<wa-button>`.
	 * @cssPart `scroll-button-start` — The starting scroll button.
	 * @cssPart `scroll-button-end` — The ending scroll button.
	 * @cssPart `scroll-button__base` — The scroll button's exported `base` part.
	 * @cssProperty `--indicator-color` — The color of the active tab indicator.
	 * @cssProperty `--track-color` — The color of the indicator's track (the line that separates tabs from panels).
	 * @cssProperty `--track-width` — The width of the indicator's track (the line that separates tabs from panels).
	 */
	'wa-tab-group': {
		elementClass: HTMLElementTagNameMap['wa-tab-group'];
		properties: {
			tabGroup?: HTMLElementTagNameMap['wa-tab-group']['tabGroup'];
			defaultSlot?: HTMLElementTagNameMap['wa-tab-group']['defaultSlot'];
			nav?: HTMLElementTagNameMap['wa-tab-group']['nav'];
			/**
			 * @attribute `active`
			 * @reflects
			 */
			active?: HTMLElementTagNameMap['wa-tab-group']['active'];
			/**
			 * @attribute `placement`
			 */
			placement?: HTMLElementTagNameMap['wa-tab-group']['placement'];
			/**
			 * @attribute `activation`
			 */
			activation?: HTMLElementTagNameMap['wa-tab-group']['activation'];
			/**
			 * @attribute `without-scroll-controls`
			 */
			withoutScrollControls?: HTMLElementTagNameMap['wa-tab-group']['withoutScrollControls'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tab-group']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tab-group']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tab-group']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tab-group']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tab-group']['didSSR'];
		};
		attributes: {
			/**
			 * @property `active`
			 */
			active?: string;
			/**
			 * @property `placement`
			 */
			placement?: 'top' | 'bottom' | 'start' | 'end';
			/**
			 * @property `activation`
			 */
			activation?: 'auto' | 'manual';
			/**
			 * @property `withoutScrollControls`
			 */
			'without-scroll-controls'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-tab-show'?: GlobalEventHandlersEventMap['wa-tab-show'];
			'wa-tab-hide'?: GlobalEventHandlersEventMap['wa-tab-hide'];
		};
		cssProperties: {
			'--indicator-color'?: string;
			'--track-color'?: string;
			'--track-width'?: string;
		};
		cssParts: {
			base?: string;
			nav?: string;
			tabs?: string;
			body?: string;
			'scroll-button'?: string;
			'scroll-button-start'?: string;
			'scroll-button-end'?: string;
			'scroll-button__base'?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			nav?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTextarea extends WebAwesomeFormAssociatedElement (<wa-textarea>)
	 * ```
	 * ---
	 * @summary Textareas collect multi-line text input from the user, with optional resizing and character counting.
	 * @slot `label` — The textarea's label. Alternatively, you can use the `label` attribute.
	 * @slot `hint` — Text that describes how to use the input. Alternatively, you can use the `hint` attribute.
	 * @fires `blur` — {`Event`} — Emitted when the control loses focus.
	 * @fires `change` — {`Event`} — Emitted when an alteration to the control's value is committed by the user.
	 * @fires `focus` — {`Event`} — Emitted when the control gains focus.
	 * @fires `input` — {`Event`} — Emitted when the control receives input.
	 * @fires `wa-invalid` — {`Event`} — Emitted when the form control has been checked for validity and its constraints aren't satisfied.
	 * @attribute `title` — {`string`}
	 * @attribute `name` — {`string | null`} — The name of the textarea, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `value` — {`string`} — The default value of the form control. Primarily used for resetting the form control.
	 * @attribute `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The textarea's size.
	 * @attribute `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The textarea's visual appearance.
	 * @attribute `label` — {`string`} — The textarea's label. If you need to display HTML, use the `label` slot instead.
	 * @attribute `hint` — {`string`} — The textarea's hint. If you need to display HTML, use the `hint` slot instead.
	 * @attribute `placeholder` — {`string`} — Placeholder text to show as a hint when the input is empty.
	 * @attribute `rows` — {`number`} — The number of rows to display by default.
	 * @attribute `resize` — {`'none' | 'vertical' | 'horizontal' | 'both' | 'auto'`} — Controls how the textarea can be resized.
	 * @attribute `disabled` — {`boolean`} — Disables the textarea. — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `readonly` — {`boolean`} — Makes the textarea readonly.
	 * @attribute `required` — {`boolean`} — Makes the textarea a required field.
	 * @attribute `minlength` — {`number`} — The minimum length of input that will be considered valid.
	 * @attribute `maxlength` — {`number`} — The maximum length of input that will be considered valid.
	 * @attribute `autocapitalize` — {`'off' | 'none' | 'on' | 'sentences' | 'words' | 'characters'`} — Controls whether and how text input is automatically capitalized as it is entered by the user.
	 * @attribute `autocorrect` — {`boolean`} — Indicates whether the browser's autocorrect feature is on or off. When set as an attribute, use `"off"` or `"on"`.
	 * When set as a property, use `true` or `false`.
	 * @attribute `autocomplete` — {`string`} — Specifies what permission the browser has to provide assistance in filling out form field values. Refer to
	 * [this page on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) for available values.
	 * @attribute `autofocus` — {`boolean`} — Indicates that the input should receive focus on page load.
	 * @attribute `enterkeyhint` — {`'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send'`} — Used to customize the label or icon of the Enter key on virtual keyboards.
	 * @attribute `spellcheck` — {`boolean`} — Enables spell checking on the textarea.
	 * @attribute `inputmode` — {`'none' | 'text' | 'decimal' | 'numeric' | 'tel' | 'search' | 'email' | 'url'`} — Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
	 * keyboard on supportive devices.
	 * @attribute `with-label` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @attribute `with-hint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @attribute `with-count` — {`boolean`} — Shows a character count below the textarea. When `maxlength` is set, shows remaining characters instead.
	 * @attribute `custom-error` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `assumeInteractionOn` — {`string[]`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `input` — {`(HTMLElement & { value: unknown }) | HTMLInputElement | HTMLTextAreaElement | undefined`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `base` — {`HTMLDivElement`}
	 * @property `sizeAdjuster` — {`HTMLTextAreaElement`}
	 * @property `title` — {`string`}
	 * @property `name` — {`string | null`} — The name of the textarea, submitted as a name/value pair with form data. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `value` — The current value of the input, submitted as a name/value pair with form data.
	 * @property `defaultValue` — {`string`} — The default value of the form control. Primarily used for resetting the form control.
	 * @property `size` — {`'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large'`} — The textarea's size.
	 * @property `appearance` — {`'filled' | 'outlined' | 'filled-outlined'`} — The textarea's visual appearance.
	 * @property `label` — {`string`} — The textarea's label. If you need to display HTML, use the `label` slot instead.
	 * @property `hint` — {`string`} — The textarea's hint. If you need to display HTML, use the `hint` slot instead.
	 * @property `placeholder` — {`string`} — Placeholder text to show as a hint when the input is empty.
	 * @property `rows` — {`number`} — The number of rows to display by default.
	 * @property `resize` — {`'none' | 'vertical' | 'horizontal' | 'both' | 'auto'`} — Controls how the textarea can be resized.
	 * @property `disabled` — {`boolean`} — Disables the textarea. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `readonly` — {`boolean`} — Makes the textarea readonly.
	 * @property `required` — {`boolean`} — Makes the textarea a required field. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `minlength` — {`number`} — The minimum length of input that will be considered valid.
	 * @property `maxlength` — {`number`} — The maximum length of input that will be considered valid.
	 * @property `autocapitalize` — {`'off' | 'none' | 'on' | 'sentences' | 'words' | 'characters'`} — Controls whether and how text input is automatically capitalized as it is entered by the user.
	 * @property `autocorrect` — {`boolean`} — Indicates whether the browser's autocorrect feature is on or off. When set as an attribute, use `"off"` or `"on"`.
	 * When set as a property, use `true` or `false`.
	 * @property `autocomplete` — {`string`} — Specifies what permission the browser has to provide assistance in filling out form field values. Refer to
	 * [this page on MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) for available values.
	 * @property `autofocus` — {`boolean`} — Indicates that the input should receive focus on page load.
	 * @property `enterkeyhint` — {`'enter' | 'done' | 'go' | 'next' | 'previous' | 'search' | 'send'`} — Used to customize the label or icon of the Enter key on virtual keyboards.
	 * @property `spellcheck` — {`boolean`} — Enables spell checking on the textarea.
	 * @property `inputmode` — {`'none' | 'text' | 'decimal' | 'numeric' | 'tel' | 'search' | 'email' | 'url'`} — Tells the browser what type of data will be entered by the user, allowing it to display the appropriate virtual
	 * keyboard on supportive devices.
	 * @property `withLabel` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `label` element so the server-rendered markup
	 * includes the label before the component hydrates on the client.
	 * @property `withHint` — {`boolean`} — Only required for SSR. Set to `true` if you're slotting in a `hint` element so the server-rendered markup
	 * includes the hint before the component hydrates on the client.
	 * @property `withCount` — {`boolean`} — Shows a character count below the textarea. When `maxlength` is set, shows remaining characters instead.
	 * @property `valueHasChanged` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `hasInteracted` — {`boolean`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `customError` — {`string | null`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `states` — {`CustomStateSet`} — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `emitInvalid` — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `form` — {`HTMLFormElement | null`} — By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
	 * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
	 * the same document or shadow root for this to work. — _inherited from WebAwesomeFormAssociatedElement_
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `label` — The label
	 * @cssPart `form-control-input` — The input's wrapper.
	 * @cssPart `hint` — The hint's wrapper.
	 * @cssPart `textarea` — The internal `<textarea>` control.
	 * @cssPart `base` — The wrapper around the `<textarea>` control.
	 * @cssPart `count` — The character count element, rendered when the `with-count` attribute is present.
	 * @cssState `blank` — The textarea is empty.
	 */
	'wa-textarea': {
		elementClass: HTMLElementTagNameMap['wa-textarea'];
		properties: {
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			assumeInteractionOn?: HTMLElementTagNameMap['wa-textarea']['assumeInteractionOn'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			input?: HTMLElementTagNameMap['wa-textarea']['input'];
			base?: HTMLElementTagNameMap['wa-textarea']['base'];
			sizeAdjuster?: HTMLElementTagNameMap['wa-textarea']['sizeAdjuster'];
			/**
			 * @attribute `title`
			 */
			title?: HTMLElementTagNameMap['wa-textarea']['title'];
			/**
			 * @attribute `name`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: HTMLElementTagNameMap['wa-textarea']['name'];
			value?: HTMLElementTagNameMap['wa-textarea']['value'];
			/**
			 * @attribute `value`
			 * @reflects
			 */
			defaultValue?: HTMLElementTagNameMap['wa-textarea']['defaultValue'];
			/**
			 * @attribute `size`
			 * @reflects
			 */
			size?: HTMLElementTagNameMap['wa-textarea']['size'];
			/**
			 * @attribute `appearance`
			 * @reflects
			 */
			appearance?: HTMLElementTagNameMap['wa-textarea']['appearance'];
			/**
			 * @attribute `label`
			 */
			label?: HTMLElementTagNameMap['wa-textarea']['label'];
			/**
			 * @attribute `hint`
			 */
			hint?: HTMLElementTagNameMap['wa-textarea']['hint'];
			/**
			 * @attribute `placeholder`
			 */
			placeholder?: HTMLElementTagNameMap['wa-textarea']['placeholder'];
			/**
			 * @attribute `rows`
			 */
			rows?: HTMLElementTagNameMap['wa-textarea']['rows'];
			/**
			 * @attribute `resize`
			 * @reflects
			 */
			resize?: HTMLElementTagNameMap['wa-textarea']['resize'];
			/**
			 * @attribute `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: HTMLElementTagNameMap['wa-textarea']['disabled'];
			/**
			 * @attribute `readonly`
			 * @reflects
			 */
			readonly?: HTMLElementTagNameMap['wa-textarea']['readonly'];
			/**
			 * @attribute `required`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			required?: HTMLElementTagNameMap['wa-textarea']['required'];
			/**
			 * @attribute `minlength`
			 */
			minlength?: HTMLElementTagNameMap['wa-textarea']['minlength'];
			/**
			 * @attribute `maxlength`
			 */
			maxlength?: HTMLElementTagNameMap['wa-textarea']['maxlength'];
			/**
			 * @attribute `autocapitalize`
			 */
			autocapitalize?: HTMLElementTagNameMap['wa-textarea']['autocapitalize'];
			/**
			 * @attribute `autocorrect`
			 */
			autocorrect?: HTMLElementTagNameMap['wa-textarea']['autocorrect'];
			/**
			 * @attribute `autocomplete`
			 */
			autocomplete?: HTMLElementTagNameMap['wa-textarea']['autocomplete'];
			/**
			 * @attribute `autofocus`
			 */
			autofocus?: HTMLElementTagNameMap['wa-textarea']['autofocus'];
			/**
			 * @attribute `enterkeyhint`
			 */
			enterkeyhint?: HTMLElementTagNameMap['wa-textarea']['enterkeyhint'];
			/**
			 * @attribute `spellcheck`
			 */
			spellcheck?: HTMLElementTagNameMap['wa-textarea']['spellcheck'];
			/**
			 * @attribute `inputmode`
			 */
			inputmode?: HTMLElementTagNameMap['wa-textarea']['inputmode'];
			/**
			 * @attribute `with-label`
			 */
			withLabel?: HTMLElementTagNameMap['wa-textarea']['withLabel'];
			/**
			 * @attribute `with-hint`
			 */
			withHint?: HTMLElementTagNameMap['wa-textarea']['withHint'];
			/**
			 * @attribute `with-count`
			 * @reflects
			 */
			withCount?: HTMLElementTagNameMap['wa-textarea']['withCount'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			valueHasChanged?: HTMLElementTagNameMap['wa-textarea']['valueHasChanged'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			hasInteracted?: HTMLElementTagNameMap['wa-textarea']['hasInteracted'];
			/**
			 * @attribute `custom-error`
			 * @reflects
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			customError?: HTMLElementTagNameMap['wa-textarea']['customError'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			states?: HTMLElementTagNameMap['wa-textarea']['states'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			emitInvalid?: HTMLElementTagNameMap['wa-textarea']['emitInvalid'];
			/**
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			form?: HTMLElementTagNameMap['wa-textarea']['form'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-textarea']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-textarea']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-textarea']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-textarea']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-textarea']['didSSR'];
		};
		attributes: {
			/**
			 * @property `title`
			 */
			title?: string;
			/**
			 * @property `name`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			name?: string | null;
			/**
			 * @property `defaultValue`
			 */
			value?: string;
			/**
			 * @property `size`
			 */
			size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'small' | 'medium' | 'large';
			/**
			 * @property `appearance`
			 */
			appearance?: 'filled' | 'outlined' | 'filled-outlined';
			/**
			 * @property `label`
			 */
			label?: string;
			/**
			 * @property `hint`
			 */
			hint?: string;
			/**
			 * @property `placeholder`
			 */
			placeholder?: string;
			/**
			 * @property `rows`
			 */
			rows?: number;
			/**
			 * @property `resize`
			 */
			resize?: 'none' | 'vertical' | 'horizontal' | 'both' | 'auto';
			/**
			 * @property `disabled`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			disabled?: boolean;
			/**
			 * @property `readonly`
			 */
			readonly?: boolean;
			/**
			 * @property `required`
			 */
			required?: boolean;
			/**
			 * @property `minlength`
			 */
			minlength?: number;
			/**
			 * @property `maxlength`
			 */
			maxlength?: number;
			/**
			 * @property `autocapitalize`
			 */
			autocapitalize?:
				| 'off'
				| 'none'
				| 'on'
				| 'sentences'
				| 'words'
				| 'characters';
			/**
			 * @property `autocorrect`
			 */
			autocorrect?: boolean;
			/**
			 * @property `autocomplete`
			 */
			autocomplete?: string;
			/**
			 * @property `autofocus`
			 */
			autofocus?: boolean;
			/**
			 * @property `enterkeyhint`
			 */
			enterkeyhint?:
				| 'enter'
				| 'done'
				| 'go'
				| 'next'
				| 'previous'
				| 'search'
				| 'send';
			/**
			 * @property `spellcheck`
			 */
			spellcheck?: boolean;
			/**
			 * @property `inputmode`
			 */
			inputmode?:
				| 'none'
				| 'text'
				| 'decimal'
				| 'numeric'
				| 'tel'
				| 'search'
				| 'email'
				| 'url';
			/**
			 * @property `withLabel`
			 */
			'with-label'?: boolean;
			/**
			 * @property `withHint`
			 */
			'with-hint'?: boolean;
			/**
			 * @property `withCount`
			 */
			'with-count'?: boolean;
			/**
			 * @property `customError`
			 * @inheritedFrom WebAwesomeFormAssociatedElement
			 */
			'custom-error'?: string | null;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			blur?: GlobalEventHandlersEventMap['blur'];
			change?: GlobalEventHandlersEventMap['change'];
			focus?: GlobalEventHandlersEventMap['focus'];
			input?: GlobalEventHandlersEventMap['input'];
			'wa-invalid'?: GlobalEventHandlersEventMap['wa-invalid'];
		};
		cssProperties: {};
		cssParts: {
			label?: string;
			'form-control-input'?: string;
			hint?: string;
			textarea?: string;
			base?: string;
			count?: string;
		};
		cssStates: {
			blank?: string;
		};
		slots: {
			label?: string;
			hint?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaTree extends WebAwesomeElement (<wa-tree>)
	 * ```
	 * ---
	 * @summary Trees allow you to display a hierarchical list of selectable tree items. Items with children can be expanded
	 * and collapsed as desired by the user.
	 * @slot [***default***] — The default slot.
	 * @slot `expand-icon` — The icon to show when the tree item is expanded. Works best with `<wa-icon>`.
	 * @slot `collapse-icon` — The icon to show when the tree item is collapsed. Works best with `<wa-icon>`.
	 * @fires `wa-selection-change` — {`{ selection: WaTreeItem[] }`} — Emitted when a tree item is selected or deselected.
	 * @attribute `selection` — {`'single' | 'multiple' | 'leaf'`} — The selection behavior of the tree. Single selection allows only one node to be selected at a time. Multiple
	 * displays checkboxes and allows more than one node to be selected. Leaf allows only leaf nodes to be selected.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `defaultSlot` — {`HTMLSlotElement`}
	 * @property `expandedIconSlot` — {`HTMLSlotElement`}
	 * @property `collapsedIconSlot` — {`HTMLSlotElement`}
	 * @property `selection` — {`'single' | 'multiple' | 'leaf'`} — The selection behavior of the tree. Single selection allows only one node to be selected at a time. Multiple
	 * displays checkboxes and allows more than one node to be selected. Leaf allows only leaf nodes to be selected.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssProperty `--indent-size` — The size of the indentation for nested items.
	 * @cssProperty `--indent-guide-color` — The color of the indentation line.
	 * @cssProperty `--indent-guide-offset` — The amount of vertical spacing to leave between the top and bottom of the indentation line's starting position.
	 * @cssProperty `--indent-guide-style` — The style of the indentation line, e.g. solid, dotted, dashed.
	 * @cssProperty `--indent-guide-width` — The width of the indentation line.
	 */
	'wa-tree': {
		elementClass: HTMLElementTagNameMap['wa-tree'];
		properties: {
			defaultSlot?: HTMLElementTagNameMap['wa-tree']['defaultSlot'];
			expandedIconSlot?: HTMLElementTagNameMap['wa-tree']['expandedIconSlot'];
			collapsedIconSlot?: HTMLElementTagNameMap['wa-tree']['collapsedIconSlot'];
			/**
			 * @attribute `selection`
			 */
			selection?: HTMLElementTagNameMap['wa-tree']['selection'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-tree']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-tree']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-tree']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-tree']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-tree']['didSSR'];
		};
		attributes: {
			/**
			 * @property `selection`
			 */
			selection?: 'single' | 'multiple' | 'leaf';
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			'wa-selection-change'?: GlobalEventHandlersEventMap['wa-selection-change'];
		};
		cssProperties: {
			'--indent-size'?: string;
			'--indent-guide-color'?: string;
			'--indent-guide-offset'?: string;
			'--indent-guide-style'?: string;
			'--indent-guide-width'?: string;
		};
		cssParts: {
			base?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			'expand-icon'?: string;
			'collapse-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaZoomableFrame extends WebAwesomeElement (<wa-zoomable-frame>)
	 * ```
	 * ---
	 * @summary Zoomable frames embed iframe content with built-in controls for zooming, panning, and managing interaction.
	 * @slot `zoom-in-icon` — The slot that contains the zoom in icon.
	 * @slot `zoom-out-icon` — The slot that contains the zoom out icon.
	 * @fires `load` — {`Event`} — Emitted when the internal iframe when it finishes loading.
	 * @fires `error` — {`Event`} — Emitted from the internal iframe when it fails to load.
	 * @attribute `src` — {`string`} — The URL of the content to display.
	 * @attribute `srcdoc` — {`string`} — Inline HTML to display.
	 * @attribute `allowfullscreen` — {`boolean`} — Allows fullscreen mode.
	 * @attribute `loading` — {`'eager' | 'lazy'`} — Controls iframe loading behavior.
	 * @attribute `referrerpolicy` — {`string`} — Controls referrer information.
	 * @attribute `sandbox` — {`string`} — Security restrictions for the iframe.
	 * @attribute `zoom` — {`number`} — The current zoom of the frame, e.g. 0 = 0% and 1 = 100%.
	 * @attribute `zoom-levels` — {`string`} — The zoom levels to step through when using zoom controls. This does not restrict programmatic changes to the zoom.
	 * @attribute `without-controls` — {`boolean`} — Removes the zoom controls.
	 * @attribute `without-interaction` — {`boolean`} — Disables interaction when present.
	 * @attribute `with-theme-sync` — {`boolean`} — Enables automatic theme syncing (light/dark mode and theme selector classes) from the host document to the iframe.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `iframe` — {`HTMLIFrameElement`}
	 * @property `src` — {`string`} — The URL of the content to display.
	 * @property `srcdoc` — {`string`} — Inline HTML to display.
	 * @property `allowfullscreen` — {`boolean`} — Allows fullscreen mode.
	 * @property `loading` — {`'eager' | 'lazy'`} — Controls iframe loading behavior.
	 * @property `referrerpolicy` — {`string`} — Controls referrer information.
	 * @property `sandbox` — {`string`} — Security restrictions for the iframe.
	 * @property `zoom` — {`number`} — The current zoom of the frame, e.g. 0 = 0% and 1 = 100%.
	 * @property `zoomLevels` — {`string`} — The zoom levels to step through when using zoom controls. This does not restrict programmatic changes to the zoom.
	 * @property `withoutControls` — {`boolean`} — Removes the zoom controls.
	 * @property `withoutInteraction` — {`boolean`} — Disables interaction when present.
	 * @property `withThemeSync` — {`boolean`} — Enables automatic theme syncing (light/dark mode and theme selector classes) from the host document to the iframe.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `iframe` — The internal `<iframe>` element.
	 * @cssPart `controls` — The container that surrounds zoom control buttons.
	 * @cssPart `zoom-in-button` — The zoom in button.
	 * @cssPart `zoom-out-button` — The zoom out button.
	 */
	'wa-zoomable-frame': {
		elementClass: HTMLElementTagNameMap['wa-zoomable-frame'];
		properties: {
			iframe?: HTMLElementTagNameMap['wa-zoomable-frame']['iframe'];
			/**
			 * @attribute `src`
			 */
			src?: HTMLElementTagNameMap['wa-zoomable-frame']['src'];
			/**
			 * @attribute `srcdoc`
			 */
			srcdoc?: HTMLElementTagNameMap['wa-zoomable-frame']['srcdoc'];
			/**
			 * @attribute `allowfullscreen`
			 */
			allowfullscreen?: HTMLElementTagNameMap['wa-zoomable-frame']['allowfullscreen'];
			/**
			 * @attribute `loading`
			 */
			loading?: HTMLElementTagNameMap['wa-zoomable-frame']['loading'];
			/**
			 * @attribute `referrerpolicy`
			 */
			referrerpolicy?: HTMLElementTagNameMap['wa-zoomable-frame']['referrerpolicy'];
			/**
			 * @attribute `sandbox`
			 */
			sandbox?: HTMLElementTagNameMap['wa-zoomable-frame']['sandbox'];
			/**
			 * @attribute `zoom`
			 * @reflects
			 */
			zoom?: HTMLElementTagNameMap['wa-zoomable-frame']['zoom'];
			/**
			 * @attribute `zoom-levels`
			 */
			zoomLevels?: HTMLElementTagNameMap['wa-zoomable-frame']['zoomLevels'];
			/**
			 * @attribute `without-controls`
			 * @reflects
			 */
			withoutControls?: HTMLElementTagNameMap['wa-zoomable-frame']['withoutControls'];
			/**
			 * @attribute `without-interaction`
			 * @reflects
			 */
			withoutInteraction?: HTMLElementTagNameMap['wa-zoomable-frame']['withoutInteraction'];
			/**
			 * @attribute `with-theme-sync`
			 * @reflects
			 */
			withThemeSync?: HTMLElementTagNameMap['wa-zoomable-frame']['withThemeSync'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-zoomable-frame']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-zoomable-frame']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-zoomable-frame']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-zoomable-frame']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-zoomable-frame']['didSSR'];
		};
		attributes: {
			/**
			 * @property `src`
			 */
			src?: string;
			/**
			 * @property `srcdoc`
			 */
			srcdoc?: string;
			/**
			 * @property `allowfullscreen`
			 */
			allowfullscreen?: boolean;
			/**
			 * @property `loading`
			 */
			loading?: 'eager' | 'lazy';
			/**
			 * @property `referrerpolicy`
			 */
			referrerpolicy?: string;
			/**
			 * @property `sandbox`
			 */
			sandbox?: string;
			/**
			 * @property `zoom`
			 */
			zoom?: number;
			/**
			 * @property `zoomLevels`
			 */
			'zoom-levels'?: string;
			/**
			 * @property `withoutControls`
			 */
			'without-controls'?: boolean;
			/**
			 * @property `withoutInteraction`
			 */
			'without-interaction'?: boolean;
			/**
			 * @property `withThemeSync`
			 */
			'with-theme-sync'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {
			load?: GlobalEventHandlersEventMap['load'];
			error?: GlobalEventHandlersEventMap['error'];
		};
		cssProperties: {};
		cssParts: {
			iframe?: string;
			controls?: string;
			'zoom-in-button'?: string;
			'zoom-out-button'?: string;
		};
		cssStates: {};
		slots: {
			'zoom-in-icon'?: string;
			'zoom-out-icon'?: string;
		};
	};
	/**
	 * ```jsx
	 * class WaMarkdown extends WebAwesomeElement (<wa-markdown>)
	 * ```
	 * ---
	 * @summary Markdown elements render markdown content as HTML directly in the browser, making it easy to display
	 * user-generated content or documentation without a server-side build step.
	 * @attribute `tab-size` — {`number`} — The tab stop width used when converting leading tabs to spaces during whitespace normalization.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `tabSize` — {`number`} — The tab stop width used when converting leading tabs to spaces during whitespace normalization.
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 */
	'wa-markdown': {
		elementClass: HTMLElementTagNameMap['wa-markdown'];
		properties: {
			/**
			 * @attribute `tab-size`
			 */
			tabSize?: HTMLElementTagNameMap['wa-markdown']['tabSize'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-markdown']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-markdown']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-markdown']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-markdown']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-markdown']['didSSR'];
		};
		attributes: {
			/**
			 * @property `tabSize`
			 */
			'tab-size'?: number;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class WaPage extends WebAwesomeElement (<wa-page>)
	 * ```
	 * ---
	 * @summary Pages scaffold an entire application layout with header, navigation, sidebar, main content, aside, and
	 * footer regions. Use them to structure full pages with minimal markup and responsive behavior built in.
	 * @slot [***default***] — The page's main content.
	 * @slot `banner` — The banner that gets display above the header. The banner will not be shown if no content is provided.
	 * @slot `header` — The header to display at the top of the page. If a banner is present, the header will appear below the banner. The header will not be shown if there is no content.
	 * @slot `subheader` — A subheader to display below the `header`. This is a good place to put things like breadcrumbs.
	 * @slot `menu` — The left side of the page. If you slot an element in here, you will override the default `navigation` slot and will be handling navigation on your own. This also will not disable the fallback behavior of the navigation button. This section "sticks" to the top as the page scrolls.
	 * @slot `navigation-header` — The header for a navigation area. On mobile this will be the header for `<wa-drawer>`.
	 * @slot `navigation` — The main content to display in the navigation area. This is displayed on the left side of the page, if `menu` is not used. This section "sticks" to the top as the page scrolls.
	 * @slot `navigation-footer` — The footer for a navigation area. On mobile this will be the footer for `<wa-drawer>`.
	 * @slot `navigation-toggle` — Use this slot to slot in your own button + icon for toggling the navigation drawer. By default it is a `<wa-button>` + a 3 bars `<wa-icon>`
	 * @slot `navigation-toggle-icon` — Use this to slot in your own icon for toggling the navigation drawer. By default it is 3 bars `<wa-icon>`.
	 * @slot `main-header` — Header to display inline above the main content.
	 * @slot `main-footer` — Footer to display inline below the main content.
	 * @slot `aside` — Content to be shown on the right side of the page. Typically contains a table of contents, ads, etc. This section "sticks" to the top as the page scrolls.
	 * @slot `skip-to-content` — The "skip to content" slot. You can override this If you would like to override the `Skip to content` button and add additional "Skip to X", they can be inserted here.
	 * @slot `footer` — The content to display in the footer. This is always displayed underneath the viewport so will always make the page "scrollable".
	 * @attribute `view` — {`'mobile' | 'desktop'`} — The view is a reflection of the "mobileBreakpoint", when the page is larger than the `mobile-breakpoint` (768px by
	 * default), it is considered to be a "desktop" view. The view is merely a way to distinguish when to show/hide the
	 * navigation. You can use additional media queries to make other adjustments to content as necessary.
	 * The default is "desktop" because the "mobile navigation drawer" isn't accessible via SSR due to drawer requiring JS.
	 * @attribute `nav-open` — {`boolean`} — Whether or not the navigation drawer is open. Note, the navigation drawer is only "open" on mobile views.
	 * @attribute `mobile-breakpoint` — {`string`} — At what page width to hide the "navigation" slot and collapse into a hamburger button.
	 * Accepts both numbers (interpreted as px) and CSS lengths (e.g. `50em`), which are resolved based on the root element.
	 * @attribute `navigation-placement` — {`'start' | 'end'`} — Where to place the navigation when in the mobile viewport.
	 * @attribute `disable-navigation-toggle` — {`boolean`} — Determines whether or not to hide the default hamburger button.
	 * This will automatically flip to "true" if you add an element with `data-toggle-nav` anywhere in the element light DOM.
	 * Generally this will be set for you and you don't need to do anything, unless you're using SSR, in which case you should set this manually for initial page loads.
	 * @attribute `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @attribute `did-ssr` — _inherited from WebAwesomeElement_
	 * @property `header` — {`HTMLElement`}
	 * @property `menu` — {`HTMLElement`}
	 * @property `main` — {`HTMLElement`}
	 * @property `aside` — {`HTMLElement`}
	 * @property `subheader` — {`HTMLElement`}
	 * @property `footer` — {`HTMLElement`}
	 * @property `banner` — {`HTMLElement`}
	 * @property `navigationDrawer` — {`WaDrawer`}
	 * @property `navigationToggleSlot` — {`HTMLSlotElement`}
	 * @property `view` — {`'mobile' | 'desktop'`} — The view is a reflection of the "mobileBreakpoint", when the page is larger than the `mobile-breakpoint` (768px by
	 * default), it is considered to be a "desktop" view. The view is merely a way to distinguish when to show/hide the
	 * navigation. You can use additional media queries to make other adjustments to content as necessary.
	 * The default is "desktop" because the "mobile navigation drawer" isn't accessible via SSR due to drawer requiring JS.
	 * @property `navOpen` — {`boolean`} — Whether or not the navigation drawer is open. Note, the navigation drawer is only "open" on mobile views.
	 * @property `mobileBreakpoint` — {`string`} — At what page width to hide the "navigation" slot and collapse into a hamburger button.
	 * Accepts both numbers (interpreted as px) and CSS lengths (e.g. `50em`), which are resolved based on the root element.
	 * @property `navigationPlacement` — {`'start' | 'end'`} — Where to place the navigation when in the mobile viewport.
	 * @property `disableNavigationToggle` — {`boolean`} — Determines whether or not to hide the default hamburger button.
	 * This will automatically flip to "true" if you add an element with `data-toggle-nav` anywhere in the element light DOM.
	 * Generally this will be set for you and you don't need to do anything, unless you're using SSR, in which case you should set this manually for initial page loads.
	 * @property `pageResizeObserver`
	 * @property `updateAsideAndMenuHeights`
	 * @property `initialReflectedProperties` — {`Map<string, unknown>`} — _inherited from WebAwesomeElement_
	 * @property `internals` — {`ElementInternals`} — _inherited from WebAwesomeElement_
	 * @property `dir` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `lang` — {`string`} — _inherited from WebAwesomeElement_
	 * @property `didSSR` — _inherited from WebAwesomeElement_
	 * @cssPart `base` — The component's base wrapper.
	 * @cssPart `banner` — The banner to show above header.
	 * @cssPart `header` — The header, usually for top level navigation / branding.
	 * @cssPart `subheader` — Shown below the header, usually intended for things like breadcrumbs and other page level navigation.
	 * @cssPart `body` — The wrapper around menu, main, and aside.
	 * @cssPart `menu` — The left hand side of the page. Generally intended for navigation.
	 * @cssPart `navigation` — The `<nav>` that wraps the navigation slots on desktop viewports.
	 * @cssPart `navigation-header` — The header for a navigation area. On mobile this will be the header for `<wa-drawer>`.
	 * @cssPart `navigation-footer` — The footer for a navigation area. On mobile this will be the footer for `<wa-drawer>`.
	 * @cssPart `navigation-toggle` — The default `<wa-button>` that will toggle the `<wa-drawer>` for mobile viewports.
	 * @cssPart `navigation-toggle-icon` — The default `<wa-icon>` displayed inside of the navigation-toggle button.
	 * @cssPart `main-header` — The header above main content.
	 * @cssPart `main-content` — The main content.
	 * @cssPart `main-footer` — The footer below main content.
	 * @cssPart `aside` — The right hand side of the page. Used for things like table of contents, ads, etc.
	 * @cssPart `skip-links` — Wrapper around skip-link
	 * @cssPart `skip-link` — The "skip to main content" link
	 * @cssPart `footer` — The footer of the page. This is always below the initial viewport size.
	 * @cssPart `dialog-wrapper` — A wrapper around elements such as dialogs or other modal-like elements.
	 * @cssProperty `--menu-width` — The width of the page's "menu" section.
	 * @cssProperty `--main-width` — The width of the page's "main" section.
	 * @cssProperty `--aside-width` — The wide of the page's "aside" section.
	 * @cssProperty `--banner-height` — The height of the banner. This gets calculated when the page initializes. If the height is known, you can set it here to prevent shifting when the page loads.
	 * @cssProperty `--header-height` — The height of the header. This gets calculated when the page initializes. If the height is known, you can set it here to prevent shifting when the page loads.
	 * @cssProperty `--subheader-height` — The height of the subheader. This gets calculated when the page initializes. If the height is known, you can set it here to prevent shifting when the page loads.
	 */
	'wa-page': {
		elementClass: HTMLElementTagNameMap['wa-page'];
		properties: {
			header?: HTMLElementTagNameMap['wa-page']['header'];
			menu?: HTMLElementTagNameMap['wa-page']['menu'];
			main?: HTMLElementTagNameMap['wa-page']['main'];
			aside?: HTMLElementTagNameMap['wa-page']['aside'];
			subheader?: HTMLElementTagNameMap['wa-page']['subheader'];
			footer?: HTMLElementTagNameMap['wa-page']['footer'];
			banner?: HTMLElementTagNameMap['wa-page']['banner'];
			navigationDrawer?: HTMLElementTagNameMap['wa-page']['navigationDrawer'];
			navigationToggleSlot?: HTMLElementTagNameMap['wa-page']['navigationToggleSlot'];
			/**
			 * @attribute `view`
			 * @reflects
			 */
			view?: HTMLElementTagNameMap['wa-page']['view'];
			/**
			 * @attribute `nav-open`
			 * @reflects
			 */
			navOpen?: HTMLElementTagNameMap['wa-page']['navOpen'];
			/**
			 * @attribute `mobile-breakpoint`
			 */
			mobileBreakpoint?: HTMLElementTagNameMap['wa-page']['mobileBreakpoint'];
			/**
			 * @attribute `navigation-placement`
			 * @reflects
			 */
			navigationPlacement?: HTMLElementTagNameMap['wa-page']['navigationPlacement'];
			/**
			 * @attribute `disable-navigation-toggle`
			 * @reflects
			 */
			disableNavigationToggle?: HTMLElementTagNameMap['wa-page']['disableNavigationToggle'];
			pageResizeObserver?: HTMLElementTagNameMap['wa-page']['pageResizeObserver'];
			updateAsideAndMenuHeights?: HTMLElementTagNameMap['wa-page']['updateAsideAndMenuHeights'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			initialReflectedProperties?: HTMLElementTagNameMap['wa-page']['initialReflectedProperties'];
			/**
			 * @inheritedFrom WebAwesomeElement
			 */
			internals?: HTMLElementTagNameMap['wa-page']['internals'];
			/**
			 * @attribute `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: HTMLElementTagNameMap['wa-page']['dir'];
			/**
			 * @attribute `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: HTMLElementTagNameMap['wa-page']['lang'];
			/**
			 * @attribute `did-ssr`
			 * @reflects
			 * @inheritedFrom WebAwesomeElement
			 */
			didSSR?: HTMLElementTagNameMap['wa-page']['didSSR'];
		};
		attributes: {
			/**
			 * @property `view`
			 */
			view?: 'mobile' | 'desktop';
			/**
			 * @property `navOpen`
			 */
			'nav-open'?: boolean;
			/**
			 * @property `mobileBreakpoint`
			 */
			'mobile-breakpoint'?: string;
			/**
			 * @property `navigationPlacement`
			 */
			'navigation-placement'?: 'start' | 'end';
			/**
			 * @property `disableNavigationToggle`
			 */
			'disable-navigation-toggle'?: boolean;
			/**
			 * @property `dir`
			 * @inheritedFrom WebAwesomeElement
			 */
			dir?: string;
			/**
			 * @property `lang`
			 * @inheritedFrom WebAwesomeElement
			 */
			lang?: string;
			/**
			 * @property `didSSR`
			 * @inheritedFrom WebAwesomeElement
			 */
			'did-ssr'?: unknown;
		};
		events: {};
		cssProperties: {
			'--menu-width'?: string;
			'--main-width'?: string;
			'--aside-width'?: string;
			'--banner-height'?: string;
			'--header-height'?: string;
			'--subheader-height'?: string;
		};
		cssParts: {
			base?: string;
			banner?: string;
			header?: string;
			subheader?: string;
			body?: string;
			menu?: string;
			navigation?: string;
			'navigation-header'?: string;
			'navigation-footer'?: string;
			'navigation-toggle'?: string;
			'navigation-toggle-icon'?: string;
			'main-header'?: string;
			'main-content'?: string;
			'main-footer'?: string;
			aside?: string;
			'skip-links'?: string;
			'skip-link'?: string;
			footer?: string;
			'dialog-wrapper'?: string;
		};
		cssStates: {};
		slots: {
			''?: string;
			banner?: string;
			header?: string;
			subheader?: string;
			menu?: string;
			'navigation-header'?: string;
			navigation?: string;
			'navigation-footer'?: string;
			'navigation-toggle'?: string;
			'navigation-toggle-icon'?: string;
			'main-header'?: string;
			'main-footer'?: string;
			aside?: string;
			'skip-to-content'?: string;
			footer?: string;
		};
	};
	/**
	 * ```jsx
	 * class JsonSchemaFormWebawesome extends JsonSchemaFormElement (<jsf-webawesome>)
	 * ```
	 * ---
	 * Web Awesome implementation of the JSON Schema Form element.
	 * @summary Concrete Web Awesome form custom element.
	 * @fires `jsf-change` — {`CustomEvent`}
	 * @fires `jsf-input` — {`CustomEvent`}
	 * @attribute `accept-charset` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `action` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `autocomplete` — {`'off' | 'on' | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `data` — {`GenericData`} — _inherited from JsonSchemaFormElement_
	 * @attribute `enctype` — {`EncType | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `method` — {`'dialog' | 'get' | 'post' | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `name` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `novalidate` — {`boolean | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `schema` — {`ReadonlyJSONSchema7 | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `target` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @attribute `ui` — {`UiSchema`} — _inherited from JsonSchemaFormElement_
	 * @attribute `widgets` — {`typeof widgets`}
	 * @property `acceptCharset` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `action` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `autoComplete` — {`'off' | 'on' | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `data` — {`GenericData`} — _inherited from JsonSchemaFormElement_
	 * @property `debug` — {`boolean`} — _inherited from JsonSchemaFormElement_
	 * @property `encType` — {`EncType | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `form` — {`JsonSchemaFormEngine<Schema, Ui, Data> | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `method` — {`'dialog' | 'get' | 'post' | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `name` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `noValidate` — {`boolean | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `schema` — {`ReadonlyJSONSchema7 | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `target` — {`string | undefined`} — _inherited from JsonSchemaFormElement_
	 * @property `ui` — {`UiSchema`} — _inherited from JsonSchemaFormElement_
	 * @property `widgets` — {`typeof widgets`}
	 */
	'jsf-webawesome': {
		elementClass: HTMLElementTagNameMap['jsf-webawesome'];
		properties: {
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			acceptCharset?: HTMLElementTagNameMap['jsf-webawesome']['acceptCharset'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			action?: HTMLElementTagNameMap['jsf-webawesome']['action'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			autoComplete?: HTMLElementTagNameMap['jsf-webawesome']['autoComplete'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			data?: HTMLElementTagNameMap['jsf-webawesome']['data'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			debug?: HTMLElementTagNameMap['jsf-webawesome']['debug'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			encType?: HTMLElementTagNameMap['jsf-webawesome']['encType'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			form?: HTMLElementTagNameMap['jsf-webawesome']['form'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			method?: HTMLElementTagNameMap['jsf-webawesome']['method'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			name?: HTMLElementTagNameMap['jsf-webawesome']['name'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			noValidate?: HTMLElementTagNameMap['jsf-webawesome']['noValidate'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			schema?: HTMLElementTagNameMap['jsf-webawesome']['schema'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			target?: HTMLElementTagNameMap['jsf-webawesome']['target'];
			/**
			 * @inheritedFrom JsonSchemaFormElement
			 */
			ui?: HTMLElementTagNameMap['jsf-webawesome']['ui'];
			/**
			 * @summary Widget implementation map backed by Web Awesome components.
			 */
			widgets?: HTMLElementTagNameMap['jsf-webawesome']['widgets'];
		};
		attributes: {
			/**
			 * @property `acceptCharset`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			'accept-charset'?: string | undefined;
			/**
			 * @property `action`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			action?: string | undefined;
			/**
			 * @property `autoComplete`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			autocomplete?: 'off' | 'on' | undefined;
			/**
			 * @property `data`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			data?: unknown;
			/**
			 * @property `encType`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			enctype?: unknown | undefined;
			/**
			 * @property `method`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			method?: 'dialog' | 'get' | 'post' | undefined;
			/**
			 * @property `name`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			name?: string | undefined;
			/**
			 * @property `noValidate`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			novalidate?: boolean | undefined;
			/**
			 * @property `schema`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			schema?: unknown | undefined;
			/**
			 * @property `target`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			target?: string | undefined;
			/**
			 * @property `ui`
			 * @inheritedFrom JsonSchemaFormElement
			 */
			ui?: unknown;
			/**
			 * @property `widgets`
			 */
			widgets?: unknown;
		};
		events: {
			/**
			 * @summary Emitted when the underlying form engine dispatches a change event.
			 */
			'jsf-change'?: GlobalEventHandlersEventMap['jsf-change'];
			/**
			 * @summary Emitted when the underlying form engine dispatches an input event.
			 */
			'jsf-input'?: GlobalEventHandlersEventMap['jsf-input'];
		};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class NfBackgroundElement extends HTMLElement (<nf-background>)
	 * ```
	 * ---
	 * @attribute `pattern` — Grid pattern style.
	 * @attribute `no-rescale` — Disable adaptive coarsening of the grid when zoomed out.
	 * @property `slot`
	 * @property `flow`
	 * @property `pattern` — Grid pattern style.
	 * @property `noRescale` — Disable adaptive coarsening of the grid when zoomed out.
	 * @cssProperty `--nf-background-color`
	 * @cssProperty `--nf-background-grid-lines-color`
	 * @cssProperty `--nf-background-grid-line-width`
	 * @cssProperty `--nf-background-grid-line-spacing`
	 */
	'nf-background': {
		elementClass: HTMLElementTagNameMap['nf-background'];
		properties: {
			slot?: HTMLElementTagNameMap['nf-background']['slot'];
			flow?: HTMLElementTagNameMap['nf-background']['flow'];
			pattern?: HTMLElementTagNameMap['nf-background']['pattern'];
			noRescale?: HTMLElementTagNameMap['nf-background']['noRescale'];
		};
		attributes: {
			pattern?: unknown;
			'no-rescale'?: unknown;
		};
		events: {};
		cssProperties: {
			'--nf-background-color'?: string;
			'--nf-background-grid-lines-color'?: string;
			'--nf-background-grid-line-width'?: string;
			'--nf-background-grid-line-spacing'?: string;
		};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class NfCanvasElement extends HTMLElement (<nf-interactive-canvas>)
	 * ```
	 * ---
	 * @property `flow`
	 */
	'nf-interactive-canvas': {
		elementClass: HTMLElementTagNameMap['nf-interactive-canvas'];
		properties: {
			flow?: HTMLElementTagNameMap['nf-interactive-canvas']['flow'];
		};
		attributes: {};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class NfFlowElement extends HTMLElement (<nf-flow>)
	 * ```
	 * ---
	 * @slot `background`
	 * @slot `background-interactive`
	 * @slot `foreground`
	 * @slot `foreground-interactive`
	 * @property `flow`
	 */
	'nf-flow': {
		elementClass: HTMLElementTagNameMap['nf-flow'];
		properties: {
			flow?: HTMLElementTagNameMap['nf-flow']['flow'];
		};
		attributes: {};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {
			background?: string;
			'background-interactive'?: string;
			foreground?: string;
			'foreground-interactive'?: string;
		};
	};
	/**
	 * ```jsx
	 * class NfHandleElement extends HTMLElement (<nf-handle>)
	 * ```
	 * ---
	 */
	'nf-handle': {
		elementClass: HTMLElementTagNameMap['nf-handle'];
		properties: {};
		attributes: {};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class NfLinksElement extends HTMLElement (<nf-links>)
	 * ```
	 * ---
	 * @property `slot`
	 * @property `flow`
	 */
	'nf-links': {
		elementClass: HTMLElementTagNameMap['nf-links'];
		properties: {
			slot?: HTMLElementTagNameMap['nf-links']['slot'];
			flow?: HTMLElementTagNameMap['nf-links']['flow'];
		};
		attributes: {};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class NfNodeElement extends HTMLElement (<nf-node>)
	 * ```
	 * ---
	 * @property `node`
	 */
	'nf-node': {
		elementClass: HTMLElementTagNameMap['nf-node'];
		properties: {
			node?: HTMLElementTagNameMap['nf-node']['node'];
		};
		attributes: {};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
	/**
	 * ```jsx
	 * class NfPortElement extends HTMLElement (<nf-port>)
	 * ```
	 * ---
	 * @attribute `cableoffset`
	 * @property `port`
	 * @property `cableOffset`
	 */
	'nf-port': {
		elementClass: HTMLElementTagNameMap['nf-port'];
		properties: {
			port?: HTMLElementTagNameMap['nf-port']['port'];
			cableOffset?: HTMLElementTagNameMap['nf-port']['cableOffset'];
		};
		attributes: {
			cableoffset?: unknown;
		};
		events: {};
		cssProperties: {};
		cssParts: {};
		cssStates: {};
		slots: {};
	};
}
export type { CustomElementsExternals };
